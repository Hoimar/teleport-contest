interface:
  display_name: "Teleport Implementation Loop"
  short_description: "Run a sustained parity implementation loop in teleport-contest"
  default_prompt: "Use $teleport-implementation-loop to keep iterating on generalized parity for the AGENTS.md full-day marathon budget, refreshing the queue and stopping only when interrupted or at a valid global stop condition."

policy:
  allow_implicit_invocation: true
