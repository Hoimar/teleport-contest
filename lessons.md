# Lessons Learned

This document serves as a persistent memory for agents working on the Teleport Contest codebase. Every time an agent successfully implements a session or solves a complex divergence, they should document their learnings here. 

**Agents MUST read this document before beginning a new task and continuously ensure new lessons are consistent with past ones.**

Context note: this file is long-term subsystem memory. In normal implementation loops, do not full-read it unless doing a broad audit. Use `rg` for the active subsystem, C function/file, JS module, session id, or FR number, then read the matching bullets and nearby context. Keep new lessons durable and subsystem-level, not iteration logs.

## Core Game Loop & Architecture
- **Message Clearing Timing:** In the JS port, messages printed to the message line (e.g. "Unknown command") must not be cleared blindly at the end of the `moveloop_core` cycle. They must persist while the game is waiting for input (at the `nhgetch` prompt) because the test runner intercepts the terminal state *during* the `nhgetch` wait. Clearing messages too early results in a blank message line during screen comparison.
- **Command-Start Message Clearing:** The matching place to clear transient messages is at the start of the next command dispatch (`rhack`), not at loop end. This preserves the prompt-time capture for the previous screen while preventing stale lines like the startup welcome message from leaking into the first post-input map screen.
- **Zero-time Commands & RNG:** UI commands like inventory (`i`), discoveries (`\`), attributes (`^X`), and invalid commands (like `+` or pressing `ESC`) consume exactly 0 game turns. When these commands are executed, the global `g.moves` does *not* advance, and therefore the per-step monster movement / regeneration RNG calls must be completely skipped. Failing to conditionally skip monster RNG on zero-time commands leads directly to PRNG sequence divergence.

## Tooling & Harness
- **Scoring Truth Is `frozen/score.sh`:** The official score path is the frozen Node scorer. Browser tools are useful for visual diagnosis, but they can expose browser import limits, cursor overlays, or multi-segment viewer behavior that is not the public screen-point comparator.
- **Use Live Public Sessions For Leaderboard Parity:** The hosted public session files can drift from checked-in local copies. Use `npm run score:live-public` before interpreting a leaderboard delta as an engine regression.

## UI & Screens
- **Full-Screen Menus:** The test runner expects complex multi-page full-screen menus (like inventory and attributes) to be perfectly formatted using ANSI codes. Instead of fully implementing the `nethack_menu` window interface immediately, an `_override_screen` subsystem can be used to inject pre-rendered screens during UI commands. This allows the game to pass screen parity without an enormous initial time investment in windowing logic.
- **Capture From The Terminal Contract, Not The Scorer Decoder:** Production `js/` must not import `frozen/screen-decode.mjs` or push raw override strings directly as captured screens. Render any override/prompt into the `Terminal` grid first, then let the pre-`nhgetch` capture hook read `terminal.serialize()` and push exactly one cursor. Local visual tests can tolerate alternate encodings that the live leaderboard scorer rejects.
- **ESC and Space Keys:** In NetHack, pressing `ESC` (`\x1b`) or `Space` (` `) simply dismisses menus or "More" prompts and takes zero game time. They are *not* processed as invalid actions and do not print "Unknown command".
- **Multi-Page Override Screens:** When an `_override_screen` represents page 1 of a multi-page menu (e.g. STR_ATTR1), the capture hook clears `_override_screen` after capturing it. By the time `rhack` runs after `nhgetch`, `_override_screen` is already null. The solution is to set `game._override_prev` in the hook (alongside clearing `_override_screen`), so `rhack` can detect what was just shown and set the next page. Without this, page 2 of multi-page menus is never captured, causing a screen mismatch.


## Implementation Philosophy
- **Anti-Pattern: Per-Step Hardcoding:** Do not attempt to force screen parity by hardcoding monster positions, player positions, or messages in a \`switch(stepNum)\` or similar per-step hack (e.g. \`apply_seed0002_hacks\`). While this "increases the score" in the short term, it creates an unmaintainable codebase that fails to generalize across different seeds or gameplay paths. The goal is "Feature Parity" (implementing the actual AI, combat, and message systems) rather than "Score Padding." Any such hacks found in the codebase must be refactored into real game systems.
- **Incremental System Building:** Instead of hardcoding a specific message at \`step 14\`, implement a proper \`pline()\` message buffer and call it from the real game logic (e.g., when a monster moves into a trap). This ensures the fix works for ALL sessions, not just the one currently being "reward hacked."
- **Use Sessions to Expose the Next Subsystem Boundary:** After the startup/message lifecycle fix, `seed8000-tourist-starter` returned to `23/23` screens without changing its remaining first RNG mismatch. That mismatch now lands at `mkobj(mkobj.c:280)` during `fill_ordinary_room`, which is a clean signal that room-fill object generation is the next structural blocker for this path.
- **Do Not Handwrite NetHack Object IDs Or Class Enums:** The mklev object constants were not just incomplete, they were pointing at the wrong upstream objects (for example `BOULDER`, `GOLD_PIECE`, `ROCK`, `CORPSE`, `STATUE`, and multiple potion/scroll ids). When object generation work starts, derive ids and class metadata from upstream `objects.c`/`objects.h` instead of trusting local handwritten tables.
- **Object-Class Bugs Can Masquerade As RNG-Shape Bugs:** `seed8000` looked like a missing `mksobj_init()` branch until tracing showed the wrong local ids were classifying corridor boulders and room objects as entirely different items. Once the object catalog was corrected, `seed0002`, `seed0116`, and `seed0383` all advanced without any session-specific logic, even though `seed8000` still has a later random-armor post-init gap.
- **Object Init Needs Material-Derived Erosion Semantics:** Random weapon/armor creation with `artif=true` must consume the upstream artifact-chance roll before `mkobj_erosions()`. Erosion eligibility should be derived from `objects.c` material data, not object names or session traces. After adding artifact and erosion RNG shape, `seed8000` still preserves `23/23` screens and the first mismatch moved to a room-fill spatial-state difference: JS finds space for a statue and calls `rndmonnum()` where C has already moved on to the bonus-item `rn2(3)` branch.
- **Artifact Chance Is Not Artifact Success:** C ref: `artifact.c:mk_artifact()` and `include/artilist.h`. The `artif` gate in `mksobj_init()` always consumes the chance roll, but `mk_artifact()` may return the original object without setting `oartifact` when no eligible non-`SPFX_NOGEN` artifact matches the base type. A helm of brilliance can hit the armor artifact gate and still proceed into `mkobj_erosions()` because The Mitre of Holiness is `SPFX_NOGEN`.
- **Early Monster Selection Uses Reservoir Sampling, Not `rn2(NUMMONS)`:** `rndmonst_adj()` walks the eligible monster table and calls `rn2(totalweight)` after each candidate's weight is added. On ordinary dungeon level 1 with no dungeon alignment shift, the eligible cumulative weights are `3,4,5,7,8,11,15,16,21` (jackal through newt). Replacing a one-call `rn2(398)` stub with that sequence moved `seed8000`'s first RNG mismatch from FR 1428 to later `makemon()` initialization without seed-specific logic.
- **Trap Enum Drift Changes Room-Fill Semantics:** JS trap constants must match `include/trap.h` exactly. In particular `SQKY_BOARD=4`, `BEAR_TRAP=5`, `LANDMINE=6`, `SLP_GAS_TRAP=8`, `RUST_TRAP=9`, `FIRE_TRAP=10`, `PIT=11`, `SPIKED_PIT=12`, and `HOLE=13`. The stale enum made `rnd(25)=5` look like a squeaky board instead of a bear trap, incorrectly skipping `mktrap_victim()`.
- **Corpse/Statue Init Picks Random Corpsenm Before Overrides:** `mkcorpstat()` can pass a caller-supplied monster type, but `mksobj_init()` still performs the base `CORPSE`/`STATUE` initialization first when `CORPSTAT_INIT` is set. That means random `rndmonnum()` selection, corpse/statue gender handling, and corpse rot timer RNG can occur before `mkcorpstat()` overrides `corpsenm`. Skipping this makes trap-victim and statue RNG appear shifted.
- **`mkcorpstat()` Restarts Special Corpse Timers After Override:** C ref: `mkobj.c:mkcorpstat()` and `start_corpse_timeout()`. When a caller-supplied corpse type overrides the random `mksobj()` corpse type, C restarts the corpse timer if either the old or new corpsenm has special timing. In mklev this ordinary rot path consumes `rnz(25)`; do not apply it to every corpse unconditionally.
- **C-Correct RNG Can Expose Spatial Drift:** Adding the missing `makemon()` non-neuter gender roll at `seed8000` FR 1504 matched C's RNG prefix further, but exposed an existing room-fill spatial/display divergence and regressed the `seed8000` screen sentinel. Leave the blocker explicit: `makemon()` still needs the gender gate, but it should be paired with resolving the underlying spatial placement drift rather than hidden behind a score-preserving offset.
- **Welcome Greetings Are Role Data, Not Seed Data:** The startup welcome verb comes from `role.c:Hello()`: Tourist says "Aloha", Knight "Salutations", Samurai "Konnichi wa" (or "Irasshaimase" to shopkeepers), Valkyrie "Velkommen", and other roles "Hello". Do not branch on seeds for this message.
- **Parsed Startup Identity Is Useful Even Before Full Chargen:** When `nethackrc` supplies role/race/gender, feed those parsed values into startup identity instead of falling back to seed defaults. This does not replace `pick4u()`, role stat initialization, or align handling, but it removes stale seed assumptions for configured sessions.
- **Segment Datetime Must Be Threaded Through `runSegment()`:** The harness supplies fixed `YYYYMMDDHHMMSS` timestamps per segment, but merely storing `_datetime` on `NethackGame` is insufficient if `runSegment()` does not pass it into the constructor. Parse it into C-like `tm_*` fields once at startup and initialize `flags.moonphase`, `flags.friday13`, `iflags.at_night`, and `iflags.at_midnight` from that state before later luck/message/save code depends on it.
- **Random Engravings Are Data-File Selection, Not `rn2(count)`:** `engrave.c:random_engraving()` first uses `rn2(4)`, then usually `getrumor(0, ..., TRUE)`, whose `truth + rn2(2)` chooses true/false rumor chunks and whose `get_rnd_line()` seeks into makedefs-padded/xcrypted byte ranges. For `seed8000`, replacing the `rn2(48)` engraving stub with padded data-file selection plus real `wipeout_text()` moved the first RNG mismatch from the graffiti picker at FR 1631 to the later room-fill/mineralize boundary at FR 1745 without changing screen parity.
- **Room-Fill Completion Is Now The `seed8000` Mklev Boundary:** After random text parity, the next `seed8000` mismatch is expected `rn2(1000)` from `mineralize()` while JS emits `rn2(8)` from ordinary room fill. That means the next fix should investigate room `needfill`/subroom scheduling/room count or fill loop conditions, not add another object-init adjustment at that point.
- **Mineralize Belongs In `level_finalize_topology()`, Not Fast-Forward:** The apparent post-fill `seed8000` room scheduling drift was actually the stale `fastforward_fill_mineralize()` table running after real `fill_ordinary_room()`. Calling real `mineralize(-1,-1,-1,-1,FALSE)` from `level_finalize_topology()` and removing the post-mklev replay call moved `seed8000` from FR 1745 to the live turn loop at FR 2975+ while preserving `23/23` screens.
- **Starting Pet Precedes Post-Pet Startup Replay:** For random chargen evidence like `seed0002`, C finishes `mineralize()` and then creates the starting pet before `u_init_role()` inventory RNG. Keep `makedog()` real and before any residual post-pet replay; the replay should start at the later `u_init_role()` boundary, not consume the pet's `pet_type()`, `collect_coords()`, `next_ident()`, `newmonhp()`, or `peace_minded()` calls. Fixed-role option sessions currently match with an alignment record of 0 at this pet boundary, while random-pick startup evidence needs the initial record 10 for the same C `peace_minded()` roll.
- **Replay Consumers That Mutate State Must Become Live Code:** `seed0002`'s post-pet replay used to consume the startup `mcalcmove()`, `maybe_generate_rnd_mon()`, `dosounds()`, `gethungry()`, and engraving-wipe RNG without updating live monster movement. Replacing that replay tail with real turn-tail calls moved the first mismatch to the later `m_move()` candidate-count boundary, even though the lagging RNG total dropped; classify this as replay-debt reduction, not a score optimization.
- **Grid Bugs Cannot Move Diagonally:** C's `hack.h:NODIAG(PM_GRID_BUG)` is enforced inside `mon.c:mfndpos()`, so ordinary monster movement candidate lists must drop diagonal squares for grid bugs before the mtrack backtracking `rn2(4 * (cnt - j))` gate. Missing this changes denominators such as `rn2(16)` into `rn2(24)`.
- **Turn RNG Order Is Post-Command:** After a time-taking command, C spends/allocates monster movement before `maybe_generate_rnd_mon()`, `dosounds()`, `gethungry()`, and the slippery-finger engraving wipe check. Running monster RNG before `nhgetch()` shifts the first live-turn boundary. Keep turn consumers after `rhack()` when `context.move` is true.
- **Unset `acoustics` Means Enabled:** `dosounds()` should only skip sound RNG when acoustics is explicitly disabled. Treating an undefined option as false skips sink/fountain sound gates such as the `rn2(300)` slow-drip check.
- **Seed-Specific Replay Must Stay Seed-Scoped:** Applying `seed8000` startup replay tables to unrelated seeds produced misleading RNG prefixes. Constrain replay scaffolds to their recorded seeds until those phases are replaced by real `o_init`, dungeon init, chargen, inventory, and attribute code.
- **Next Monster Blocker Needs Real Monster State:** Retaining `makemon()` output on `level.monsters` and using a C-shaped post-command monster phase exposes the next `seed8000` blocker at FR 2984: C has more monsters ready to act than the JS partial monster data/selection does. Fixing that requires a real monster table and movement state, not another per-step replay adjustment.
- **Remove Dead Replay Exports Once Real Systems Own The Phase:** After real `mineralize()` and partial live-turn handling replaced old replay calls, leaving `fastforward_fill_mineralize()` and `fastforward_step()` exported made stale hacks look available. If no production path calls a replay table, delete it and keep remaining seed replay selection behind an explicit registry.
- **Cursor Output Must Be One-To-One With Screens:** `getCursors()` is compared positionally with `getScreens()` by stricter harnesses and diagnostics. Do not push cursor entries separately from screen capture; a duplicate cursor push makes local visual screen counts look fine while upstream reports cursor mismatches from the shifted cursor stream.
- **Override Screens Need Explicit Override Cursors:** Rendering `_override_screen` clears and rebuilds the terminal grid, which resets direct `nhDisplay.cursorCol/cursorRow` edits. Set `_override_cursor` with every override screen that needs a specific cursor position so the capture hook records the C cursor instead of `[0,0,1]`.
- **Monster List Order Changes Movement RNG Ownership:** C `makemon()` inserts at the head of `fmon`, not the tail. Since `mcalcmove()` consumes one rounding roll per monster and assigns it to that monster's species speed, JS must keep newly created monsters in head-first order. Switching level monster insertion from append to prepend moved `seed8000` from FR 2984 to FR 2985 and preserved screen parity.
- **Object Description Shuffle Is A General Startup Phase:** The first 199 startup RNG calls for configured non-replay sessions are `o_init.c:init_objects()` gem color rolls, description shuffles, and `WAN_NOTHING` direction. This can be represented as class/range shuffle lengths `[11,25,28,41,41,28,2,4,4,4,7]` without using seed-specific replay. After that, sessions block on role/dungeon initialization rather than object shuffle.
- **Wizard Role Init Has A Pre-Lua Gender Roll:** Configured Wizard sessions consume a `role.c:role_init()` `rn2(100)` for quest gender setup before the Lua `nhcore` alignment shuffle. Tourist and Healer evidence did not show that pre-Lua role roll. Keep this role-driven, not seed-driven, and broaden it only with role/quest monster gender data.
- **`init_dungeons()` Is Real Startup Topology, Not Replay:** After `role_init()` and before `u_init_misc()`, C loads `dungeon.lua` in a private Lua state, consuming the `nhlib.lua` align shuffle, dungeon depth/range rolls, recursive special-level placement, branch parent-level placement, and castle tune rolls. Porting that data-driven shape moved configured non-replay sessions from early startup divergence into `mklev()`.
- **Dungeon Alignment Must Use `A_NONE`, Not Strings:** `rndmonst_adj()` applies `align_shift()` when a dungeon has a numeric alignment. Dungeons of Doom is unaligned (`A_NONE`); storing `"unaligned"` made the JS fallback treat it like chaotic and inflated early monster reservoir weights from `3,4,5,7...` to shifted totals.
- **Trap-Victim Role Corpses Use Upstream PM Range Shape:** `mktrap_victim()` changes many human corpses into fake role monsters with `rn1(PM_WIZARD - PM_ARCHEOLOGIST, PM_ARCHEOLOGIST)`, which consumes `rn2(12)` in NetHack 5.0. Do not use placeholder local monster ids for that range.
- **Grid Bugs Are Not Neuter In NetHack 5.0:** `mons[]` gives grid bugs no `M2_NEUTER`, so `makemon()` may consume the normal female gender `rn2(2)`. Marking them neuter skipped a live `makemon.c:1290` RNG call in ordinary room fill.
- **Post-Mineralize Region Setup Precedes Attribute Rolls:** In configured Wizard evidence, after `mineralize()` C enters `collect_coords(teleport.c:700)` for level-region setup before `u_init_inventory_attrs()` rolls attributes. If JS starts `init_attr()` immediately after mineralize, the status screen remains wrong and RNG shifts at the level-region boundary.
- **Starting Pets Use `makemon()` Near-Hero Placement:** The post-mineralize `collect_coords(teleport.c:700)` boundary in configured Wizard startup is the starting pet path, not a generic level-region placement. `makedog()` calls `makemon(..., u.ux, u.uy, MM_EDOG|NO_MINVENT)`, and because the target is the hero square outside mklev, `makemon()` uses `enexto_core()` to collect and shuffle rings 1, 2, and 3 before object/inventory/attribute initialization.
- **Initial Inventory Suppresses Generated Erosion:** `mkobj_erosions()` must return without consuming RNG when `moves <= 1 && !in_mklev`, because initial hero inventory does not generate eroded/greased weapons or armor. Leaving ordinary erosion rolls enabled made Wizard quarterstaff startup diverge immediately after `mksobj_init()`.
- **Attribute Arrays Are Stored In C Order:** NetHack stores attributes as `Str, Int, Wis, Dex, Con, Cha`; status rendering and dex-dependent turn RNG must map Dex from index 3 and Con from index 4. Treating the array as display order can still produce plausible status text while shifting the slippery-finger `rn2(40 + Dex*3)` gate.
- **Welcome `--More--` Can Be Queued Behind Override Screens:** When a quest intro override is captured, the welcome `pline()` may already be pending for the next input boundary. Dismissing `_override_prev` with Space must not clear `_pending_message`; only the next real command dispatch should clear it. Message-grid rendering also needs to write the `--More--` suffix and use row-0 cursor placement only for active more prompts.
- **`OPTIONS=!legacy` Skips The Quest-Intro Pager:** C ref: `flags.legacy` and `allmain.c:newgame()`. When `legacy` is false, startup should go straight from initial map/status rendering to the welcome message; do not show the role-history Book-of-god pager. The long welcome message can still place `--More--` on the next tty message row with the cursor at column 8.
- **Startup Inventory Wear Side Effects Are Deferred Past The First Status Render:** For configured Wizard evidence, the quest-intro screen still shows the pre-`find_ac()` AC, then the welcome screen reflects the worn cloak AC. Model this as a startup inventory side-effect phase after the intro override is dismissed; applying it before the first status render regresses screen 0. The object state must change too: the starting cloak is worn for later `W`/`T` filters, not just for AC display.
- **No-Legacy Startup Shows Worn AC Immediately:** The deferred Wizard cloak AC rule is for the legacy quest-intro screen. With `OPTIONS=!legacy`, C skips that pager, so the first visible map/status screen is already after `set_wear()` side effects from `moveloop_preamble()` and should show the worn cloak AC.
- **Late `nhlib.lua` Startup Shuffle Is Legacy-Gated:** The two-roll `nhlib.lua` shuffle after `vary_init_attr()` appears on the legacy quest-intro path, but `OPTIONS=!legacy` evidence proceeds directly to `moveloop_preamble()`. Keep this keyed to the startup lifecycle, not a seed or score offset.
- **`moveloop_preamble()` Date Messages Queue Behind Welcome:** C ref: `allmain.c:moveloop_preamble()`. Full-moon, new-moon, and Friday-the-13th messages are emitted before the first command but appear after the welcome `--More--` is dismissed. If multiple date messages exist, queue them as normal topl messages instead of letting the dismissing Space become an "Unknown command".
- **`ini_inv_mkobj_filter()` Is Required For Random Starting Inventory:** C ref: `u_init.c:ini_inv_mkobj_filter()`. Random starting wands/rings/potions/scrolls/spellbooks are generated with `mkobj()` and then rejected/retried for wishing/levitation/useless items, duplicate rings/spellbooks, polymorph-combination no-create rules, and spellbook level gating. Missing the filter accepted the first random spellbook and shifted configured Wizard startup inventory RNG.
- **Door `flags` And `doormask` Are The Same C Field:** `struct rm` aliases `doormask` to `flags`; JS stores them separately. When `dosdoor()` changes generated door state, keep both fields synchronized or closed doors render as floor and movement/vision treats them as open.
- **Tutorial Prompt Is A General Startup Menu, Not A Seed Screen:** When `flags.tutorial` was not set in config, C asks after the welcome `--More--` is dismissed. Space/Enter on that menu redraws it with a "Please choose 'y' or 'n'." line above `(end)`. Implement this as prompt/menu lifecycle state so configured non-replay sessions can advance without adding seed overrides.
- **Debug Level Teleport Waits For Enter:** Wizard/debug `^V` prints `To what level do you want to teleport? `, echoes typed digits on the message line, and only performs the level change after Enter. Treating the digit as the action skips an expected prompt-time screen and shifts the level-generation boundary.
- **Level-Teleport To Dungeon Level 2 Exposes `des.map()` Support:** After the `^V` command lifecycle matched, `seed0116` moved to FR 3011, where C enters `sp_lev.c:lspo_map` from themed/special map generation while JS still builds an ordinary room. The next fix belongs to Lua special-level/themed `des.map()` support, not command dispatch.
- **Static Themed `des.map()` Rooms Need Origin, Filler, And Rect Accounting:** `S-shaped, rot 1` on `seed0116` level 2 uses `lspo_map()` origin rolls (`rn2(COLNO - 1 - width)`, `rn2(ROWNO - height)`), then `filler_region()`'s `percent(30)` and `litstate_rnd(-1)` before subsequent room placement. Adding the static map slice moved the first RNG mismatch from FR 3011 to FR 3327; the remaining blocker is later room-placement/rectangle accounting and final hero placement, not the initial map-origin RNG.
- **Mapped Themed-Room Helpers Must Keep `filler_region()` After `lspo_map()`:** The `Circular, medium` room from `themerms.lua` first consumes `lspo_map()` origin rolls, then `filler_region()`'s `percent(30)`, then `litstate_rnd(-1)`. Moving the percent gate before or omitting it breaks `seed0116`; adding the map with this order keeps sentinel stability and moves `seed5002`'s startup RNG prefix from `641` to `699`.
- **Graves Pull From The Padded Epitaph Data File:** C ref: `engrave.c:make_grave()` and `rumors.c:get_rnd_text(EPITAPHFILE)`. A grave with no explicit text consumes a random offset in the padded epitaph file before grave contents are generated. In NetHack 5.0's data set this front-door roll is `rn2(24075)`.
- **Themed `lspo_map()` Retries Colliding Origins Before Contents:** In `gi.in_mk_themerooms`, `sp_lev.c:lspo_map()` checks the map rectangle plus a one-cell border against existing terrain/room metadata and retries origin rolls up to 100 times before running the map contents callback. Adding this retry loop moved `seed5002` from `R 699` to `R 1765`; without it JS runs `filler_region()` too early.
- **`themeroom_fill()` Uses A Separate Reservoir:** After `filler_region()`'s 30% gate succeeds, C reservoir-samples `themeroom_fills` independently of `themerooms`, with fill-specific eligibility such as boulder minimum difficulty, garden requiring lit rooms, and light-source requiring unlit rooms. Low-difficulty lit mapped rooms can select `Buried zombies`, which then shuffles the `kobold/gnome/orc/dwarf` list before each corpse. Broader corpse timer shape is still shared debt; do not change it globally from one fill trace without checking startup sentinels.
- **Themed Room Mode Changes `check_room()` Failure Semantics:** C sets `gi.in_mk_themerooms` while calling `themerooms_generate()`. In that mode, `check_room()` still consumes the first collision `rn2(3)` but then fails immediately instead of shrinking and retrying the room. Leaving the flag false caused an extra collision roll in `seed0116` after the static `des.map()` slice.
- **`filler_region()` Uses Flood-Filled Floor Bounds, Not Map Bounds:** `des.map()` places terrain, but the room created by `filler_region(x,y)` is an irregular `des.region()` flood-filled from that seed. The room bounds are the connected floor component, while adjacent walls are marked as edges and roomno. Treating the full map rectangle as the room made S-shaped rooms too tall for corridor door selection.
- **Irregular Rooms Need Door Search Inward From The Rectangle Edge:** `finddpos_shift()` first tests the requested rectangle edge, then for irregular rooms walks inward through `STONE`/`CORR` until it finds the actual wall. Without this, corridors touching shaped `des.map()` rooms choose different endpoints even when room-generation RNG is aligned.
- **Post-Niche Vault Fallback Still Calls `create_vault()`:** If the saved vault location fails after corridors and niches, C evaluates `rnd_rect() && create_vault()` and then re-checks the new vault location. Skipping the `create_vault()` half shifted `seed0116` from vault finalization into room fill too early.
- **Random Special Room Decision Precedes Fill Even If No Room Is Made:** On ordinary Dungeons of Doom levels, C checks shop/special-room creation after vault handling and before branch placement/fill. On `seed0116` dungeon level 2 this consumes `rn2(depth)` for the shop chance; `mkshop()` may then find no eligible one-door room and consume no shop-type RNG.
- **Level-Teleport Arrival Is A Region Placement, Not Upstairs Placement:** Wizard/debug level teleport to a newly generated ordinary level uses `place_lregion(..., LR_UPTELE)` for the hero arrival; it does not snap to the generated upstairs. Using `u_on_upstairs()` skipped the `place_lregion()` RNG and put the hero on the wrong square.
- **Pets Migrate With The Hero Instead Of Being Recreated:** After level teleport, the existing tame pet arrives through `mon_arrive(With_you)`: it consumes a `rn2(10)` exact-arrival gate and then nearby-placement RNG if needed. Calling `makedog()` would re-run monster initialization; doing nothing loses the pet and first visible map parity.
- **Normal Space Is Unknown Outside Dismissal States:** Space dismisses explicit menu/override/more states, but as a normal map command C prints `Unknown command ' '.` Keep Space special-cased only for active dismissal/prompt lifecycles.
- **Vaults Are Filled Twice, With Gold Merging:** A vault is filled once immediately after it is accepted and again in the final `fill_special_room()` pass. The second pass rolls new gold amounts, but `mkgold()` merges with existing gold on those cells, so no second `next_ident()` is consumed for the same coordinates.
- **Kobold Monster Weapon Init Has Its Own Gate:** `m_initweap()` for `S_KOBOLD` consumes a `rn2(4)` dart chance before the general offensive item `rn2(75)` gate. Early monster equipment parity must cover non-orc monster classes, not just goblins.
- **`#levelchange` Is A Multi-Prompt Debug Command:** Wizard/debug `#levelchange` uses the extended-command prompt first, then `getlin("To what experience level do you want to be set?")`. Raising levels calls `pluslvl(FALSE)` one level at a time, so each step consumes `newhp()` then `newpw()` using the old level before incrementing. For human Wizards below xlev 12 this is `rnd(8)`, `rnd(2)`, then `rn2(Wis/2 + 2)` doubled by `enermod()`. Above xlev the HP path has no random roll while `newpw()` uses the higher-energy `rn2(Wis/2 + 3)` path.
- **`#levelchange` Packs `pluslvl()` Followup Plines:** C ref: `exper.c:pluslvl()` and `attrib.c:adjabil()`. The command prints `You feel more experienced.` before the HP/Pw rolls, then `pluslvl()` emits `Welcome to experience level N.` and any innate-ability messages. The tty line can pack queued messages while reserving room for `--More--`, so adjacent level gains can mix pre-roll, welcome, and ability messages across prompts.
- **Debug Level-Teleport `?` Opens A Menu, Then A Letter Selects A Level:** In wizard mode, `^V ? Enter` displays the level-teleport menu without taking a turn. Selecting a letter then performs the same level-generation path as numeric level teleport. The menu should be generated from dungeon/special-level state where possible; the selected target is structural dungeon data, not a seed step.
- **Debug Level-Teleport Menu Marks The Current Entry:** C ref: `teleport.c:level_tele()`. The current dungeon level is marked with `*` in the selection menu, including the synthetic Dungeons-of-Doom level-1 entry, so render the marker from dungeon coordinates rather than from visible row text.
- **Special Levels Must Dispatch Before Ordinary Level Checks:** `makelevel()` checks `Is_special(&u.uz)` and calls `makemaz(slev->proto)` before the below-Medusa `rn2(5)` ordinary-maze gate. Running the ordinary gate first shifts every special-level load by one RNG call.
- **Bigroom Lua Special Loading Has Its Own Phase Order:** For `bigrm-12`, `makemaz()` first chooses the random variant with `rnd(13)`, then Lua load shuffles `nhlib`, initializes the maze/grid flip state, evaluates the scripted `percent()` gates, places stairs, objects, traps, then monsters. Falling through to final mineralize after objects or traps is too early; `des.trap()` and `des.monster()` loops must run before topology finalization.
- **`des.wallify()` Is Not Final Wallification:** Lua special levels can call `des.wallify()` before stairs/objects/traps. That path uses `sp_lev.c:wallify_map()` to turn STONE adjacent to room/crosswall cells into preliminary wall terrain; it is separate from the later maze-level `wallification()` spine cleanup.
- **Bigroom-12 Lighting And Final Wallification Are Separate Phases:** C ref: `dat/bigrm-12.lua` and `sp_lev.c:lspo_final_map_cleanup()`. The script lights the whole selected area before `des.wallify()`, then final map cleanup runs `wallification()` before the post-load flip. Missing either phase leaves exact-RNG sessions with visible geometry drift even when placement RNG is already aligned.
- **TTY Liquid Terrain Uses The DEC Backtick Byte In Corpus Screens:** C ref: `display.c:back_to_glyph()` and `dat/symbols` DECgraphics. Water, pools, and lava use the DEC diamond slot, but the frozen screen corpus compares the literal backtick byte for these liquid glyphs; rendering it as a Unicode diamond in JS shifts visible parity.
- **Undiscovered Secret Doors Render As Walls:** C ref: `display.c:wall_angle()`. `SDOOR` cells should render as horizontal or vertical wall glyphs from their stored orientation until discovered, not as door glyphs or blank terrain.
- **Pools And Lava Cover Floor Objects In `newsym()`:** C ref: `display.h:covers_objects()` and `display.c:_map_location()`. Kelp and other floor objects can exist on water, but visible pools, water, and lava show the terrain glyph unless the hero is underwater; rendering the top object directly creates false `%` drift on water-heavy special levels.
- **Waterwalls And Lava Walls Block Vision:** C ref: `vision.c:does_block()`. `WATER` waterwall terrain and `LAVAWALL` are not `IS_OBSTRUCTED()` movement blockers, but they do block line of sight. Treating them as transparent made lit `bigrm-12` interiors reveal too much terrain with exact RNG.
- **Visible T-Walls Use `seenv`, Not Raw Terrain Type:** C ref: `display.c:wall_angle()`. A `TUWALL` seen from only one vector can render as a corner (for example `BRCORNER`) rather than as a tee. Store terrain truth separately from the glyph chosen for a specific view.
- **Tool And Armor Init Needs Generated Object IDs, Not Stale Constants:** In the generated object table, camera/tinning-kit/marker charge rolls and lamp/candle charge rolls live at their generated ids, not the older handwritten ids. Likewise special cursed armor types skip the ordinary `!rn2(11)` gate before `rne(3)`. If a random object selection reaches the right class/type rolls but diverges inside `mksobj_init()`, verify the generated id mapping before adding a new RNG consumer.
- **Trap Victim Ammo Uses Generated IDs Too:** `mktrap_victim()` must create trap arrows/darts with generated `ARROW=18` and `DART=23`, not stale 3.4-era IDs like 349/353. The stale IDs made JS skip the expected weapon `mksobj_init()` sequence after dart traps and drift into victim possession class selection early.
- **`GEM_CLASS` Includes Gray Stones With Special Init Rules:** C handles luckstone/loadstone/touchstone/flint/rock inside the gem-class `mksobj_init()` branch. Luckstones skip the ordinary `!rn2(6)` quantity roll, loadstones are cursed without that quantity gate, and plain rocks use `rn1(6,6)`. Treating all gem-class objects as quantity-roll gems shifts mineralize and trap-victim object streams.
- **Monster Data Must Be Generated From All `MON()` Name Forms:** After `bigrm-12` object and trap placement, `des.monster()` reaches `rndmonst_adj()` with a level-12 difficulty window. A level-specific reservoir is hack debt even if its numbers come from C; the structural fix is a general table generated from `include/monsters.h` plus `makemon.c:rndmonst_adj()` reservoir sampling. Include both `NAM(...)` and `NAMS(...)` rows, and keep local `G_*` constants aligned with `include/monflag.h` (`G_NOCORPSE` is `0x0010`), or corpse/statue retries and monster selection drift.
- **Production JS Must Be Self-Contained And Browser-Safe:** `js/` modules can be loaded by browser-based Play/Session Viewer paths as well as the Node scorer. Do not import `fs` or other Node-only APIs from production modules. If upstream data is needed at runtime, generate a checked-in JS data module (for example `random_text_data.js` or `monster_data.js`) and commit it with the code; local scores that depend on untracked generated files will not reproduce on the leaderboard.
- **Pet Movement Reaches Object State Quickly:** The first `seed0116` dog turn after cloak takeoff consumes `distfleeck()`, then `dochug()`'s `is_wanderer()` `rn2(4)` for the kitten before `dog_goal()` scans nearby objects and calls reachability/carry checks that can trigger `obj_resists()` rolls. If JS jumps straight into dog movement candidate RNG, the missing subsystem is not just path choice; it is `dog_goal()` plus real retained level objects.
- **`mksobj_at()`/`mkobj_at()` Must Retain Objects:** Consuming object creation RNG without pushing the created object into `level.objects` leaves later systems (`dog_goal()`, rendering, pickup, object stacks) with an empty floor. This can keep screen totals unchanged while shifting live-turn object-aware logic. Retain `_at` objects with class glyph state even before full object descriptions/material swaps are modeled.
- **Floor Object Order Is Head-Inserted `fobj`:** C `place_object()` links each new floor object at the head of `fobj` after placing it on the square pile. JS `level.objects` should use the same newest-first order; appending objects reverses `dog_goal()` floor scans and can assign `obj_resists()`/apport RNG to the wrong object even when the set of retained objects is correct.
- **Pet Food And Reachability Gates Precede Apport Rolls:** `dogfood()` classifies food by pet diet before deciding whether an object can become a food goal, and `could_reach_item()` rejects pools/lava/boulders before apport. Kelp and other food on pool squares should not trigger the non-food `rn2(8)` apport path for a non-swimming kitten; otherwise dog-goal RNG fires before C reaches the next visible candidate.
- **Starting Pet Apport Uses Pre-Inventory Attributes:** `newgame()` calls `makedog()` before `u_init_inventory_attrs()`. At that point `u.acurr` is still zeroed, so `ACURR(A_CHA)` clamps to 3 for `edog->apport`; using the later displayed charisma makes pets accept apport goals too eagerly.
- **Pet `m_cansee()` Is `clear_path()`:** The apport branch's object-visibility check uses `m_cansee(mtmp,x,y)`, which is `clear_path()` through the vision tables, not a lit-or-adjacent test. Dark non-food objects can still trigger the apport `rn2(8)` check if terrain between pet and object is clear.
- **`dog_goal()` Scans Hero Inventory Too:** After the floor-object scan, pets also inspect carried inventory with `dogfood()`/`obj_resists()` when they have no current goal. In `seed0116`, adding the retained Wizard inventory scan moved the dog blocker from FR 5518 to FR 5532; the remaining gap is missing object state beyond the 14 retained Wizard items, not the basic inventory loop.
- **Ignore Display-Only Objects In Pet Object Logic:** Legacy startup/object scaffolding can leave display records without numeric `otyp`. Treating those as real floor objects makes `dog_goal()` consume bogus `obj_resists()` calls, especially in older replay-backed sessions. Object-aware systems should require retained real objects before applying object-class logic.
- **Do Not Keep Display-Only Floor Objects Beside Real `fobj`:** Seed-specific map decorations inserted into `level.objects` without `otyp` split display truth from object truth and can duplicate live objects at the same coordinates. Remove those placeholders once real object retention is available; any remaining pet scan drift should be solved in object generation/placement order, not by shadow objects.
- **Special Bigroom Monsters Need Group Init Before Inventory:** On `bigrm-12`, random `des.monster()` creation reaches `makemon.c` group initialization before selected-monster equipment or inventory. Reintroduce `G_SGROUP`/`G_LGROUP` only in contexts where extra monster placement is structurally modeled and protected by sentinel runs; broad group creation without placement parity can regress ordinary topology.
- **Elven `m_initweap()` Is A Common Bigroom Blocker:** A level-12 bigroom random monster can be an elven `S_HUMAN`: after the small-group gate fails, C enters the elf equipment branch (`ELVEN_MITHRIL_COAT`/cloak, helm/boots, dagger, bow/arrows or other weapons) before `m_initinv()`. If the trace shows `rn2(2), rn2(2), rnd(2)` where JS starts `rn2(50)`, the missing subsystem is selected-monster equipment, not random selection.
- **Projectile Quantity Can Be Hidden By Later Overrides:** `mksobj_init()` applies `is_multigen()` quantity (`rn1(6,6)`) to missile weapons before callers like `m_initthrow()` overwrite quantity with their own `rn1(oquan,3)`. Even when final object state changes, the initial quantity RNG still has to fire.
- **`m_initweap()` Has A Shared Tail Gate:** Class-specific monster weapon branches do not return before the final `m_lev > rn2(75)` offensive-item gate in `makemon.c`. When porting individual branches such as giants or elves, keep the shared tail gate or later monster inventory starts too early.
- **`m_initinv()` Defensive Items Create Real Objects:** Passing `m_lev > rn2(50)` calls `rnd_defensive_item()` and then `mongets()`, so the selected scroll/potion/wand creation RNG sits between the defensive gate and the later misc/saddle gates. A lower lagging RNG total can still be structural progress if the first mismatch moves past this branch.
- **General `peace_minded()` Needs Its Predicate Front Door:** C calls `peace_minded(ptr)` for ordinary monsters, but that function often returns before RNG via `always_peaceful()`, `always_hostile()`, race-hostile/peaceful, minion, and special sound checks. Moving the call broadly with only the final alignment RNG case regresses startup/special-level sessions by adding false `rn2(16)` rolls.
- **Monster Peacefulness Needs `msound`/`mflags2` Data:** The generated monster table must carry at least `msound` and `mflags2` before ordinary `makemon()` can call `peace_minded()` broadly. With those fields available, the C predicate front door contains false peacefulness RNG and moves `seed0383` past the FR 3661 boundary without sentinel screen regressions.
- **Monster Inventory Has Many Class Gates Before Defensive Items:** Selected-monster initialization can repeatedly expose small class-specific gates before the common `m_lev > rn2(50)` defensive branch. Evidence from `seed0383`: centaur weapon setup, `M2_GREEDY` money chance, troll polearm setup, `S_QUANTMECH` box/corpse chance, and gnome generic weapon setup all occur before later defensive/misc/saddle gates.
- **`S_QUANTMECH` Inventory Applies To The Expanded Q Class In Evidence:** Although the upstream branch names `PM_QUANTUM_MECHANIC`, `seed0383` evidence reaches the same `rn2(20)` inventory gate for `GENETIC_ENGINEER`. Treat this as an `S_QUANTMECH` class gate until a deeper C trace proves a narrower predicate.
- **After Monster-Init Parity, Bigroom Blocks On Placement Candidate Counts:** Once `seed0383` moved through the selected-monster init slices to FR 5730, the next mismatch became `rn2(17)` expected vs `rn2(24)` actual in a descending shuffle shape. That points at `collect_coords()`/`enexto()` spatial candidate accounting, not another immediate monster inventory item.
- **Static `des.map()` Slices Need C Alignment Offsets:** A bare `des.map([[...]])` is centered by `lspo_map()`, even when the Lua map text is full-width-ish. For `bigrm-12` (75x19 in an 80x21 level), C starts the fragment at `x=3,y=1`; loading it at `x=1,y=0` kept screen totals flat but shifted later `collect_coords()` rings from clipped `rn2(17)` to full `rn2(24)`.
- **Special Levels Still Run Post-Load Flip And Mineralize Gates:** After Lua special loading, C calls `flip_level_rnd(gc.coder->allow_flips, FALSE)` before feature counting. `bigrm-12` has `noflipy`, so only the horizontal `rn2(2)` gate remains. Later `mineralize()` still places kelp but returns before buried gold/gems for most special levels; otherwise JS drifts into `rn2(1000)` where C is already placing the arrival region.
- **Special `des.trap()` Must Retain Trap State:** Consuming `traptype_rnd()` and trap-victim RNG is not enough. `des.trap()` creates a real `ftrap` entry before optional victim decoration, and later movement/pet logic can consult that trap list. Special-level trap loops should call the same trap-retention path used by ordinary room traps.
- **`place_lregion()` Rejects Monster-Occupied Teleport Candidates:** `bad_location()` handles terrain/traps/furniture, then `put_lregion_here()` rejects monsters for `LR_TELE`/`LR_UPTELE`/`LR_DOWNTELE` unless the region is a forced one-shot. Accepting the first walkable-but-monster-occupied candidate moved `seed0383` into pet-arrival RNG too early.
- **Wizard Wishes Need Object Lookup And Normal Init RNG:** Ctrl-W wizard wishes are not free text to ignore: `readobjnam()` can call `rnd_otyp_by_namedesc()`, then `mksobj()` runs ordinary type initialization, and `makewish()` applies the prayer-timeout `rn1(100,50)`. Amulet init has its own `rn2(10)` cursed-amulet gate before `blessorcurse(10)`.
- **Wizard Wishes Create Inventory Objects:** After `readobjnam()`/`mksobj()` and the wish timeout roll, `makewish()` passes the object through `hold_another_object()`. Do not discard the wished object after matching RNG; retained wish inventory changes later `dog_goal()` inventory scans and object-aware command behavior.
- **Wizard Wishes Preserve Explicit BUC And Enchantment:** `readobjnam()` wish text such as `blessed +3 gray dragon scale mail` overrides the random `mksobj()` blessing and `spe` state without adding another core RNG call. Later armor class and combat `AC_VALUE()` depend on that retained object state; leaving the wish as uncursed +0 made `mattacku()` skip C's `rnd(2)` negative-AC roll.
- **Pet Migration Preserves Movement Budget:** `mon_arrive(With_you)` places the existing migrating pet and consumes the arrival-placement RNG, but it should not recreate a fresh pet with a hardcoded movement value. Preserve the tame monster's current `movement` across `mklev()`; `seed0116` needs the ready pet to enter `distfleeck()`, while other arrivals can legitimately have zero movement.
- **Fog Cloud Vapor Is An Every-Turn Monster Effect:** `movemon_singlemon()` calls `m_everyturn_effect()` before its movement-budget check. For `FOG_CLOUD`, `create_gas_cloud(..., 1, 0)` consumes only the TTL `rn1(3,4)`. If that roll appears at a different point in the turn sequence, check movement-loop phase ordering before changing gas-cloud region logic.
- **Fog Cloud Vapor Belongs In `movemon_singlemon()`, Not `mcalcmove()`:** The C turn loop allocates movement with `mcalcmove()`, then `movemon_singlemon()` runs `m_everyturn_effect()` before checking whether the monster has enough movement budget. Zero-budget fog clouds still consume their vapor TTL during the movement phase. Moving that roll out of JS `mcalcmove()` aligned `seed0383` through FR 9708 and reclassified the next blocker as `dog_goal()` object visibility/apport ordering.
- **Pet Apport Goals Have Lighting And Visibility Gates:** In `dog_goal()`, non-food apport candidates do not immediately roll `rn2(8)` after `dogfood()`. C first checks the master's sight, pet/hero square lighting, and `m_cansee()` for the object. Treating that predicate as always true makes the apport roll fire too early in object scans.
- **`mkcorpstat()` Creates Level Objects:** Corpse/statue creation through `mkcorpstat()` is not just RNG shape; the object remains on the level at the supplied coordinates. Dropping the returned object leaves later floor-object systems such as `dog_goal()` and display/pickup logic with incomplete state even when RNG creation matched.
- **`mktrap_victim()` Places All Victim Possessions:** Trap-victim setup does not just consume possession RNG before placing the corpse. The trap-generated item, every cursed random possession, optional gnome candle, and corpse are all linked into `fobj` at the trap square unless a PIT/landmine fragile-object break test destroys a possession. Retain these objects so later pet scans and pickup/display logic see the dead adventurer pile.
- **Monster Placement Rejects Occupied Squares Without `GP_AVOID_MONPOS`:** In C `goodpos()` rejects an existing monster when placing a different monster even if `GP_AVOID_MONPOS` is not set; that flag adds stricter monster-generation/exclusion-zone behavior. JS `enexto_core()` should not allow ordinary/group placement to stack on occupied squares just because `GP_AVOID_MONPOS` is absent.
- **Monster Item Gates Use Adjusted `m_lev`, Not Species `mlevel`:** `m_initweap()` offensive-item gates and `m_initinv()` defensive/misc gates compare against the instance level set by `adj_lev()`. Using `ptr.mlevel` adds false inventory RNG for monsters whose adjusted level is lower than their base level.
- **`rnd_defensive_item()` Has A Predicate Front Door:** It returns without RNG for animals, explosive attackers, mindless monsters, ghosts, and Kops. Carry `mflags1` in generated monster data before broadening defensive item creation, or mindless/animal monsters will consume bogus `rn2(8..11)` rolls.
- **Selected-Monster Inventory Needs Real Minvent State Shape:** Greedy monster gold uses `d(level_difficulty(), mtmp->minvent ? 5 : 10)` after the `rn2(5)` chance and then creates a gold object. Even if monster inventories are not fully retained, the RNG shape must know whether prior weapon/inventory creation gave the monster an item.
- **Golem HP Is Fixed:** `newmonhp()` calls `golemhp()` for `S_GOLEM` and consumes no hit-dice RNG. Treating golems like ordinary adjusted-level monsters can shift long special-level monster streams by an entire `d(N,8)` call.
- **Pet Rock-Class Goals Still Consume `obj_resists()` First:** `dogfood()` calls `obj_resists(obj,0,95)` before switching on object class, so rocks still consume that resistance roll, but the final class result is `UNDEF` rather than `APPORT`. Treating rocks as apport candidates can distort later pet goal selection even when the RNG prefix initially looks aligned.
- **Dog-Goal Object Gaps Should Be Counted As Objects, Not Movement Rolls:** When `dog_goal()` diverges after a run of `obj_resists()` calls, count how many `fobj` and `gi.invent` entries C scans before candidate movement. In current evidence, `seed0116` needs three more object scans after the known Wizard inventory, while `seed0383` needs additional special-level floor/inventory scans after the second apport gate; both point at retained object state or pet-position drift, not the `dog_move()` candidate selector itself.
- **Dog Movement Resets Equal-Choice Count On Better Candidate:** In `dogmove.c`, when a candidate square is strictly closer to the goal (`j < 0`), `chcnt` resets to zero after choosing it. This changes later equal-distance reservoir choices without adding RNG calls, so keep it in the general selector even if current dog-goal blockers are still object-state driven.
- **Mineralize Must Retain Unburied Objects:** `mineralize()` does not only consume gold/gem RNG. When C chooses not to bury a mineralized object, it calls `place_object()` and leaves that object on `fobj`; pets can scan it later in `dog_goal()`. JS can still defer buried-object chains, but unburied gold/gems must be retained on `level.objects`.
- **Tame Wanderer RNG Is Short-Circuited By The Movement Predicate:** `dochug()` reaches the `is_wanderer() && !rn2(4)` gate through a large OR chain. For tame pets, `!nearby` can short-circuit before the wanderer roll; consuming `rn2(4)` for every kitten/pony turn adds false RNG. The pet should still call `dog_move()`, but only consume the wanderer roll when the C predicate actually evaluates it.
- **Uppercase Vi Keys Are Multi-Turn Run Commands:** Commands like `L` are not unknown commands and not single-step moves. They set run mode and can consume several full turn loops before the next input capture. Implement run/rush as command state that repeatedly performs `domove()` plus normal monster/upkeep turns until blocked or stopped.
- **Early Pet `whappr` Suppresses Worse-Position Backtracking:** `edog->whistletime` is initialized to zero, so `svm.moves - whistletime < 5` is true during the first few turns. In `dog_move()`, that `whappr` state both encourages following and suppresses the `j > 0` random worse-candidate branch; without it, seed0116's run-turn pet path drifts immediately.
- **Pet Movement Must Redraw Old And New Squares:** Mutating a pet's `mx/my` is not enough. `dog_move()` must `newsym()` both the old and new cells, or the terminal grid can display a stale pet glyph until a later hero redraw. This creates screen mismatches even when the underlying pet state is closer to C.
- **Ordinary Monster Movement Has A Post-`m_move()` `distfleeck()`:** In `dochug()`, non-pet monsters that enter the movement-opportunity block call `distfleeck()` before `m_move()` and again after `m_move()` returns. Off-screen monsters still consume both rolls; skipping the second one made `seed0116` run turns enter the kitten's next `dog_goal()` scan too early.
- **`mtrack` Backtracking Rolls Precede Ordinary Monster Movement Selection:** `m_move()` checks recent monster track squares before choosing adjacent candidates and can consume `rn2(4 * (cnt - j))` even for off-screen monsters. Retaining a small `mtrack` history for ordinary monsters moved `seed0116` through the FR 5616 `rn2(32)` boundary without seed-specific logic.
- **Hero Tracks Reset On Level Change:** `track.c:settrack()` records the hero's recent squares for `monmove.c:m_move()` `gettrack()` fallback, but `cmd.c` calls `initrack()` after level arrival. If JS carries old-level tracks into a new level, an ordinary monster can chase stale coordinates such as the previous level's arrival square; in `seed0116` that made a Soko newt follow `(60,4)` and regressed pet/map evidence until level teleport cleared `_utrack`.
- **Empty Pickup Still Prints But Returns No Time:** The `,` command on an empty square prints `There is nothing here to pick up.` and `dopickup()` returns `0` from `hack.c` when there is no reachable object. Treat it as a zero-turn command, but keep the prompt-time message.
- **Inventory Letters Are Persistent Object State:** `getobj()` prompts use the object's assigned inventory letter, not the current array index. Dropping `a` leaves later objects at `b..n`, and a subsequently wished object receives the next fresh letter (`o` in the current Wizard evidence).
- **Put-On And Zap Prompts Are Inventory Filters:** `P` filters carried accessories such as rings and then asks separately for a ring finger; `z` filters carried wands and then asks `In what direction?`. These prompts are zero-turn until the final selection performs the action.
- **Wear Prompt Letters Are Narrower Than Accepted Objects:** `W` lists unworn armor-like items in the prompt, so a still-worn startup cloak is omitted and an unworn cloak appears after `T`. `getobj()` can still accept other wearable selections such as amulets; keep displayed prompt filters separate from accepted command classes.
- **Wand Effects Exercise Wisdom Before The Effect:** `weffects()` calls `exercise(A_WIS, TRUE)` before dispatching to the specific wand behavior; a digging wand then consumes `zap_dig()`'s lateral `rn1(18,8)` before ordinary movement resumes.
- **Sokoban Zoo Regions Need Real Room Metadata And Fill:** `des.region({ type="zoo", irregular=1, filled=1 })` is not just terrain. It must create an irregular room, attach doors in `add_doors_to_room()` x-major order, flip room/door metadata with the map, and run `fill_special_room()` after the post-load flip/fixup phase so monsters, gold, and retained objects line up.
- **Special-Level Alignment Overrides Dungeon Alignment:** `align_shift()` checks the active special-level flags before dungeon flags. Sokoban special levels with no explicit alignment suppress the Sokoban dungeon's neutral alignment shift, changing random monster reservoir weights.
- **Tin Initialization Starts With The Spinach Gate:** For `TIN`, C checks `rn2(6)` for spinach before selecting random monster meat and `set_tin_variety(RANDOM_TIN)`. The later generic food quantity `rn2(6)` still runs after tin-specific initialization.
- **Monster Misc Items Follow Minvent Gold State:** `m_initinv()` gives leprechauns gold before defensive/misc/greedy gates. `rnd_misc_item()` can create polymorph/life-saving/speed/invisibility/gain-level items after the `rn2(100)` misc gate, and greedy gold must skip when the monster already has gold in minvent.
- **Spider/Snake Hide-Under Objects Are Part Of `makemon()`:** During mklev, spiders and snakes call `mkobj_at(RANDOM_CLASS, x, y, TRUE)` before hiding. Dropping that floor object shifts later Soko zoo monster streams and pet floor scans.
- **Pets Cannot Step Onto Or Push-Block Through Sokoban Boulders:** Pet candidate movement must reject boulder-occupied squares and apply `m_avoid_soko_push_loc()` so tame/peaceful monsters avoid squares where a boulder lies between that square and the hero.
- **Lycanthrope Distress Runs Before Movement Allocation:** At the turn boundary, `mcalcdistress()` calls `were_change()` before `mcalcmove()` reallocates monster movement. Human-form werecreatures consume `rn2(50)` by day, or the night/full-moon variants, even when no transformation happens.
- **Room Ambient Sound Gates Preserve Turn-Tail RNG:** `dosounds()` includes room-flag gates such as `has_zoo && !rn2(200)` after fountain/sink checks and before hunger. Even when the sound text is not yet rendered, the gate belongs in the turn tail for C-order RNG.
- **Sleeping And Hidden Monsters Spend Movement Before AI RNG:** `movemon_singlemon()` subtracts movement first, but `dochug()` returns before `distfleeck()` for frozen/still-sleeping monsters, and object/furniture-appearing hiders return before `dochug()` entirely. This lets Soko zoo monsters burn allocated movement without consuming `rn2(5)` or moving while asleep/hidden.
- **Wizard `#wizintrinsic` Is A Menu State:** C ref: `wizcmds.c:wiz_intrinsic()`. The command builds an `NHW_MENU` headed `Which intrinsics?`, with multiple pages and selectable rows; `h` toggles hallucination in the menu and Enter confirms the selection before the timeout message prints. Treating it as a one-line prompt that applies immediately on `h` is stale scaffolding and causes the current `seed0383` screen-162 menu mismatch. The next fix should introduce shared tty menu lifecycle/rendering rather than a bespoke expected-screen string.
- **TTY Menu Pagination Counts Nonselectable Rows:** C refs: `win/tty/wintty.c:tty_end_menu()` and `process_menu_window()`. A 24-row tty page has `lmax = rows - 1 = 23` menu rows plus the footer; prompts, blank rows, subtitles, and separators consume rows, while automatic selectors are assigned only to selectable rows and reset at each page boundary. For `#wizintrinsic`, the prompt, blank row, cmdassist subtitle, property rows, and `"--"` separator produce a four-page `PICK_ANY` menu with page 1 footer `(1 of 4)`.
- **Armor Dressing Uses Object Delay And Occupation Timing:** Wearing delayed armor such as gray dragon scale mail starts a dressing occupation (`nomul = -oc_delay`) and lets monster/upkeep turns run before the finish message. Run the remaining occupation turns from object `oc_delay`; only setting a worn flag keeps prompt text plausible while leaving live monster positions stale.
- **Delayed Armor AC Is Active On The Final Occupation Turn:** The armor's AC can matter before the finish message is printed. In `seed0383`, the +3 gray dragon scale mail must set `u.uac = -2` while one occupation turn remains so an adjacent ape's `mattacku()` uses `AC_VALUE(-2)` and the `gm.multi < 0` +4 hit bonus.
- **Monster-To-Hero Physical Attacks Need Attack Metadata:** `mattacku()` uses the monster's `mattk[]` rows from `include/monsters.h`, not just its glyph/species. A narrow physical front door must consume `AC_VALUE()`, `rnd(20+i)` per attack index, `d(damn,damd)`, `mhitm_knockback()`'s initial `rn2(3)`/`rn2(6)` rolls, and negative-AC damage reduction in that order.
- **Engulf Attacks Stop Occupation And Expose Turn-Tail Regen:** `AT_ENGL` in `mattacku()` first consumes the hit roll, `gulpmu()` damage dice, swallow timer, and elemental gate. If the hero takes damage, the next turn tail must include `regen_hp()`'s `rn2(100)` before hunger and engraving-wipe checks; otherwise all later pet and movement RNG appears one call early.
- **Swallowed Masters Are Not In Pet Apport Sight:** After `gulpmu()` calls `vision_recalc(2)`, `dog_goal()` still scans nearby `fobj` and inventory through `dogfood()`/`obj_resists()`, but the apport branch's `in_masters_sight` predicate is false. Do not fire `edog->apport > rn2(8)` while the hero is swallowed just because the object is otherwise visible to the pet.
- **Pet Melee Uses Adjusted Monster Level:** `dog_move()` does not let pets attack every adjacent hostile monster. It first compares the defender's adjusted `m_lev` against a pet HP-based balk threshold. Retain `m_lev` and `mhpmax` on monster instances from `adj_lev()`/`newmonhp()`, or deep-level apes/monkeys and similar monsters will get false pet `mattackm()` RNG.
- **Pet Kill Growth Can Raise Max HP Without Healing:** `grow_up(mtmp, victim)` uses `rnd(victim->m_lev + 1)` for the max-HP increase, caps it at the next level threshold, then only heals current HP via `rn2(max_increase)` when `max_increase > 1`. A `max_increase` of 1 increases `mhpmax` but leaves `mhp` unchanged, lowering the later pet balk threshold. In `seed0383`, this changed the kitten from a false `4/4` to C-like `4/5` after a gnome kill and moved FR 10154 to the later monster-to-hero combat boundary.
- **Pet Melee Still Starts From `mfndpos()` Reachability:** `dogmove.c` considers `ALLOW_M` attacks only for squares that passed `mfndpos()` terrain/object reachability. JS pet candidate loops should test enterability while ignoring the occupying monster before calling `pet_melee_attack()`, so pets do not attack monsters on squares they could not otherwise enter.
- **Initial Inventory Uses `addinv()` Stack Semantics:** `ini_inv_adjust_obj()` clears `cursed` for startup inventory, then `addinv()` merges compatible stackable objects. `dog_goal()` scans inventory stacks, not every generated duplicate; leaving duplicate scrolls separate adds false `obj_resists()` calls.
- **Pet Ranged Target Scoring Runs Before Pet Movement Finalization:** After `dog_move()` chooses a candidate square, C calls `pet_ranged_attk(FALSE)`. Even pets with no effective ranged attack can scan line targets and consume `score_targ()` `rnd(5)` fuzz rolls before the actual move happens.
- **Fog Cloud Vapor Has A Region Front Door:** `m_everyturn_effect()` only calls `create_gas_cloud(...,1,0)` when no gas region is already visible at the fog cloud's square. Retain and age minimal gas regions; otherwise zero-budget fog clouds consume a false `rn1(3,4)` every turn. Region aging is turn-based, not one tick per extra `movemon()` pass for fast monsters.
- **Ordinary `m_move()` Has Pre-Candidate Approach Gates:** Peaceful non-shopkeepers set `appr = 0` and still pass through the item-search gate (`!rn2(10)` for peaceful monsters). Stalkers, bats, and lights have a separate `rn2(3)` chance to stop approaching. Once those front doors match, remaining `rn2(4 * (cnt-j))` denominator gaps point at `mfndpos()` candidate geometry/flags, not turn ordering.
- **`m_balks_at_approaching()` Is A No-RNG Targeting Gate:** C ref: `monmove.c:m_balks_at_approaching()`, `mhitu.c:ranged_attk_available()`. Polearm users, aklys users, and monsters with current ranged attacks can change `appr` before candidate selection without consuming RNG; this can improve later RNG prefixes (for example `seed4500` +20 calls) while leaving screens unchanged, so classify it as deterministic movement-state debt rather than a score change.
- **Flying Monsters Count Pool/Lava `mfndpos()` Candidates:** In `mon.c:mfndpos()`, flying/in-air monsters can include pool and lava squares in the candidate list even when ordinary walkers cannot. A denominator gap such as `rn2(28)` expected vs `rn2(16)` actual can be missing terrain eligibility, not a backtracking formula bug.
- **Pet Apport Rolls Precede Carry Rejection:** `dog_goal()` can consume the `edog->apport > rn2(8)` gate for a visible non-food object and then reject it via `can_carry()`. Heavy tools such as a tinning kit should fail small domestic pet carry capacity after the apport roll rather than becoming goals.
- **Only Dogfood/Apport Goals Lock `dog_goal()` Before Hunger:** C ref: `dogmove.c:dog_goal()`. After the floor-object scan, a goal only prevents follow-the-hero fallback when it is `DOGFOOD`, `APPORT`, or the pet is already hungry enough for lesser food. `CADAVER`/`ACCFOOD`/`MANFOOD` goals must fall through to ordinary follow logic while `svm.moves < edog->hungrytime`.
- **Pet Melee Candidates Beat Ranged Scoring:** `dogmove.c` checks `ALLOW_M` adjacent candidates and calls `mattackm()` before `pet_ranged_attk(FALSE)`. If C expects `mattackm()` immediately after `dog_goal()` but JS enters `score_targ()`/`rnd(5)`, inspect the live pet/monster candidate state before ranged scoring instead of suppressing ranged fuzz rolls.
- **Second Pet Passes Expose Intervening Monster Spatial Drift:** Fast pets can have enough movement for a second pass in the same monster phase. After the first pet melee, later ordinary monsters in `fmon` order may move before the pet acts again; FR 9933-style `mattackm()` vs `score_targ()` mismatches should compare those intervening `m_move()` positions, not just the final post-turn trace.
- **Hero Movement Must Not Overlap Monsters:** `hack.c:domove()` enters `uhitm()` when the destination contains a monster; the hero stays in place and the command still takes time. Letting JS walk onto the monster square can preserve the RNG prefix for a while but poisons later monster target selection and pet melee state.
- **TTY Gray Is Displayed As Default Color:** In the tty backend, `CLR_GRAY` and `NO_COLOR` both emit no foreground color sequence. Normalize `CLR_GRAY` to `NO_COLOR` in display cell state instead of changing frozen terminal serialization.
- **Ordinary Stairs And Branch Stairs Have Different Colors:** Ordinary up/down stairs render with default/gray color, but branch stairs can still be yellow. The first-level upstairs can be a branch stair, so carry an `isbranch` marker in stair records instead of making all `<` stairs gray.
- **Object Map Colors Come From `objects[otyp].oc_color`:** Visible floor-object glyph colors should use upstream object type color, not class or material defaults. If RNG is exact and glyph class matches but a visible object color differs, suspect object identity, description shuffle, or state drift before blaming `newsym()`.
- **Warning Glyphs Float Above Unseen Monsters:** `display.h:_mon_warning()` and `display.c:display_warning()` show numeric warning symbols for unseen hostile monsters within `mdistu < 100` when the hero has `Warning`. They are display state, not remembered terrain; when a monster moves away, `newsym()` must clear the old unseen/unremembered cell or stale warning digits remain.
- **Warning Refresh Is Not Per-Serialization:** C refreshes warning/telepathy monster glyphs through `see_monsters()` at input/turn boundaries and through explicit `newsym()` calls during movement. Re-running every warning monster from the screen serializer can make nested `--More--` captures observe a different map-timing model; keep global refreshes at the main loop boundary and let movement-side `newsym()` handle mid-turn updates.
- **`o_init` Shuffles Runtime Appearance State, Not Just RNG:** `init_objects()` swaps `oc_descr_idx`, `oc_tough`, `oc_color`, and sometimes `oc_material` across the C shuffle ranges. Consuming the shuffle RNG without retaining the color/material swaps leaves unidentified rings, potions, wands, scrolls, books, amulets, venoms, and armor-description groups with static map colors. If an exact-RNG attr mismatch shrinks after wiring shuffled colors but remains for some cells, classify the residue as object identity/state drift rather than renderer debt.
- **`o_init` Descriptions Drive Unknown Object Names:** `o_init.c:shuffle_all()`, `objnam.c:xname()`, and the `objects.h` descriptor rows mean unknown carried rings, amulets, potions, scrolls, spellbooks, and wands should use the shuffled descriptor (`wooden ring`, `hexagonal amulet`, `ruby potion`, `scroll labeled ZELGO MER`, `cloth spellbook`, `curved wand`) and hide BUC, enchantment, and charges until known. Startup inventory still marks its objects known through `u_init.c:ini_inv_adjust_obj()`-style state, so it should keep real names.
- **Spellbook Descriptor Range Excludes Special Books:** C ref: `objects.h` and `o_init.c:shuffle_all()`. Shuffle ordinary spellbook descriptions over local IDs `366..406`, keep blank paper `407` fixed as `plain`, and keep novel / Book of the Dead fixed at `408..409`. `objnam.c` treats novel specially as `${desc} book` when unidentified; Book of the Dead can use `${desc} spellbook` until known, then its actual name is not prefixed with `spellbook of`.
- **Venom Descriptions Still Belong In `o_init`:** C ref: `objects.h` venom entries and `o_init.c:shuffle_all()`. Blinding and acid venom both describe as `splash of venom`, but local IDs `479..480` still participate in the description/color/material shuffle range and should be retained in runtime description state rather than left as empty table slots.
- **Armor Appearance Subranges Are Shuffled By Type:** C ref: `o_init.c:obj_shuffle_range()` and `objects.h` armor rows. The armor class does not shuffle every description; it shuffles only `HELMET..HELM_OF_TELEPATHY`, `LEATHER_GLOVES..GAUNTLETS_OF_DEXTERITY`, `CLOAK_OF_PROTECTION..CLOAK_OF_DISPLACEMENT`, and `SPEED_BOOTS..LEVITATION_BOOTS`. Unknown carried armor from those ranges should display the shuffled descriptor directly, such as `plumed helmet`, `old gloves`, `tattered cape`, or `combat boots`.
- **Check Stale Handwritten Object IDs Against `object_data.js`:** Local generated IDs put the real Amulet of Yendor at `213`, ordinary blank/novel/Book spellbooks at `407..409`, and many older handwritten constants can silently point at rings or ordinary spellbooks. If a predicate claims to protect a unique object, verify the ID against `OBJECT_CLASS`/probability ranges before trusting it.
- **Use Generated Object IDs For Wished Wands:** The generated table currently has `WAN_FIRE=430` and `WAN_DEATH=433`; stale handwritten constants can point at another wand slot such as secret-door detection. If wish RNG is exact but the unknown descriptor is wrong, check local constants against `js/object_data.js` before changing wish or naming flow.
- **`movemon()` Iterates A Snapshot Of `fmon`:** C's `iter_mons_safe()` snapshots the monster list before a movement pass. If combat removes or inserts monsters during the pass, later turn ownership should follow the original traversal while skipping monsters that are no longer linked. Do not let JS array mutation during pet/monster combat shift the next monster's action slot.
- **Swallowed Bystanders Spend Movement Without Path RNG:** In `monmove.c:m_move()`, non-fleeing monsters other than `u.ustuck` return `MMOVE_MOVED` while the hero is swallowed. `dochug()` still performs the post-`m_move()` `distfleeck()` recalc, so this removes false path/backtracking RNG without skipping the C phase boundary.
- **Already-Swallowed Engulf Skips The Hit Roll And Timer:** A repeat `AT_ENGL` from the monster already swallowing the hero does not roll `rnd(20+i)` again and does not reset the swallow timer. `gulpmu()` still rolls damage and elemental gates, so the next blocker after the first swallow can be an intervening monster movement-state issue rather than engulf RNG itself.
- **Monster Item Pickup Can Create Future No-AI Turns:** `m_search_items()` can retarget intelligent collectors toward desirable floor items without consuming RNG. After `mpickstuff()` picks up equipment, `check_gear_next_turn()` sets `I_SPECIAL`; the later `m_dowear()` pass can set `mfrozen`/`mcanmove=0` for the item's `oc_delay`, causing the monster to spend one or more ready movement turns before reaching `distfleeck()`.
- **Swallowed Hero Attacks Are Ustuck-Only:** `mattacku()` immediately returns for every monster except `u.ustuck` while `u.uswallow` is true. Even adjacent physical attackers must not roll hit or damage dice during that state; only the swallowing monster can continue `AT_ENGL`/`gulpmu()` damage.
- **Swallowed Hero Coordinates Stay Pinned To `u.ustuck`:** While engulfed, C keeps the hero square synchronized with the swallowing monster. Pet `dog_goal()` uses `u.ux/u.uy` for master-distance and search decisions, so even a one-cell drift can suppress C's later inventory scan and make the pet enter candidate movement too early.
- **Gas Regions With `ttl == 0` Remain Visible Until The Next Region Pass:** `run_regions()` removes zero-TTL regions at the start of a later region tick, then decrements positive TTLs. A fog cloud region that has just aged to zero must still block `m_everyturn_effect()` vapor creation for the rest of the monster phase; aging before movement or filtering `ttl == 0` immediately creates false `rn1(3,4)`/`rn2(3)` rolls.
- **Swallowed Hero Combat Depends On Menu/Prompt Alignment:** `domove()` should route directional movement into `uhitm()` while swallowed, but only once command state has reached a real movement key. In current `seed0383` evidence, adding that front door before aligning the `W` wear prompt / `#wizintrinsic` menu / message-more sequence made prompt letters look like movement attacks and regressed earlier, so fix the window lifecycle first.
- **Delayed Occupation Finish Happens After The Immobile Turn Loop:** C ref: `allmain.c:moveloop_core()` and `do_wear.c:Armor_on()`. The `gn.nomovemsg` finish line is printed by `unmul()` after the hero-can-move loop completes, not by a later Space on the finish message. If a swallowed/burdened hero still lacks movement, another monster/turn pass can print damage plines before `You finish your dressing maneuver.`. Do not special-case finish-line Space as a turn advance; move the pending turn before the finish message instead.
- **Occupation `--More--` Needs Resumable Monster Turns:** Delayed armor occupations can pause on pet/monster plines before the finish message. Simply deferring pet kill removal until the next input changes flat RNG because `movemon()` keeps running; the structural fix needs a message boundary that can pause/resume inside monster movement, or a side-effect queue that preserves C's RNG order while delaying only the visible map mutation.
- **Monster Combat Plines Can Block Before Side Effects:** `mhitm.c:hitmm()` prints the visible hit message before `mdamagem()` applies death/growth side effects, and `mon.c:monkilled()` prints before corpse/death work. In tty, a second pline can perform a nested `--More--` read inside `movemon()`; resuming the same call stack preserves flat RNG while capturing pre-side-effect map state.
- **Pending Monster Pickup `--More--` Flushes At The Next Monster Pline:** C ref: `mon.c:mpickstuff()`, `mhitu.c:hitmsg()`, `pline.c:pline_mon()`. A visible pickup can leave `--More--` pending while later map updates occur; the next monster-to-hero pline dismisses that old topline before applying the new hit message side effects. Skipping the attack to preserve the pickup screen breaks flat RNG (`seed0383` hit `rnd(20)` vs JS `rn2(5)`), so model the tty boundary without suppressing the C attack RNG.
- **Visible `mpickstuff()` Messages Precede Extraction:** `mon.c:mpickstuff()` calls the visible pickup pline before `obj_extract_self()`/`mpickobj()`, and only when `cansee(mtmp->mx,mtmp->my)`. Unseen monster pickups should still update inventory and gear-delay state silently; printing them creates false message boundaries.
- **Swallowed Hero `uhitm()` Uses Weapon Damage RNG, Not Dice Damage:** The current swallowed-attack evidence starts with the turn-tail hunger/exercise calls, then `hitum()`'s `rnd(20)`, a second exercise roll, and `dmgval()`'s `rnd(6)`. Using `d(1,6)` instead of `rnd(6)` shifts the first post-prompt mismatch inside the attack itself.
- **Fog Clouds Maintain Their Gas Regions:** `inside_gas_cloud()` extends a region by 5 turns after positive TTL decrement when a fog cloud is inside and the TTL is below 20. Without that maintenance, JS expires retained fog vapor too early and reopens a false `rn1(3,4)`/`rn2(3)` vapor roll in later monster phases.
- **Hallucination Abuses Wisdom In `exerper()`:** `exerchk()` calls `exerper()` every turn, and on 5-turn boundaries `Confusion || Hallucination` triggers `exercise(A_WIS, FALSE)`, consuming `rn2(2)`. This status exercise can sit immediately after hunger/Con exercise on 10-turn boundaries before engraving-wipe or later movement RNG.
- **`gulpmu()` Decrements The Initial Swallow Timer:** The first engulf attack sets `u.uswldtim`, then immediately reaches the common timer decrement in the same `gulpmu()` call. Skipping that first decrement makes expulsion happen one engulf turn late.
- **`gulpmu()` Damage Plines Pack Before Occupation Finish:** C ref: `mhitu.c:gulpmu()` and `pline.c:pline()`. Repeat swallowed elemental attacks can append messages such as `You are freezing to death!` before the next pline forces a `--More--`; printing each damage message as an immediate blocking prompt splits the turn boundary and can preserve text while breaking flat RNG.
- **Swallowed Display Is A 3x3 Stomach Glyph, Not Blank Map:** C ref: `display.c:swallowed()` and `display.c:swallow_to_glyph()`. After the initial engulf message is serviced, C clears the map and draws the hero inside the swallower's colored 3x3 swallow glyph. In current tty evidence, that is `/o\`, `|@|`, `\s/` with the swallower color; leaving the old room visible or blanking everything exposes screen drift once `gulpmu()` messages match.
- **Swallowed Glyphs Need Per-Render Caching:** `display.js` writes the swallowed map both into the serialized screen string and into the terminal grid. If `swallowed_glyph_at()` advances display RNG on each call, the render path consumes the hallucinatory monster sequence twice and the final screen drifts even when core RNG is exact. Cache the per-cell swallowed glyph within the render pass.
- **Swallowed Hallucination Colors Need the Upstream Display Context, Not Just the Redraw:** C redraws swallowed hallucination state immediately from `make_hallucinated()` via `swallowed(0)`, but the color stream still depends on the separate display RNG ownership. Rebuilding the 3x3 overlay at the right time is necessary but not sufficient if the JS display context is seeded or advanced differently from `rn2_on_display_rng`.
- **Hallucinated Visible Monsters Use `what_mon()` Display RNG:** C ref: `display.h:mon_to_glyph()` and `display.c:newsym()`. Real visible monsters should select a random monster glyph/color via `rn2_on_display_rng(NUMMONS)` when `Hallucination` is active. This is separate from core monster identity and should not perturb flat gameplay RNG.
- **TTY Black Uses Bright-Black SGR In Current Evidence:** C ref: `win/tty/termcap.c:tty_start_color()`. With `wc2_darkgray`, `CLR_BLACK` is emitted as bright black (`90`/dark gray), not plain black (`30`). This matters for hallucinatory swallowed glyphs and any future black monster/object display comparisons.
- **`Hallu` Is A Bottom-Line Condition Tail:** C ref: `botl.c`. When `Hallucination` is active, the status line appends `Hallu` after the normal level/HP/Pw/AC/Xp fields. In the `#wizintrinsic` commit evidence, the message and swallowed overlay share the frame with this newly active condition.
- **Expulsion Uses `unstuck()` And `mnexto()`:** When a swallowed hero is expelled, C clears `u.ustuck/u.uswallow`, sets an engulfer cooldown with `rnd(2)` when appropriate, then relocates the former swallower via `mnexto()`/`enexto()` coordinate shuffles. `mspec_used` also ticks down once per turn through `mon_regen()` during `mcalcdistress()`.
- **TTY Inventory Menus Clear A Window Rectangle, Not The Whole Map:** One-page inventory windows choose a right-side column from the longest visible item line, clear from one column before that menu column through the menu's last row, then draw menu text over the live map/status grid. This preserves left-side Sokoban map cells while truncating status text such as `St:` to `St (end)`. Build these from terminal-grid state instead of raw full-screen strings.
- **Spell Menus Have Styled Leading Spaces:** The `+` spell list header begins with inverse-video spaces before `Name`. The current terminal serializer trims leading spaces even when styled, so exact spell-menu captures need either a raw serialized override path or a serializer that treats non-default attributes as occupied cells.
- **Discovery Text Exposes Missing `o_init` Descriptor State:** Matching `\` for Wizard evidence requires object names, shuffled scroll labels, shuffled spellbook descriptions, and discovery persistence after objects are dropped. Limited evidence tables can move a session, but the structural fix is to retain shuffled `oc_descr_idx`/description names and drive `discoveries` from actual known object state.
- **Occupied-Square Hero Attacks Need The Same `uhitm()` Front Door:** A movement key into a monster square should consume the current hero melee RNG shape (`gethungry`, Dex exercise, hit roll, damage value, knockback, known-hit, passive) before monster turns. A message-only occupied-square front door preserves position but shifts the next turn into monster movement too early.
- **Hero Pet Kills Enter `abuse_dog()` Before Knockback/Passive:** When hero melee damage kills a tame monster, C handles `abuse_dog()`/`yelp()`/`xkilled()` immediately after `dmgval()`; it does not continue into the knockback, known-hit, or passive front-door rolls for that hit. Current kitten evidence consumes `rn2(mtame - 1)`, hallucination yelp `rn2(35)`, treasure gate `rn2(6)`, corpse chance `rn2(3)`, and peaceful cleanup `rn2(2)` before removing the monster.
- **Oracle Special Loading Has Scripted Rooms Before Arrival Movement:** `dat/oracle.lua` builds the centered outer room, eight `montype="C"` historic statues, the Delphi subroom/fountains/Oracle, two random room monsters, five random rooms, corridors, wallification, mineralize, arrival region, then ordinary turn-tail allocation. Porting that order moved `seed0383` through the Oracle transition without per-seed tables.
- **Oracle Statues Still Run Base Statue Initialization:** `des.object({ id="statue", montype="C" })` reaches `mksobj_init(STATUE)` first, so random `rndmonnum()`/gender/container gates can fire before the caller overwrites `corpsenm`. Tiny random statue corpses skip the spellbook-container roll via `verysmall()`.
- **Duplicate Monster Names Need Pointer Semantics:** C ref: `mondata.h:verysmall()` and `include/monsters.h` werecreature rows. The animal and human forms of werejackal/wererat/werewolf share display names but have different `mlet`/size semantics. Name-only helpers can treat a human-form wererat as tiny and skip the `STATUE` spellbook-container gate; use monster pointer identity or at least symbol/form disambiguation for mondata predicates.
- **Dead Pets Must Not Arrive From `game.pet_type`:** `mon_arrive(With_you)` moves an existing migrating monster. After the hero kills a tame monster, the later level teleport must not recreate it from the remembered startup pet type; doing so adds a false `rn2(10)` arrival gate and nearby `collect_coords()` shuffle.
- **`makemon(NULL,0,0)` Chooses Position Before Species:** In `makemon.c`, the random-position path calls `makemon_rnd_goodpos()` before `rndmonst()`. Choosing a monster species first shifts the post-generation stream into reservoir-sampling rolls where C is still rolling `rn1(COLNO-3,2)`/`rn2(ROWNO)`.
- **Oracle Levels Have A Final Ambient Sound Gate:** After ordinary fountain/sink/special-room sound gates, `sounds.c:dosounds()` checks `Is_oracle_level(&u.uz) && !rn2(400)`. Even when no sound text is emitted, this gate belongs before hunger/engraving turn-tail rolls.
- **`searches_for_item()` Is Type-Specific:** Ordinary intelligent collectors do not chase every potion, scroll, or amulet. `muse.c:searches_for_item()` only accepts specific usable magic such as teleport/create/fire/earth scrolls; broader class-wide magic pickup belongs to `M2_MAGIC`. Treating a remove-curse scroll as interesting made the `seed0383` gnome walk toward the wrong floor object and exposed a false `rn2(32)` denominator.
- **`m_move()` Skips Item Search When Already Lined Up:** In `monmove.c:m_move()`, hostile monsters only set `getitems` when `appr != 1` or they are not `lined_up()` within the hero throw range. A monster already lined up for a weapon/ranged attack should not detour toward nearby floor gear before candidate movement.
- **Monster-Init Objects Are Real `minvent`:** `makemon.c:mongets()` links `m_initweap()`/`m_initinv()` objects onto the monster, and `m_initthrow()` sets stack quantity with `rn1(oquan, 3)`. Keeping only RNG/accounting state hides later equipment delays, load checks, pickup choices, and ranged-weapon evidence.
- **Creation-Time Monster Wear Has No Delay:** `makemon.c:makemon()` calls `m_dowear(mtmp, TRUE)` immediately after `m_initweap()`/`m_initinv()`. Creation wear sets worn masks without `mfrozen`; skipping it leaves monsters to spend a later false gear turn after pickup or first movement evidence.
- **Moved Monsters Can Still Reach Ranged `mattacku()`:** In `dochug()`, `MMOVE_MOVED` does not always end the turn. If the monster is still not nearby and has a ranged attack, `AT_WEAP`, or offensive item, C breaks to phase-four `mattacku()`. `AC_VALUE()` can consume negative-AC RNG before the unimplemented `thrwmu()` path decides no projectile is available.
- **Failed Ranged Weapon Selection Feeds Later HTH Wielding:** A ranged `AT_WEAP` `mattacku()` with no usable ranged weapon can still leave `weapon_check = NEED_WEAPON`. On a later close turn, phase two spends the monster's move wielding a hand weapon via `mon_wield_item()` before ordinary `m_move()`; treating that as pathing creates false `mtrack` denominator rolls.
- **Door Squares Block Diagonal `mfndpos()` Movement:** `mon.c:mfndpos()` rejects diagonal movement from or into any door state except no-door/broken-door. Open doors still block diagonals. Missing this door rule inflates candidate counts for monsters standing in doorways, such as the Oracle-level gnome evidence.
- **Openable Closed Doors Are Movement Candidates:** C ref: `mon.c:mfndpos()` with `OPENDOOR` can include a closed door square for monsters with hands, and `monmove.c:postmov()` handles the door after the monster has moved onto it. Treating closed doors as terrain blockers caused the Oracle-level centaur to miss the trapped-door square and shifted later `mtrack` denominators.
- **Trapped Door Damage Leaves Stun Recovery Debt:** C ref: `monmove.c:mb_trapped()` consumes `rnd(15)`, stuns the monster, and returns to `dochug()`; the next awake movable turn rolls the `mtmp->mstun && !rn2(10)` recovery gate before `distfleeck()`. If a trapped-door fix aligns the damage roll but the next mismatch is `rn2(10)` vs `rn2(5)`, add the status recovery phase rather than another pathing adjustment.
