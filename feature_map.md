# Feature Map — NetHack JS Port

Cross-reference of every major subsystem between the C reference and the JS port.
Use this to choose your next implementation target. Update the **Status** and **Notes** columns after each session.

Context note: this file is an index. Prefer `rg` by session id, subsystem, JS module, C source, or FR number, then read only the relevant row and nearby section. Avoid full-reading it during tight implementation loops unless refreshing global priorities.

**Status legend:**
- 🔴 Not started / fully stubbed
- 🟡 Partially implemented / hardcoded for specific seeds
- 🟢 Correct for all tested seeds
- ✅ Verified passing (screen-level parity confirmed)

**Metric column:** "gates Y screens" means getting this right is expected to unlock Y screens across the public corpus.
Treat screen totals as lagging evidence of subsystem progress, not as the optimization target.

---

## 🏗️ Harness & Infrastructure

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| `runSegment()` API contract | `nethack-c/upstream/unixmain.c` | `js/jsmain.js` `runSegment()` | ✅ | Entry point, contract correct. Segment `datetime` now reaches game state. Do not break. |
| Screen capture hook (pre-nhgetch) | `sys/tty/tty_getch()` | `js/jsmain.js` `_installCaptureHook()` | ✅ | Fires before every `nhgetch()` call. Captures exactly one screen and one cursor per input boundary via `terminal.serialize()`. Do not push raw override strings or import scorer-side screen decoders from production `js/`. |
| PRNG log accumulation | `rng.c` | `js/rng.js` + `frozen/isaac64.js` | ✅ | Frozen ISAAC64 engine. Log format must be `rn2(N)=M` etc. |
| Screen comparison / canonicalization | `frozen/ps_test_runner.mjs` | (frozen — do not modify) | ✅ | DEC→Unicode, SGR normalise, version banner sentinel. |
| Per-session regression runner | `frozen/ps_test_runner.mjs` | (frozen) | ✅ | `node frozen/ps_test_runner.mjs [session...]` |
| Full regression suite | `frozen/score.sh` | (frozen) | ✅ | Run before committing broad changes and before handoff. Regressions must be classified; accidental regressions should be fixed, while expected regressions from hack removal or upstream-shaped refactors should be documented. |
| Live public regression suite | hosted `/sessions/` | `scripts/fetch-live-public-sessions.mjs` | ✅ | `npm run score:live-public` fetches the current public session files into `.cache/live-sessions` before running the frozen scorer. Use this when comparing against leaderboard public totals. |
| Debug tooling convention | — | Reusable diagnostics in `scripts/`; generated/checkpoint notes in `scratch/` | 🟡 | Temp debug scripts MUST be deleted after use. Removed stale tracked one-off probes `scratch/check_features.mjs`, `scratch/check_mklev.mjs`, and `scratch/inspect_screens.mjs`; remaining tracked scratch files are checkpoint/generated triage outputs. `scripts/triage-corpus.mjs` regenerates `scratch/divergence-inventory.md` from compact triage data for subsystem-level planning. `scripts/trace-dog-goal.mjs` runs a session prefix and prints the tame pet, C-shaped dog-goal search rectangle, floor objects, inventory, and expected-vs-actual RNG window for pet/object parity work. Never leave `console.log` or `import('fs').writeFileSync` in production paths (jsmain.js, allmain.js). |

---

## 🎲 PRNG / RNG

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Core PRNG engine (ISAAC64) | `rng.c` / `frozen/isaac64.js` | `frozen/isaac64.js` | ✅ | Frozen. Bit-exact with C. |
| `rn2(N)` | `rng.c:rn2()` | `js/rng.js:rn2()` | ✅ | |
| `rnd(N)` | `rng.c:rnd()` | `js/rng.js:rnd()` | ✅ | |
| `rn1(N,B)` | `rng.c:rn1()` | `js/rng.js:rn1()` | ✅ | |
| `rnl`, `rne`, `rnz`, `d` | `rng.c` | `js/rng.js` | 🟡 | `rnl()` now uses C's Luck adjustment shape, including the small-denominator Luck compression and the extra Luck gate before clamping (`C ref: src/rnd.c:rnl()`). `rne`, `rnz`, and `d` are partial; verify broader log format against C. |
| Three PRNG contexts (core / Lua / display) | `rng.c` context switching | `js/rng.js` | 🔴 | Hallucination/display context not implemented. Blocks `seed0383-wizard-hallucinate`. |

---

## 🗺️ Level Generation (mklev)

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Room generation (random room count/size/pos) | `mkmaze.c`, `mkroom.c` | `js/mklev.js` | 🟡 | Produces correct room count for seed8000/seed0002, and ordinary non-special `mklev()` now runs the final full-map wallification pass after `makelevel()` so late room wall/corner topology matches current seed0002 stair-descent evidence. Verify other seeds. |
| Corridor generation | `mkmaze.c:makecorridors()` | `js/mklev.js` | 🟡 | Connected but exact corridor paths may differ from C for non-tested seeds. |
| Door placement | `mkmaze.c:makedoor()` | `js/mklev.js` | 🟡 | `dosdoor()` now keeps JS `flags` and `doormask` synchronized to mirror C's alias, fixing closed-door rendering/movement state exposed by `seed0116`. |
| Stair generation (down stairs) | `mklev.c:generate_stairs()` | `js/mklev.js:generate_stairs()` | 🟡 | Down stairs generated. |
| Stair generation (up stairs, dlvl≥2) | `mklev.c:generate_stairs()` | `js/mklev.js:generate_stairs()` | 🟡 | Correctly skipped on dlvl 1. |
| Branch entrance placement (Mines etc.) | `mklev.c:place_lregion()` | `js/mklev.js:place_lregion()` | 🟡 | `place_lregion()`/`bad_location()` accepts ROOM, AIR, or corridor-on-maze targets like upstream; `end1_up: true` remains hardcoded for seed8000. Verify with other seeds. |
| Wallification (corner/t-junction glyphs) | `mklev.c:wallification()` | `js/mklev.js` | 🟡 | Ordinary levels now run the final `wallification(1,0,COLNO-1,ROWNO-1)` pass after `makelevel()` (`C ref: src/mklev.c:mklev()`), fixing current seed0002 late wall/corner display. Special-level and edge-case wallification still need broader evidence. |
| Monster/object fill (mkfill, mineralize) | `mklev.c`, `mkobj.c`, `makemon.c`, `engrave.c`, `rumors.c` | `js/mklev.js` + `js/random_text.js` + `js/monster_data.js` + startup fast-forward scaffolds | 🟡 | `mkobj()` consumes upstream-derived class/object probability and material metadata. `rndmonst_adj()` now walks a generated `include/monsters.h` table with C's weighted reservoir RNG shape, numeric dungeon/special alignment (`A_NONE` for unaligned special levels), Gehennom uncommon-monster rejection of lawful or no-hell candidates, and hot/cold level `temperature_shift()` from generated monster resistance fields; trap constants match `trap.h`; `occupied()` rejects trap squares; `CORPSE`/`STATUE`, duplicate-name wereform `verysmall()` disambiguation, random-artifact eligibility before erosion, trap-victim role corpse range, special-corpse timer restart after caller corpsenm overrides, no-corpse retry, grave epitaph data-file offset, egg init with `can_be_hatched()`/`little_to_big()`/breeder-gate and hatch-timer RNG, grid bug gender, multigen projectile quantity, tin spinach/random-meat ordering with `undead_to_corpse()` and no-corpse retry, container gold quantities, container rock-to-gem `rnd_class(DILITHIUM_CRYSTAL, LOADSTONE)`, spider/snake hide-under `mkobj_at()`, kobold/KOP/giant/elven/centaur/troll/gnome/ogre/wraith/zombie/salamander/angel/default `m_initweap()` slices, mummy/Nazgul/lich/`S_QUANTMECH` inventory, leprechaun gold/minvent state, retained monster-init `minvent` objects, creation-time boot wear via `m_dowear(mtmp, TRUE)`, initial sandestin/doppelganger/chameleon/vampire shapechange before inventory on currently evidenced branches, `m_initthrow()` stack quantities, `M2_GREEDY` money gates with minvent-shaped dice, adjusted-`m_lev` inventory gates including the Wizard of Yendor base-level exception, `mflags1` defensive-item predicates, golem fixed HP, adult dragon HP, mercenary soldier/officer weapon paths, live-`ARM_BONUS` armor rounds, level-generated giant-eel sleep gates, shared offensive-item selection, `rnd_misc_item()` slices, amulet cursed-gate init, placed-gold object display color, gem appearance descriptions, gem color/description randomization, visible-monster gem pickup naming, and `peace_minded()` predicates now consume closer C RNG. Random graffiti uses NetHack-style padded data-file selection and `wipeout_text()`; trapdoor/teleport niches create C's dust warning engravings (`Vlad was here`/`ad aerarium`), mark one-shot non-rock traps, and immediately wipe 5 characters (`C refs: src/mklev.c:makeniche()`, `src/engrave.c:wipe_engr_at()`). `level_finalize_topology()` calls real `mineralize()`, retains unburied mineralized gold/gems on `level.objects`, and skips buried gold/gems after kelp on most special levels. Evidence: `seed0116` now carries Soko zoo monsters/gold/objects into complete RNG parity; `seed0360` now reaches Wizard3 generation at `S 318/833 R 86032/120639` with current first blocker in Wizard3 lregion terrain acceptance; `seed0383` has moved beyond earlier pet object scans, `mfndpos()` terrain denominators, swallowed-position drift, gas-region lifetime, fog-cloud region maintenance, swallowed-command attack, hallucination status exercise, expulsion relocation, occupied-square hero attack RNG, pet abuse/kill RNG, Oracle loading, item-search drift, ranged/weapon ownership, later openable/trapped-door monster movement, bigroom display, amulet/wand appearance naming, warning glyphs, delayed monster-message boundaries, and the current ape-hit pline. Current `seed0383` RNG is complete; remaining blocker is ape-hit map drift, not object generation. `seed5002` now reaches `S 72/410 R 5836/12167`; `seed8000` screen parity remains `23/23`; `seed0105-valk-chat-lamp-ration` is exact (`S 30/30 R 2499/2499`). |
| Random text preprocessing | `makedefs.c:do_rnd_access_file()`, `makedefs.c:grep0()`, `rumors.c:get_rnd_text()` | `js/random_text.js` | 🟡 | Random engraving text now applies makedefs caret-condition filtering before padding/xcrypt selection, so `^?MAIL`/`^.` are not selectable records while the `MAIL`-enabled enclosed line remains. Evidence: `seed0360` startup graffiti chunk now matches `rn2(2894)=151`, moving first RNG mismatch from FR 1355 to FR 19241 and target RNG from `47273/120639` to `47303/120639` with sentinels stable. |
| Long worm creation ordering | `makemon.c:makemon()` | `js/mklev.js:makemon()` | 🟡 | Long worms now run the level-generation sleep gate before `initworm()` tail-count/tail-placement RNG, matching C's order at `makemon.c:1389` then `makemon.c:1406`. Evidence: `seed0360` Castle long-worm creation now matches through `rnd_nextto_goodpos()`, moving first RNG mismatch from FR 19241 to FR 43164 and target RNG to `47313/120639` with sentinels stable. |
| Shop generation | `mkroom.c:mkshop()`, `shknam.c`, `makemon.c:set_mimic_sym()` | `js/mklev.js:do_mkroom()` / `mkshop()` / `stock_room()` partial | 🟡 | The ordinary-level special-room decision now runs before fill. `mkshop()` can scan for an eligible one-door ordinary room, mark a shop type, convert big wand/book shops to general stores, and the special-room fill path now creates a shopkeeper, capital gold, optional charging scroll, tribute special-spot RNG, and a first C-shaped `stock_room()`/`mkshobj_at()` pass with mimic/object front doors. Shopkeepers now receive basic `mextra.eshk` state from `shkinit()` (`shoproom`, `shoptype`, `shoplevel`, `shd`, `shk`, `shknam`, and zeroed account/following fields), enough for current movement, greeting, and billing ownership; ordinary shop doorways without door masks are promoted to open/closed door state before stocking. Shop mimics now use the shop-specific `rn2(10) >= depth` strange-object branch before falling back to shop item/object-class appearance; invalid/random object-class fallback uses C's `set_mimic_sym()` object-symbol table with furniture excluded and two strange-object entries. Orcus ghost-town shops stock through the ordinary front door, then remove the shopkeeper and consume carried-record discard resistance RNG like `stock_room()`'s Orcus cleanup path. Full billing/following/customer semantics, all shop-specific item tables, and exact stocking side effects remain incomplete. Evidence: `seed0116` moved past the level-2 shop chance gate to FR 4592 and remains a full pass; `seed0360` remains exact; `seed5002` remains exact; `seed0002` now uses retained shop names/state for shop entry, item quotes, and partial `#pay` billing before the current run-sound map latch frontier. |
| Special level loading (Lua specials) | `sp_lev.c` | `js/mklev.js` static themed-map/bigroom/Soko/Oracle/Castle/Tower slices | 🔴 | Full special level support is still absent. Static themed `des.map()` fragments now use exact upstream map literals for implemented shapes, enable `in_mk_themerooms` failure semantics, create irregular `filler_region()` rooms from flood-filled floor bounds, and use C-like irregular door search. The mapped themed-room helper now includes the `Circular, medium` shape, retries colliding `lspo_map()` origins against terrain/room metadata before contents, and keeps the C order of origin rolls, `filler_region()` percent gate, `litstate_rnd(-1)`, and the separate `themeroom_fill()` reservoir. Dynamic `des.room({ type="themed", contents=themeroom_fill })` entries now also run the fill reservoir, with current support for `Buried zombies` and the `Storeroom` percent/default-inventory mimic front door plus explicit post-`makemon()` `appear_as="obj:chest"` override; corpse timer shape and many fill contents remain shared debt. `bigrm-12` now dispatches before ordinary level generation, consumes the random variant, Lua shuffle/init, applies the C-centered map offset (`x=3,y=1` for the 75x19 slice), runs scripted percent gates, lights the whole selected area (`C ref: dat/bigrm-12.lua des.region(..., "lit")`), applies `des.wallify()`'s preliminary `wallify_map()` pass, performs stair/object/trap/monster placement with retained `des.trap()` state, runs final `wallification()` before the post-load horizontal flip (`C ref: sp_lev.c:lspo_final_map_cleanup()`), consumes the post-load `flip_level_rnd()` horizontal gate for `noflipy`, and skips special-level buried minerals after kelp. Soko1/Soko2/Soko3/Soko4 slices now create filled irregular zoo room metadata where scripted, attach doors in x-major order, flip room/door metadata/engravings with the map, run `fill_special_room()` after flip/fixup, apply dummy-endgame depth fixup, repair display wall spines after flip, premap Sokoban remembered terrain/boulders/traps, load Soko2/Soko3/Soko4 stairs, boulders, rolling-boulder/hole/pit trap victim gates, random and typed objects, branch lregions, and locked doors with C-centered map origins, and let scripted boulder mimics run ordinary `set_mimic_sym()` RNG before the explicit `appear_as="obj:boulder"` override without adding trap-avoidance retries. Oracle now scripts the centered room, centaur statues, Delphi subroom, fountains, Oracle monster, random rooms, corridors, wallification, and `create_monster()`'s occupied-coordinate `enexto()` relocation before `makemon()`. Castle now loads the upstream map terrain on the centered `(9,3)` mazegrid origin, `nhlib`/script shuffles, scripted doors/drawbridge, storeroom objects, wishing chest contents with container-coordinate RNG, Elbereth/scare scroll, trap doors, named-monster lookup before alignment, class-monster alignment before `mkclass_aligned()`, placeholder rejection in `mk_gen_ok()`, fixed soldiers/dragons/eels/sharks, giant gem inventory, court class monsters, both scripted `des.mazewalk()`/`fill_empty_maze()` passes, map-relative lregion exclusions, scripted stair-up/branch fixup, Castle-only barracks room fill with adjacent-door skips, lit-selection regions that do not create rooms, wallification, and `noflipy` horizontal flip gate. Vlad's Tower now dispatches `tower1`, `tower2`, and `tower3` static maps with C-shaped shuffle/init gates, ladders/doors, branch lregion fixup, scripted monster alignment/class selection, Vlad's Candelabrum, vampire weapon init, Tower2 chest/object/spellbook payload, Tower3 random monster/object/trap payload, `traptype_rnd()` filtering, and Tower3's `(17,5)` centered origin. Wizard quest-start now loads `dat/Wiz-strt.lua` as a centered `(3,1)` 76x20 slice with cloud terrain, lit/dark regions, fixed doors/stairs/branch lregion, Neferet inventory, apprentice/eel/siege monsters, random traps through `traptype_rnd()`, final wallification/flip/fixup, and quest-branch `Home` status rendering. Evidence: `seed0116` is complete (`S 127/127`, `R 12562/12562`); `seed0360` now reaches `S 408/833 R 101246/120639`, with first RNG frontier at `R 100502` in Wizard quest-start monster movement and first screen mismatch after travel/path-failure cursor state; `seed0383` now reaches `S 196/219`, `R 16915/16915`; `seed5002` now reaches `S 222/410 R 11673/12167`, and `seed0013` sentinel RNG prefix remains `580/4804` without screen regression. |
| Special level loading (Medusa/Bigroom additions) | `dat/medusa-3.lua`, `dat/bigrm-4.lua`, `dat/bigrm-7.lua`, `mkmaze.c:fixup_special`, `trap.c:mk_trap_statue` | `js/mklev.js` Medusa fixup, Bigroom-4/7 loaders, statue-trap front door | 🔴 | Medusa-3 now creates its `arrival_room=true` ordinary region and runs the Medusa `fixup_special()` statue block after lregion processing, including `mk_tt_object(STATUE)`'s init-false statue species metadata, top-ten rank probe, role fallback, and stone-immune rerolls. Bigroom-4 now dispatches from the random Big Room proto roll, loads the centered 75x18 map at `(3,3)`, performs the terrain-choice/replacement RNG over every `L` cell, places fountains/lit region/stairs/objects/traps/monsters, and final-wallifies before arrival. Bigroom-7 now dispatches from the same proto roll, loads its centered 75x19 slice at `(3,1)`, chooses replacement terrain from `L/T/{/.`, consumes replacement rolls for every original `L`, lights the scripted interior, places stairs/objects/traps/monsters, marks the slice nondiggable, and runs the final wallification/flip/light-wall cleanup. `STATUE_TRAP` now runs `mk_trap_statue()` with a temporary monster inventory transfer. Evidence: `seed0360` moves from `S 309/833 R 59231/120639 FR 58616` to `S 309/833 R 68478/120639 FR 68428`, with sentinels stable (`S 429/1063 R 38854/64569`); `seed0399` now reaches `S 113/532 R 10275/11409` with strict sentinels exact. |
| Rogue level generation and topology | `extralev.c`, `mklev.c:makelevel`, `do_name.c:roguename`, `dungeon.c:level_map` | `js/mklev.js`, `js/dungeon.js` | 🟡 | Rogue levels now take the C ordinary-level Rogue branch after the below-Medusa gate: `makeroguerooms()` builds the 3x3 real/dummy room graph, `miniwalk()` door table, Rogue corridors/doors, room light gates, and `makerogueghost()` object bundle. Rogue rooms skip ordinary fill, monster inventory skips on Rogue levels, and the ghost keeps the ordinary `rndghostname()` roll before `roguename()`. Dungeon init now records `rogue_level` and floats the Fort Ludios/Knox branch parent to the bogus dungeon number so `Is_branchlev()` does not place a portal on overlapping special levels. Evidence: `seed0360` moves from `S 309/833 R 68478/120639 FR 68428` to `S 309/833 R 68721/120639 FR 68690`, with sentinels stable (`S 429/1063 R 38854/64569`). |
| Special level loading (Asmodeus slice) | `dat/asmodeus.lua`, `dat/nhlib.lua:hell_tweaks`, `sp_lev.c:lspo_map`, `sp_lev.c:lspo_level_init`, `sp_lev.c:get_location`, `nhlsel.c`, `selvar.c`, `mkmaze.c:walkfrom`, `makemon.c`, `teleport.c:noteleport_level`, `mkobj.c:mkobj` | `js/mklev.js` Asmodeus static loader, hell_tweaks front door, demon-court/object-init front doors | 🔴 | Asmodeus now dispatches as a mazegrid special, loads `asmo1` at `(15,5)` and `asmo2` at `(35,9)`, scripts Asmodeus, objects, traps, monsters, levregions, asmo2 `des.mazewalk()`/`fill_empty_maze()`, final wallification/flip/lregions, and the source-backed front of `hell_tweaks()`. Shared fixes cover hell/rogue `mkobj(RANDOM_CLASS)` probability tables, high-mlevel fixed HP, Asmodeus wands, ice-devil spear gate, iron-golem default weapon init, and Gehennom demon-court no-teleport defensive-item retries. `hell_tweaks()` now uses C-ordered selection percentage, active-`gx.xstart` selection-coordinate storage (`x=1+rn2(79)`, protected effects at `x=3..77`), relative `rndcoord()` handoff to `randline`, `nhlsel.c:l_selection_randline()` recursion depth 12, global `ROOM` floor matching, river growth, riverbank front doors, and the two post-river wall replacement gates/pattern selections. Evidence: `seed0360` verifies at `S 309/833 R 72096/120639 FR 72078`, past Asmodeus generation and lregion placement. Sentinels remain stable (`S 429/1063 R 38854/64569`). |
| Special level loading (Juiblex slice) | `dat/juiblex.lua`, `sp_lev.c:lvlfill_swamp`, `sp_lev.c:lspo_level_flags`, `sp_lev.c:lspo_region`, `sp_lev.c:create_monster`, `mkmaze.c:place_lregion`, `mklev.c:water_has_kelp` | `js/mklev.js` Juiblex static loader, swamp init, SWAMP region metadata, random class/named monsters, lregions | 🔴 | Juiblex now dispatches before ordinary Gehennom generation, consumes the `nhlib` shuffle, applies `mazelevel`/`shortsighted`/`noflip`/`temperate`, ports `LVLINIT_SWAMP`, loads the two side maps and centered lair at `(15,3)`, creates the filled SWAMP region room metadata before fountain mimic placement, and scripts fountains, mimics, Juiblex, lemures, random class monsters, named jellyfish, class objects, random objects, boulder, traps with victim gates, side stair/branch lregions, teleport regions, and post-load special mineralize kelp ordering. Random monsters choose class/name before `get_location()` over the lair bounds, and occupied random coordinates relocate through `enexto_core()` before `makemon()`. Evidence: `seed0360` now verifies at `S 313/833 R 74822/120639`, past Juiblex generation, lregions, and mineralize; sentinels remain stable (`S 429/1063 R 38854/64569`). |
| Special level loading (Baalz slice) | `dat/baalz.lua`, `sp_lev.c:lspo_map`, `sp_lev.c:lspo_mazewalk`, `sp_lev.c:create_monster`, `mkmaze.c:fixup_special`, `mkmaze.c:baalz_fixup`, `mkmaze.c:place_lregion` | `js/mklev.js` Baalz static loader, stocked maze walk, lair payload, lregion front door, fake-pool wall fixup | 🔴 | Baalz now dispatches as a solidfill/corrmaze special, loads the 49x13 right-centered lair map at `(29,5)`, runs `des.mazewalk(00,06,"west")` with default stocked maze fill, scripts the down stair, locked door, Baalzebub, random weapons/armor/gems/potions/scrolls, traps with victim gates, fixed and random class monsters, final wallification/flip, and the front of stair-up/branch/teleport lregions. The scripted nondiggable beetle area is marked, `fixup_special()` runs the Baalz fake-pool wall fixup after lregion processing, and mineralize no longer treats the fake `P` markers as kelp-eligible pools. Evidence: `seed0360` now verifies at `S 318/833 R 76676/120639`, past Baalz generation, lregion placement, mineralize, and arrival placement; sentinels remain stable (`S 429/1063 R 38854/64569`). |
| Special level loading (Orcus slice) | `dat/orcus.lua`, `dat/nhlib.lua:hell_tweaks`, `sp_lev.c:lspo_map`, `sp_lev.c:lspo_mazewalk`, `mkroom.c:fill_zoo`, `mkmaze.c:place_lregion` | `js/mklev.js` Orcus static loader, ghost-town payload, lregion front door, Orcus protected `hell_tweaks()` | 🔴 | Orcus now dispatches before ordinary Gehennom generation, consumes the `nhlib` shuffle, loads the right-centered 45x17 ghost-town map at `(33,3)`, runs `des.mazewalk(00,06,"west")` with stocked empty-maze fill, scripts the down stair, boulders, fixed door masks, noalign altar, morgue/shop room metadata, random traps/objects, the magic-marker/lamp gate, Orcus and companion monsters, bare random-monster `induced_align()` before location selection, Orcus-shaped protected `hell_tweaks()` with shifted selection-fill bounds `x=3..77,y=3..19`, final wallification/flip, and stair-up/branch/teleport lregions. Lua string monsters now honor gendered display names such as `"vampire lord"` during the `find_montype()` front door, fixed-HP special monsters store C's approximate `m_lev` before inventory gates, and Orcus shops remove the ghost-town shopkeeper after stocking. Exact morgue fill and late payload RNG remain incomplete. Evidence: `seed0360` now reaches Wizard3 generation at `S 318/833 R 86032/120639`; sentinels remain stable (`S 429/1063 R 38854/64569`). |
| Special level loading (Wizard slices) | `dat/wizard1.lua`, `dat/wizard2.lua`, `dat/wizard3.lua`, `dat/nhlib.lua:hell_tweaks`, `sp_lev.c:lspo_map`, `sp_lev.c:lspo_mazewalk`, `sp_lev.c:link_doors_rooms`, `mkmaze.c:place_lregion`, `trap.c:maketrap` | `js/mklev.js` Wizard1/Wizard2/Wizard3 static loaders and lregion front door | 🔴 | Wizard1, Wizard2, and Wizard3 now dispatch as mazegrid specials, consume the `nhlib` shuffle, load centered 29x13 transparent maps at `(25,5)`, script room regions, doors, ladders, fixed and random monsters/traps/objects where currently covered, `hell_tweaks()`, final wallification/flip, and stair-up/branch/teleport lregions. Fixed special doors are linked to bordering room metadata before `fill_special_room()`, matching `sp_lev.c:link_doors_rooms()`/`maybe_add_door()` so Wizard2 zoo gold uses door distance rather than the roomless fallback. `specialRndTrap()` respects `dungeon.c:Can_dig_down()` hardfloor rejection on these hardfloor special levels, and `maketrap()` returns null for `TRAPPED_DOOR`/`TRAPPED_CHEST` selections so special-level random trap rolls do not leave fake map traps that block `place_lregion()`. Evidence: `seed0360` now reaches `S 355/833 R 98495/120639`, first RNG mismatch `FR 98412: rn2(4)=1=>rn2(3)=2`; sentinels remain stable (`S 429/1063 R 38854/64569`). |
| Special level loading (Mines slices) | `dat/minetn-5.lua`, `dat/minend-2.lua`, `sp_lev.c:lspo_map()` | `js/mklev.js` Minetown-5 and Mines End wine-cellar loaders | 🔴 | Minetown-5 now loads its static town map/payload, shop rooms, random doors, watch monsters, temple front door, and Mines wall colors. Mines End `minend-2` now loads the wine-cellar map at the C-centered `(3,3)` origin for a 75x18 fragment, scripts the cellar/treasure/random object and monster payload, handles web-trap spider creation, applies Mines mineralization multipliers, keeps raw `region_islev=1` teleport bounds, runs final `wallification()` before the post-load flip gate, and transposes saved arrival rectangles during final level flips. Evidence: `seed0360` verifies at `S 242/833 R 47273/120639`; the step-192 RNG and screen are exact through flipped arrival placement, with the current first mismatch at step 205's gem pickup name. |
| Special level loading (Valley slice) | `dat/valley.lua`, `sp_lev.c`, `mkroom.c:fill_zoo()` | `js/mklev.js` Valley static loader and morgue fill front door | 🔴 | Valley now dispatches as a special level instead of falling through generic maze generation. It loads the upstream static terrain at `(3,1)`, runs the three `percent(50)` terrain gates, applies the `temperate` level flag over the Gehennom default, creates typed temple/morgue room metadata, flood-fills irregular morgue membership from each scripted seed, places fixed doors/altar/stairs/traps, creates the shrine priest during altar placement, records branch and down-teleport lregions across the final flip, places scripted random corpses/loot/traps/undead through the shared special-level dry-location helper, retries random trap coordinates that land on stairs/ladders, converts special-map `B` crosswalls to room during map cleanup, and runs the post-fixup `fill_zoo(MORGUE)` front door. Exact Valley payload RNG, typed object/corpse details, `morguemon()` selection, and arrival placement still need parity. Evidence: `seed0360` now reaches `S 189/833 R 22966/120639`; Castle arrival and Valley entry messages match, with the remaining first mismatch at screen 164 map/arrival placement. |
| Special level loading (Sanctum slice) | `dat/sanctum.lua`, `sp_lev.c`, `priest.c:priestini()` | `js/mklev.js` Sanctum static loader and priest/minion front doors | 🔴 | Sanctum now dispatches as a static special level, loads the upstream map terrain, applies hardfloor/nommap/noteleport and red Gehennom walls, creates temple metadata and a flood-filled irregular morgue with existing map doors attached before fill, places fixed doors/stairs/traps/objects/monsters, runs final wallification and `flip_level_rnd(3)`, and registers the down-teleport region after any flip. The high priest uses the Sanctum `priestini()` path with `HIGH_PRIEST`, Amulet of Yendor, and `rn1(3,2)` spellbook inventory; scripted `peaceful=0` overrides post-creation monster attitude; `aligned cleric` rows use the level-12 priest data and skip random `induced_align()` because Lua specifies `align="noalign"`. The full recorded Sanctum generation move now matches RNG through arrival placement; exact later status/depth display remains incomplete. Evidence: `seed0360` currently verifies at `S 201/833 R 41723/120639` with first mismatch at a status-row depth digit on screen 175; sentinels stable. |
| Special level loading (Temple fill) | `sp_lev.c:create_altar()`, `priest.c:priestini()`, `sp_lev.c:fill_special_room()` | `js/mklev.js` Valley shrine altar and `TEMPLE` flag front doors | 🔴 | Special shrine altars inside typed `TEMPLE` rooms now create the aligned priest immediately with `MM_EPRI` during altar placement, before later scripted Valley objects/traps/monsters. `filled=2`/`FILL_LVFLAGS` temple rooms only set level flags during the later `fill_special_room()` pass and do not create another priest. The priest front door now scans directions from the rolled `rn2(8)` offset and uses the C-shaped mace `spe`/curse path and priest robe/shield/gold inventory gates; exact priest metadata, altar alignment, and later object placement remain incomplete. Evidence: `seed0360` remains at screen 164 with sentinel stability; current Valley baseline is `S 189/833 R 22966/120639`. |
| Special level scripted monster placement | `sp_lev.c:create_monster()`, `dungeon.c:induced_align()`, `teleport.c:enexto()` | `js/mklev.js` Castle/Valley scripted monster helpers | 🔴 | Castle and Valley scripted monsters now share the important C ordering: named-monster gender lookup first, then induced alignment and class selection, then special-level dry-location selection, with occupied target squares relocated by `enexto()`. Special class `V` now maps to `S_VAMPIRE` instead of falling through to a random monster. Exact Lua argument coverage, `mkclass_aligned(S_VAMPIRE)`, and all special levels remain incomplete. Evidence: `seed0360` Valley monster ordering reached `S 189/833 R 22964/120639`; correcting `V` class mapping exposes vampire-class debt and currently verifies at `S 189/833 R 22949/120639` with sentinels stable. |
| Special level scripted objects | `sp_lev.c:create_object()`, `dat/valley.lua`, `mkobj.c:set_corpsenm()` | `js/mklev.js` typed/class Valley object payload | 🔴 | Valley object payload distinguishes typed objects from random class objects: the scripted ruby now uses a typed `mksobj_at(RUBY)` path before the remaining random gem-class entries. Scripted role corpses now restart corpse timeout RNG after `create_object()` overrides the initially random corpse type; `mkcorpstat()` remains on its narrower special-corpse restart path. Exact Lua object-argument coverage, container handling outside Castle, and all typed Valley loot remain incomplete. Evidence: `seed0360` verifies at `S 189/833 R 22980/120639`; sentinels stable. |
| Special room morgue fill | `mkroom.c:fill_zoo()`, `mkobj.c:mk_tt_object()` | `js/mklev.js:fill_zoo()` morgue branch | 🔴 | Morgue fill now uses `mk_tt_object(CORPSE)` for corpse gates instead of an ordinary random corpse: the object front door creates a corpse, rolls a top-ten entry and role corpse, restarts corpse timing, then places it. Random monster selection honors hot/cold level resistance bonuses, `make_grave()` rejects non-room and trapped squares before random epitaph text, and irregular special rooms use their attached first door to skip adjacent fill cells. Exact grave text contents and container metadata remain approximate. Evidence: `seed0360` Sanctum `fill_zoo(MORGUE)` now matches the recorded generation move RNG through `place_lregion()` and verifies at `S 201/833 R 41723/120639`; sentinels stable. |
| Special level load prelude | `bones.c:getbones()`, `sp_lev.c:load_special()` | `js/mklev.js:mklev()` and Valley loader | 🔴 | Special-level loading has one shared `getbones()` gate in `mklev()` before `makelevel()`. Valley no longer adds a second loader-local `rn2(3)`; the loader-specific prelude starts at Lua `nhlib` shuffle and `splev_initlev()` state. This can lower seed evidence when the duplicate happened to align later calls, but removes hard RNG-shape debt. Evidence: `seed0360` `S 189/833 R 22960/120639`; sentinels stable. |

---

## 🧑 Player Initialization (newgame / chargen)

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Name prompt / character selection UI | `role.c:plnamesuffix()`, `win/tty/wintty.c:tty_askname()` | `js/allmain.js:player_selection()` | 🟡 | Configured role/race/gender/align starts without `OPTIONS=name` now use the tty name prompt before startup RNG, echo printable name characters, handle backspace, and set `plname` from the typed name. The seed0002 random-chargen menu replay remains hardcoded until `pick4u()`/role menus are ported. |
| Role/race/gender/align selection from nethackrc | `role.c:plnamesiz()` etc. | `js/options.js` + `js/allmain.js` + `js/roles.js` (partial) | 🟡 | Parsed role/race/gender/align now drive startup identity, level-1 rank, role `initrecord` alignment record, welcome text, role god names, normalized display name, and status title where config supplies them. Configured role pantheons now use `role.c:roles[]`: Priest startup copies a random role pantheon through the `role_init()`/`randrole()` RNG loop when the Priest role has no own gods, Tourist neutral uses `The Lady`, Ranger neutral/chaotic use `Venus`/`Mars`, and other configured role pantheons are present instead of falling through to Marduk. This still does not implement full chargen RNG or menu flow. |
| Random chargen (pick_role etc.) | `role.c:pick4u()` | `js/allmain.js` (hardcoded `rn2()` calls) | 🔴 | Hard-coded for seed0002. Not a real implementation. |
| Hero placement (`u_on_upstairs`/`u_on_dnstairs`) | `stairs.c:u_on_upstairs()`, `stairs.c:u_on_dnstairs()` | `js/mklev.js:u_on_upstairs()`, `js/mklev.js:u_on_dnstairs()` | 🟡 | Ordinary stair traversal now lands on the corresponding destination stair for current seed0002 down-stair evidence, while level teleport still uses arrival regions. Seed0002 reaches the later run/door-bump pet-position frontier; broader branch/special stair handling remains incomplete. |
| Player stats (HP, Pw, AC, attributes) | `attrib.c`, `role.c`, `botl.c:get_strength_str()`, `insight.c:attrval()` | `js/u_init.js` + `js/allmain.js` + `js/display.js` + `js/cmd.js` (partial) | 🟡 | Configured non-replay Wizard/Rogue/Healer/Tourist/Ranger/Priest now use partial role-driven HP/Pw/AC/gold and `init_attr()`/`vary_init_attr()` RNG shape. Ranger, Priest, and Healer startup attributes/HP/Pw/AC follow `role.c:roles[]`, `u_init.c:u_init_misc()`, `attrib.c:newhp()`, and `exper.c:newpw()` for current human evidence, including level-0 role+race HP/Pw and Healer's worn-armor AC deferral. Attribute storage follows C order (`Str, Int, Wis, Dex, Con, Cha`), race `attrmax` data bounds redist/positive variation, and positive `vary_init_attr()` adjustments can raise the per-attribute maximum like `adjattrib()`. Status and `^X` attributes now format C's exceptional Strength encoding: status uses `get_strength_str()` shape (`19` as `St:18/01`, `118` as `18/**`) while insight uses `attrval()` shape (`18/100` for max exceptional strength). Wizard startup AC now distinguishes the legacy quest-intro screen, where cloak wear is deferred past the first status render, from `OPTIONS=!legacy`, where the first visible map/status screen already reflects worn cloak AC; Ranger/Rogue/Priest/Healer legacy startup similarly defers visible worn-armor AC until after the lore pager. Role `^X` background lines now include moon/Friday effects, split characteristics by the tty 23-row page budget, distinguish current/base from current/peak where current evidence can infer it, and report current startup weapon skill for modeled roles (`C refs: insight.c:background_enlightenment()`, `insight.c:attributes_enlightenment()`, `weapon.c:skill_init()`). Seed replay paths remain scoped. Evidence: `seed0016` now has full startup/wake RNG parity and `seed0700-samurai-explore-descend` is exact (`S 51/51 R 3230/3230 C 0`). |
| Starting inventory (`ini_inv`) | `invent.c:ini_inv()` | `js/u_init.js` + `js/mklev.js` object creation (partial) | 🟡 | Wizard `ini_inv(Wizard)` now consumes the general `trobj` path for fixed/random wands, rings, potions, scrolls, spellbooks, and generated-id magic marker charges, using `mkobj()`/`mksobj()` rather than replay. Healer `ini_inv(Healer)` now creates scalpel, gloves, stethoscope, healing/extra-healing potion stacks, sleep wand, healing spellbooks, apples, and the optional lamp gate as live startup state; seed0002 skips the corresponding replay prefix instead of leaving inventory absent. Tourist `u_init_role()` now rolls `u.umoney0 = rnd(1000)`, creates darts, ten random food objects, extra-healing potions, magic-mapping scrolls, Hawaiian shirt, expensive camera, credit card, the optional tin-opener/leash/towel/magic-marker gates, and real `$` money inventory; seed8000 now runs that live Tourist inventory before the remaining startup replay tail. Ranger `ini_inv(Ranger)` now creates the +1 dagger, alternate +1 bow, +2 quivered arrow stack, plain arrow stack, worn +2 cloak of displacement, and cram rations from `u_init.c:Ranger[]`; `u_init_role()` gives Rangers weapon-class preknowledge as discovery rows, and initial worn cloaks apply their `worn.c:setworn()` extrinsics even when `initial_don` suppresses wear messages. Rogue `ini_inv(Rogue)` now creates the wielded short sword, alternate dagger stack, worn +1 leather armor, sickness potion, lock pick, empty sack, optional blindfold gate, and dagger-appearance preknowledge (`C ref: u_init.c:Rogue[]`, `u_init.c:u_init_role()`, `u_init.c:ini_inv_use_obj()`). Priest `ini_inv(Priest)` now creates the blessed +1 mace, worn robe and small shield, blessed water stack, garlic, wolfsbane, random spellbooks with duplicate suppression, optional magic-marker/lamp gates, water discovery, and initial known spells for current evidence (`C refs: u_init.c:Priest[]`, `u_init.c:ini_inv_use_obj()`, `spell.c:initialspell()`). Startup `ini_inv_use_obj()` side effects now auto-quiver the first ammo/missile stack and mark first/alternate wielded weapons where currently modeled. Startup containers mark `cknown/lknown` and ordinary known NoDes startup tools use table names in inventory, so `objnam.c:doname_base()`-style empty container and tool names are visible. Random starting objects pass through a partial `ini_inv_mkobj_filter()` port with no-create tracking for polymorph combinations, duplicate ring/spellbook suppression, useless-item rejection, and spellbook level gating (`C ref: u_init.c:ini_inv_mkobj_filter()`). The optional Wizard blindfold branch is retained behind its `!rn2(5)` gate and now uses the generated tool-table blindfold id. Initial-inventory erosion is suppressed via the upstream `moves <= 1 && !in_mklev` rule. Wizard, Healer, Tourist, Ranger, Rogue, and Priest startup inventory mark current wielded/worn/alternate/quivered object state where currently modeled. Other roles and full inventory use/wear/discovery side effects remain incomplete. Evidence: `seed0013-rogue-friday13-combat` now has full parity (`S 59/59 R 4838/4838`); `seed0101-ranger-quiver-throw-travel-engrave` now has full parity (`S 27/27 R 2371/2371`); `seed0013-friday13-save-then-fullmoon-restore` now has full parity (`S 99/99 R 4804/4804`); `seed1800-tourist-eat-throw` now has full parity (`S 26/26 R 2458/2458`); `seed5002` inventory display shows the initial weapon/cloak state at screen 276 without regressing `seed0116`; `seed0002` now assigns the picked scroll letter `m` from live Healer inventory; `seed0106-priest-extcmd-sweep` is exact (`S 267/267 R 4194/4194 C 0`); `seed8000-tourist-starter` is exact (`S 23/23 R 3130/3130 C 0`) while using live `o_init()` and Tourist inventory before replay tails. |
| Samurai startup, inventory, and two-weapon evidence | `role.c:roles[]`, `u_init.c:Samurai[]`, `u_init.c:Skill_S[]`, `attrib.c:sam_abil[]`, `dog.c:makedog()`, `allmain.c:u_calc_moveamt()`, `wield.c:dotwoweapon()`, `weapon.c:weapon_hit_bonus()` | `js/u_init.js`, `js/allmain.js`, `js/cmd.js`, `js/dog.js`, `js/mklev.js` | 🟡 | Samurai now uses role-driven starting attributes/HP/Pw/AC, starts with katana, wakizashi, yumi, ya, and lacquered rustproof splint mail, grants level-1 intrinsic Fast with the 1/3 extra-action `rn2(3) == 0` cadence, applies Samurai class preknowledge/discovery ordering from `knows_class()`, and christens the default starting little dog `Hachi` when no `dogname` option overrides it. Japanese item names cover current Samurai inventory/discovery rows (`wakizashi`, `shito`, `ninja-to`, `gunyoki`, `sake`), ya remains singular in quantity and bow ammo shows `(in quiver)`. `#twoweapon` is non-autocomplete, toggles `u.twoweap`, rolls `rnd(20)` against Dex for turn cost, displays right/left-hand wield suffixes, and the current melee front door applies the unskilled two-weapon hit penalty plus a secondary swing. `#enhance`/`#sit` autocomplete and current Samurai skills/sit-on-corpse frames are modeled. `^X` reports wielded katana by the long-sword skill class and basic skill from startup inventory. Evidence: `seed0107-samurai-twoweapon-enhance` is exact (`S 98/98 R 2902/2902 C 0`) and `seed0700-samurai-explore-descend` is exact (`S 51/51 R 3230/3230 C 0`) with sentinels exact. |
| `o_init` (object type shuffle) | `o_init.c` | `js/o_init.js` | 🟡 | General RNG shape for gem colors, description shuffle ranges, and `WAN_NOTHING` direction is implemented for non-replay sessions. Runtime `oc_color`, `oc_material`, and current ring/amulet/potion/scroll/spellbook/wand/venom plus shuffled helm/glove/cloak/boot descriptions are retained for map display, material predicates, and unknown carried appearance names where user-facing naming exists (`C ref: objects.h` descriptor rows, `o_init.c:shuffle_all()`). Novel and Book of the Dead fixed descriptors are retained outside the ordinary spellbook shuffle range. Toughness swaps, discovery persistence, and complete user-facing identity state are still incomplete. |
| Starting spellbook filter | `u_init.c:ini_inv_mkobj_filter()`, `u_init.c:restricted_spell_discipline()` | `js/u_init.js` | 🟡 | Random startup spellbooks now reject spells outside Priest starting skill disciplines (`P_HEALING_SPELL`, `P_DIVINATION_SPELL`, `P_CLERIC_SPELL`) in addition to the existing level/no-create checks, so Priest random spellbooks consume rejected enchantment/matter/attack books before accepting allowed ones. Evidence: `seed0501-priest-cast-read-turn` is exact (`S 28/28 R 2238/2238 C 0`) and `seed0106-priest-extcmd-sweep` remains exact. |
| Welcome / lore screens | `sounds.c`, `pline.c`, `role.c:Hello()` | `js/allmain.js:newgame()` + `js/roles.js:roleGreeting()` | 🟡 | Welcome greeting is role-driven rather than seed-driven. Configured role/align splash text now uses role god/rank data for the generic quest intro overlay and honors `OPTIONS=!legacy` by skipping that pager; Tourist legacy lore uses the C-like left columns and clears only the text rows so the right-side map remains visible below `--More--`, and Ranger neutral/chaotic lore now uses Venus/Mars wording and goddess/god titles from `role.c:roles[]`. Startup welcome `--More--` is forced by a following tutorial prompt or queued `moveloop_preamble()` date messages; `welcome(TRUE)` alone does not block just because the text is wide or because a legacy pager was just dismissed. Width only decides whether an already-forced prompt stays on row 0 or moves to the next message row. The late `nhlib.lua` shuffle is now scoped to the legacy pager path, while full-moon/new-moon/Friday-the-13th `moveloop_preamble()` messages queue behind the welcome prompt as normal topl messages. Full chargen/menu flow and exact windowing remain incomplete. Evidence: `seed0017-samurai-altar-pray`, `seed0101-ranger-quiver-throw-travel-engrave`, `seed0398-wizard-wandpoly-pile`, and `seed1800-tourist-eat-throw` have exact startup lore/welcome frames; `seed5002` remains a full pass. |
| Tutorial prompt | `pline.c`, `options.c:ask_do_tutorial()` | `js/cmd.js` prompt overlay + seed0002 override | 🟡 | General post-welcome tutorial prompt now appears when `tutorial` was not set in config, including Space/Enter invalid-choice redraw. Generated and legacy startup override tutorial screens now share answer cleanup, so `y`/`n` clears stale welcome topline state before regular play resumes. |

---

## 👁️ Vision & Display

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Shadow-casting vision (`vision_recalc`) | `vision.c` | `js/vision.js` | 🟡 | Implemented. Spot-checked for seed8000/seed0002. Waterwall, lava-wall, cloud terrain, and boulders now block sight even though some are not movement-obstructed terrain (`C ref: vision.c:does_block()`). Adjacent night vision now distinguishes room interiors/open doorways from dark corridor endpoints: neighboring walls and `SDOOR` cells can be seen from rooms and open doors, while dark-corridor endpoint walls are not promoted by the same rule. |
| Terrain glyph rendering | `display.c:newsym()` | `js/display.js:newsym()` + `terrain_glyph()` | 🟡 | Mostly correct. DEC line-drawing output works. Rogue levels render through a no-color Rogue symset with Rogue object-class symbols and `*` for status gold; Gehennom walls use the Gehennom wall color, and lava made by special terrain setters is lit for vision. TTY `CLR_GRAY` now normalizes to `NO_COLOR`, ordinary stairs render in default color while branch stairs render yellow only when `known_branch_stairs()` would succeed (`isbranch`, traversed, and destination dungeon differs), undiscovered `SDOOR` cells render as oriented walls, T-wall glyphs use `seenv` for the visible wall angle (`C ref: display.c:wall_angle()`), liquid terrain renders with the corpus-compatible DEC backtick byte and tree terrain renders with the corpus-compatible DEC `S_tree` payload byte `g` (`C refs: display.c:back_to_glyph()`, `dat/symbols` DECgraphics), and pools/lava cover floor objects/traps in map rendering (`C ref: display.h:covers_objects()`). Lit corridors remember/display as white where C's color correction needs it, out-of-sight remembered corridors can downgrade through the dark-room/color rule, open doors render as DEC checkerboard for both orientations, visible/remembered spot-shown engravings render as bright-blue `S_engroom`/`S_engrcorr` glyphs before ordinary terrain and below floor objects (`C refs: src/display.c:_map_location()`, `include/engrave.h:engraving_to_defsym()`), and trap glyphs use the `rm.h:trap_to_defsym()` mapping. `newsym()` now stores terrain/object/trap memory before monster overlay and while the hero is drawn on top, matching `_map_location(show=false)` so unseen pets and stepped-off floor objects do not remain as wrong remembered map glyphs. `#terrain` known-map mode serializes base terrain without hero/monster/object/trap overlays, keeps the temporary terrain map under the getpos tip, distinguishes explicit `a` terrain-menu selection from implicit Space/Enter selection, and exits through the tty `Done.--More--` cursor contract (`C ref: cmd.c:doterrain()`, `detect.c:reveal_terrain()`). |
| Object rendering on map | `display.c:newsym()` | `js/display.js:newsym()` | 🟡 | Renders live objects from `game.level.objects[]` with runtime `objects[otyp].oc_color`, including `o_init()` shuffled appearance colors for non-replay sessions. Ordinary non-hallucinated statues render as the represented monster symbol with statue object color (`display.h:statue_to_glyph()`), so Oracle centaur statues display as `C` rather than generic object glyphs. Under Hallucination, visible objects now use `display.h:random_obj_to_glyph()` shape on the display RNG, with `NUM_OBJECTS` excluding the generated sentinel slot, corpse glyphs taking a random monster color, and statues taking the random-monster/gender path while remembered statue memory uses a separate `random_obj_to_glyph()` call like `display.c:map_object()`. Remaining visible color mismatches with exact RNG should be treated as object identity/state drift, retained glyph timing, or display-RNG ownership before blaming class-default rendering. |
| Corpse object rendering | `display.h:corpse_to_glyph()`, `display.c:reset_glyphmap()` | `js/display.js:object_glyph_for_display()` | 🟡 | Normal non-hallucinated corpses render as `%` with the represented monster/body color from `corpsenm`, not the generic corpse object color. Evidence: `seed1500-rogue-explore-move` fixed the trap-victim orc corpse color without display RNG changes. |
| Wizard and magic mapping | `wizcmds.c:wiz_map()`, `detect.c:do_mapping()`, `detect.c:show_map_spot()` | `js/display.js:map_level_for_wizard()` | 🟡 | Wizard `^F/#wizmap` now pre-marks every trap seen before the mapping pass, while ordinary scroll magic mapping still runs `do_mapping()` without revealing unseen traps. Mapping redraws terrain/traps/monsters, keeps floor objects tied to prior visibility, and updates remembered mapped foreground glyphs such as engravings/traps/bars without overpainting the hero square; tty text-window dismissal redraws through `docrt()`/`newsym()` so `@` remains on top of a remembered foreground glyph. Evidence: `seed2200-wizard-quaff-zap-read` is exact, with strict sentinels stable. |
| Monster rendering on map | `display.c:newsym()` | `js/display.js:newsym()` | 🟡 | Renders from `game.level.monsters[]` and now overlays warning glyphs for unseen hostile monsters within `Warning` range (`C ref: display.h:_mon_warning()`, `display.c:display_warning()`). Warning first gates on real monster level before Hallucination randomizes the displayed warning level, stale warning digits clear to memory/blank when `mdistu()` range changes, and warning refresh is driven from the main input-boundary plus non-hallucinated post-move redraw paths instead of every screen serialization. The Warning branch is skipped when the Hallucination branch already ran, matching `allmain.c:moveloop_core()`. Swallowed `newsym()` returns before external monster/object/trap redraws like C, visible cells respect `_mon_visible()` for undetected hiders and invisible monsters (`C ref: display.h:_mon_visible()`), and visible-but-unseen warned monsters show warning glyphs instead of terrain. Non-visible `newsym()` no longer materializes hallucinated monster/object glyphs before deciding to show memory/warnings. Wizard mapping now uses `wizcmds.c:wiz_map()`/`detect.c:do_mapping()` shape for terrain, traps, and monsters without revealing floor objects in current evidence; trap and throne glyph colors follow the current cmap evidence, and secret-door orientation can be derived from neighboring walls when map metadata is stale. Visible real monsters use `what_mon(..., rn2_on_display_rng)` for hallucinated appearance glyph/color; unseen warned monsters use the Hallucination warning display RNG during real monster postmove redraws without applying that to JS-only swallowed-expulsion redraw sweeps. Ordinary successful hero hits do not force an extra visible monster redraw; C only redraws on specific reveal/state changes. Single-step movement/attacks now distinguish C's `context.mv` from turn-consuming `context.move`, so the once-per-input Hallucination redraw can run after ordinary swallowed attacks. Hallucinated monster pickup now refreshes the monster layer after removing the object so the following Hallucination object refresh starts from the C display-RNG position (`C ref: mon.c:mpickstuff()`, `allmain.c:moveloop_core()`). Mimic hallucination substitutions and many visibility edge cases remain incomplete; `seed0383` is currently exact. |
| Remembered glyph (out-of-sight) | `display.c` | `js/display.js` | 🟡 | Implemented via `remembered_glyph`. |
| Status line (bot) | `status.c` | `js/display.js:bot()` | 🟡 | Verified passing for seed8000 (23/23). Bottom line now includes the active hallucination condition abbreviation (`Hallu`) for current swallowed Wizard evidence; broader condition ordering/encumbrance/status coverage remains partial. |
| Message line (pline) | `pline.c` | `js/display.js:pline()` + `js/cmd.js:rhack()` | 🟡 | Message lifetime now matches prompt-time capture better: persist through `nhgetch`, clear on next real command start, and preserve queued messages behind override `--More--` screens. More prompts are rendered into the terminal grid with row-0 cursor placement, queued `--More--` prompts are gated before normal command dispatch, and current monster-turn and fire-destruction plines can perform nested tty `--More--` reads before death/pickup or inventory-destruction side effects continue. Current fire destruction also defers turn-tail monster movement while `zap.c:zhitu()` is blocked and reaches the wizard-mode death confirmation prompt after `You die...`. Wizard-mode death now resumes interrupted monster turns after `savelife()` and prints `gn.nomovemsg` immediately unless a blocking monster/pet message defers it (`C refs: end.c:done()/savelife()`, `allmain.c:moveloop_core()`). Wizard `#levelchange` now queues `pluslvl()` welcome and innate-ability messages with tty-style prompt packing (`C ref: exper.c:pluslvl()`, `attrib.c:adjabil()`). Broader message production is still partial. |
| Sounds system | `sounds.c` | `js/sounds.js` | 🟡 | Basic `dosounds` includes fountain/sink gates, current special-room ambient `rn2(200)` gates for courts/swamps/vaults/beehives/morgues/barracks/zoos, the temple `rn2(200)` gate with Sanctum/Astral suppression, and the Oracle `rn2(400)` gate. Current vault and tended-shop ambient messages now render and use tty topline packing/More behavior, including queueing the post-`dosounds()` turn tail when a sound cannot pack, latching run-sound `--More--`, and deferring current floor-look text until the sound More is dismissed. Remaining special-room messages, guard state, and full run/More map/RNG separation are still incomplete. |
| Full-screen menus (inventory, spells, discoveries, attributes) | `pline.c`, `invent.c`, `spell.c`, `pager.c`, `wizcmds.c:wiz_intrinsic()`, `insight.c`, `mhitu.c:magic_negation()`, `win/tty/getline.c:hooked_tty_getlin()` | `js/cmd.js` menu builders + `_override_screen` lifecycle | 🟡 | Inventory `i` now builds a grouped menu from live inventory, clears the C-like right-side window rectangle, preserves underlying map/status cells, uses live shuffled descriptors for unknown current evidence, emits alternate weapon suffixes with singular/plural `objnam.c:doname_base()` wording, and calls the object-glyph display path for each menu object row like `invent.c:display_pickinv()` even when tty text is the only visible surface. Wizard `+`, `\`, and `^X` now build current evidence spell/discovery/attribute windows: startup spellbooks populate a known-spell list through `u_init.c:ini_inv_use_obj()`/`spell.c:initialspell()`, spell rows use role/current spell-skill levels for chance/retention plus C-like `dospellmenu()` widths including debug-mode `turns` columns and tty overlay placement from the widest row, discovery rows paginate live discovered/encountered object state plus the `skill_based_spellbook_id()` shape, and insight pages derive HP, XP, luck, prayer timeout, live inventory weight/capacity (including gauntlets of power at object-table weight 30), worn armor magic cancellation, magic resistance, moon/Friday background lines, and row-budget page splits from live state where current evidence needs it. High-level debug insight hunger still uses the established fallback until missing command/item hunger side effects are modeled. `#wizintrinsic` opens the paged `NHW_MENU`, honors cmdassist text, toggles hallucination on `h`, and commits on Enter with the right message-more boundary; `#wizgenesis` getlin prompts keep the C invisible input-space cursor. Full-screen tty menu/text dismissal runs the `erase_menu_or_text()` full redraw shape (`docrt()+flush_screen`) before the selected command resumes, while active override menus suppress the normal input-boundary Hallucination refresh because C is still inside `select_menu()`. Residual debt: complete discovery persistence, full spell/insight calculations, non-Wizard/non-Tourist attribute text, and broader tty windowing. Evidence: `seed0013-rogue-friday13-combat` now has full parity (`S 59/59 R 4838/4838`); `seed0108`, `seed0116`, and `seed0383` are full public passes; `seed0360` and `seed5002` remain stable with exact RNG on the menu evidence. |
| Insight weapon-skill rows | `insight.c:attributes_enlightenment()`, `u_init.c` role skill tables | `js/cmd.js` | 🟡 | The current insight page derives visible weapon skill text for modeled startup weapons: Ranger dagger/bow, Rogue short sword, Knight long sword/lance, and other current role-start weapons report `basic` through C's startup `skill_init()` shape rather than role maximums (`C refs: src/weapon.c:skill_init()`, `src/u_init.c:Skill_K[]`). Evidence: `seed0104-knight-ride-combat`, `seed1500-rogue-explore-move`, and Ranger insight sessions are exact. |
| Spell casting and study front doors | `spell.c:docast()`, `spell.c:spelleffects_check()`, `spell.c:spelleffects()`, `spell.c:study_book()`, `zap.c:zapyourself()` | `js/cmd.js` | 🟡 | `Z` now opens the same tty spell menu layout as `+` with the cast prompt, selects known spells by letter, spends spell power after the percent-success `rnd(100)`, exercises Wisdom, creates the pseudo spell object, and enters the direction prompt for directional spells. Current Priest evidence covers self-cast healing through `zapyourself()` (`d(6,4)`, `You feel better.`) and known-spellbook rereading via the `You know "... quite well already."--More--` / `Refresh your memory anyway? [yn] (n)` prompt chain. Evidence: `seed0501-priest-cast-read-turn` is exact (`S 28/28 R 2238/2238 C 0`). |
| DEC line-drawing output | `curses.c` | `js/display.js` | ✅ | Outputs Unicode directly; runner translates DEC→Unicode for comparison. |
| `cls()` / `docrt()` | `display.c` | `js/display.js` | 🟡 | Functional. |
| Hallucination display context | `display.c`, `do_name.c:rndmonnam()` | `js/rng.js` + `js/display.js` + `js/allmain.js` + `js/random_text.js` | 🟡 | Display RNG is now split from core RNG; tty black uses the same bright-black SGR shape as upstream (`wc2_darkgray`), and visible monster/object glyphs consume display RNG through `what_mon()`/`random_obj_to_glyph()`-shaped paths when hallucinating. Object glyph colors read runtime `objects[otyp].oc_color` state after `o_init()` shuffles, not the static generated table. The generated monster table now strips inactive `CHARON` rows, so active `NUMMONS` is 383 like this C build. `#wizintrinsic` hallucinatory transitions route edge changes through a shared swallowed-redraw helper (`make_hallucinated()->swallowed(0)`), `moveloop_core()` refreshes hallucination at C-like `!context.mv` input boundaries with `see_monsters(); see_objects(); see_traps();`, but suppresses that refresh while a `getlin()`/`yn_function()` prompt or active tty menu is still inside the interrupted command. `see_traps()` only redraws cells currently showing a trap glyph like C `glyph_is_trap(_glyph_at())`, the Warning branch is not repeated after that Hallucination branch, hallucinated Warning glyphs use `display_warning()`'s random warning level during that refresh, and ordinary monster postmove redraws now also consume the Hallucination warning display RNG for unseen warned monsters. `topl.c` More dismissal gets the resumed swallowed redraw, hallucinated monster names now use `rndmonnam()`/padded `bogusmon` data on display RNG for hero-hit and monster-turn plines, including `Monnam(mtmp)`-style article handling for visible monster pickup messages, and hallucinated liquid names use `do_name.c:hliquid()` display RNG for water/lava descriptions. Level teleport now models the old-level `vision_recalc(2)` warning-glyph redraw, full-screen menu dismissal redraw, arrival-side monster overlay, and pickup-layer refresh needed by current `seed0383` evidence. `docrt()` redraws remembered glyphs then overlays monsters rather than running a full visible `newsym()` sweep, initial swallowed display consumes the hidden `swallowed()` redraw batches behind More before the visible stomach frame, and swallowed expulsion latches the old stomach/status frame across the blocking More, defers `unstuck()`/relocation until that More is dismissed, then runs the resumed hallucination refresh before the normal input-boundary refresh. `seed0383` now has full screen and RNG parity; remaining hallucination debt is broader coverage outside that path rather than a known current `seed0383` blocker. |

---

## 🕹️ Command Dispatch (moveloop)

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Main game loop (`moveloop_core`) | `allmain.c:moveloop_core()` | `js/allmain.js:moveloop_core()` | 🟡 | Basic loop. Fast-forward stubs for per-step RNG. Turn tails now consume the C `u_calc_moveamt()` very-fast `rn2(3)` front door when the modeled hero state has speed boots/fast active, and a successful very-fast roll carries one pending extra action that returns to the input boundary after the next monster scan without draining additional monster passes or running the turn tail. Prayer timeout (`u.ublesscnt`) drains once per real turn before hunger, including occupation and catch-up monster turns. Turn-tail random monster generation uses the C denominator split: `rn2(25)` for demigod, `rn2(50)` below the stronghold, otherwise `rn2(70)`. Hallucinated level-arrival display keeps the pre-`docrt()` `vision_recalc(0)` for map memory, then performs one object redraw after `docrt()` before the materialize pline. Negative-`multi` helpless turns now continue hidden turn advancement and print the final `nomovemsg` at the `unmul()` boundary for current paralysis evidence; burdened helpless/vomit finish only partly repays movement debt before the next catch-up pass (`C ref: allmain.c:moveloop_core()`, `allmain.c:u_calc_moveamt()`). Run/travel repeat loops now stop on blocking `--More--` before consuming another movement turn and can resume the same repeat tail after dismissal without charging a fresh turn, while prayer occupations still finish their countdown and `gn.nomovemsg` before returning to the input boundary; current intrinsically-fast prayer evidence keeps the extra movement allocation aligned before `prayer_done()`, and wizard force-prayer restores the queued countdown when tty More interrupts the first monster turn before its turn tail (`C refs: topl.c:pline()/more()`, `pray.c:dopray()`, `allmain.c:u_calc_moveamt()`). |
| Command dispatch (`rhack`) | `cmd.c:rhack()` | `js/cmd.js:rhack()` | 🟡 | Move commands, uppercase vi run commands, rest/wait (`.`), safe wait/rest prevention for `.` near visible hostile monsters, safe-search prevention for `s` near visible hostile monsters, and the `monster_nearby()` front door now skips helpless sleeping/immobile monsters and `AT_BOOM`-only non-attackers, close-command direction prompting with tty cmdassist invalid-direction help, empty-square pickup messaging, `#pay` no-shopkeeper message plus current unpaid itemized billing/payment menu, basic drop, put-on/wear prompts for rings, amulets, immediate shield wear, and delayed armor evidence, zap-wand prompt/direction flow with zappable charge/backfire timing plus current digging/fire/sleep/polymorph front doors, carried-food `e` prompts including getobj invalid-letter `--More--` persistence and fortune-cookie rumor/More flow, zero-time object-call naming prompt completion after potion use, current scroll-of-light, scroll-remove-curse, scroll-enchant-weapon, potion-paralysis, potion-confusion, potion-booze/call, healing and extra-healing potion effects, sink inventory-prompt fallthrough, sink sewage/vomit negative-multi side effects, and sink read/quaff side effects, basic throw prompts with stack split `next_ident()` and hard-wall `breaktest()` ownership, basic `a` apply invalid-letter prompt persistence with non-dismiss keys held at the invalid-letter `--More--`, C-like no-applicable-object apply wording from `getobj()`, `:` look-here stairs/doorways/sinks/fountains, singleton floor-object packing, dungeon-exit stair `--More--` blocking, and shop-price messaging, `>`/`<` stair front doors with deferred ordinary-stair arrival messages, basic wall/non-door kick and wounded-leg kick rejection, open-door Strength exercise and vision redraw on current evidence, wizard wishes for current object evidence including explicit BUC/enchantment state and namedesc cross-class lookup, debug `^G`/`#wizgenesis` create-monster prompt/name dispatch, `#quit` confirmation/dump-core wizard exit, `#wizintrinsic` active-menu dispatch, debug `^V` numeric/menu level teleport with current-row marking, debug `^F`/`#wizmap` terrain/trap/monster mapping, generated-depth ordering and generated-target dispatch for Gehennom/Valley rows, Valley entry arrival `--More--` queueing, Gehennom temperature/smoke arrival messages, Wizard quest-start firsttime pager ordering, basic `#chat` autocomplete/prompt/self-direction handling, `#loot` box/container flow, `#dip` fountain prompt/confirm/effect front doors, off-altar `#offer`, `#terrain` extended-command selector, `#chronicle`/`#conduct`/`#overview`/`#version`/`#vanquished`/`#genocided` disclosure pages, `#adjust` prompt cancellation, basic `#ride` autocomplete/mount/dismount direction flow for saddled steeds, wizard-aware extended-command completion with C-like typed-prefix cursor, and C-like deferred execution after `rhack()` returns, `#levelchange` prompt/level-gain RNG plus welcome/innate message packing, and zero-turn inventory/spell/discovery/attribute menu lifecycles are present. TTY overlay menus clear only their owned menu band/rows for current name/terrain/conduct/vanquished evidence. `_` travel now has the first-use farlook tip/page, forced `getpos()` cursor lifecycle, known-stair feature search, line-feed south movement, C `truncate_to_map()` cursor edge-shortening, invalid-key messages, stale getpos-error clearing, and `.`/`,` target selection needed by current Wizard quest evidence. Controlled teleport and farlook exits clear stale getpos prompt cursors back to the hero/map position. Next `#wizintrinsic` work belongs in zero-time active-menu dispatch, not in a prompt-letter branch: Enter after completing the extended command opens the menu, `h` toggles a row, and Enter commits side effects. Run commands, delayed armor occupations, and heavy-pickup encumbrance now consume repeated movement/turn loops before the next input boundary, apply deferred armor AC for the final occupation turn, run speed-boot `makeknown()`/Wisdom exercise on the delayed finish, let cloak-of-displacement discovery own its single `makeknown()` Wisdom exercise before the displacement line, and can pause/resume on tty `--More--` during delayed occupation, queued fire-zap side effects, heavy-pickup/shop-quote messages, current stair-arrival messages, current run-sound floor-look deferral, and current monster-turn message boundaries. Message/redraw-interrupted runs keep their open-door boundary stop scoped to that interrupted run rather than all uppercase corridor runs. Stop/turn/lookaround rules, full travel/farlook cursor semantics, complete shop billing, and full interruption semantics remain approximate. Most commands remain unimplemented. |
| Apply lock tools | `apply.c:doapply()`, `lock.c:pick_lock()` | `js/cmd.js` | 🟡 | Lock picks and credit cards now enter the adjacent-direction prompt and the visible non-door branch prints `You see no door there.` while consuming the learned-something turn for current evidence (`C ref: src/lock.c:pick_lock()`). Full lock, door, box, and trap unlocking semantics remain incomplete. |
| Help, look, and tty text pagers | `pager.c:dohelp()/dowhatis()/dowhatdoes()`, `version.c:doextversion()`, `win/tty/wintty.c:process_text_window()/dmore()`, `getpos.c:getpos()`, `engrave.c:read_engr_at()` | `js/cmd.js` help/look state machines + `js/display.js` tty cursor width | 🟡 | Global `?` now opens the C-shaped help menu, dispatches static/about, local `dat/` text files, option help, key binding, menu-control, support, and `dowhatdoes()` prompt pages through a 23-row tty text pager that ignores non-dismissal keys. `doextversion()` consumes the one-time `dat/nhlib.lua:shuffle(align)` `rn2(3), rn2(2)` runtime-info initialization. `/` opens the look-at chooser, typed data lookup, inventory lookup, monster/object/engraving lists, and farlook `getpos()` loop with uppercase movement, More-info prompts, ANSI cursor-width parity, terrain-specific More boundaries, and ESC cancellation back to the hero cursor after a farlook description. Text/data lookup page dismissal clears the active override and uses the full-screen tty redraw path before command dispatch resumes, so map memory and hero overlay state match `erase_menu_or_text()`. `:` look-here reads visible dust engravings before falling back to `look_here()`-style feature lines and singleton floor-object text, including open doorways and the dungeon-exit stair rule: block with `--More--` only when object text follows the feature line. Evidence: `seed2200-wizard-quaff-zap-read` is exact with strict sentinels stable. |
| Cardinal movement (`domove`) | `do.c:domove()`, `hack.c:lookaround()` | `js/cmd.js:domove()` | 🟡 | Basic movement plus occupied-monster and swallowed-hero front doors: movement into a tame/peaceful monster square now enters the safe-pet `do_attack()` guard, consumes the `rn2(7)` displacement gate, then swaps positions and prints the ordinary displacement message when safe, while hostile occupied squares consume a turn, leave the hero in place, and use the same narrow current-evidence `uhitm()` RNG shape as swallowed directional movement into `u.ustuck`. Hero melee now applies damage to retained monster HP, prints kill text before `xkilled()` side effects when damage is lethal, handles a minimal tame-kill `abuse_dog()`/`xkilled()` front door, and uses current weapon small/large damage dice plus `dbon()`-style strength bonus for current Knight evidence (`C refs: src/weapon.c:dmgval()/dbon()`, `src/uhitm.c:hmon_hitmon_dmg_recalc()`). Successful movement now performs floor look, pauses multi-object floor lists before the monster turn, allows diagonal movement through `D_NODOOR` doorways, rejects Sokoban diagonal boulder pushes and boulder-corner squeezes with C-like messages, supports the current ordinary cardinal boulder push with Strength exercise and post-push vision reset, and runs uppercase corridor turns through a closer `lookaround()`-shaped `corrct`/`noturn`/`last_str_turn` path that can turn toward doors without taking earlier boulder detours. Full `uhitm()`, boulder traps/pools, traps, and pickup remain incomplete. |
| Zero-turn commands (inventory, ESC, etc.) | `cmd.c` | `js/cmd.js` | 🟡 | `g.context.move=0` on zero-turn commands confirmed in lessons. Wizard wishes for current evidence include speed boots namedesc selection, and apply prompts now distinguish absent inventory letters from carried but inapplicable non-tools. |
| Per-step monster movement / regen RNG | `monmove.c`, `regen.c`, `were.c`, `track.c` | `js/monmove.js` + `js/track.js` + partial turn consumers | 🟡 | Per-step replay is no longer called from `moveloop_core()`, and the dead `fastforward_step()` exports have been removed from the seed replay modules. `makemon()` now preserves C `fmon` head-insertion order, `movemon()` snapshots the pass like `iter_mons_safe()`, `mcalcmove()` handles slow/fast speed states, `mcalcdistress()` owns human-form werecreature `were_change()`, vampire-shifter `decide_to_shapeshift()` front doors, `mspec_used` timeout, and monster frozen-state timeout before movement allocation, damaged-hero `regen_hp()` owns the post-generation `rn2(100)` turn-tail roll, `exerchk()` handles hunger/Con exercise plus hallucination/confusion Wisdom abuse and wounded/fumbling/stunned Dexterity abuse on 5-turn boundaries, fog clouds retain vapor regions, keep zero-TTL regions visible until the next region tick, apply the fog-cloud `inside_gas_cloud()` TTL extension, and age after the final monster movement pass for the turn, uppercase run commands reuse the normal turn advancement loop, `settrack()` records the hero track ring at the turn-boundary and `initrack()` clears it on new games and level teleports (`C ref: track.c:settrack()/gettrack()`, `cmd.c` level-change `initrack()`), sleeping/frozen monsters and object/furniture-appearing hiders spend movement without entering `distfleeck()`, and ordinary non-pet monsters now take a minimal `dochug()`/`m_move()` path with post-`m_move()` `distfleeck()`, swallowed-bystander `MMOVE_MOVED`, `set_apparxy()`-style hero targeting, swallowed hero coordinates pinned to `u.ustuck`, peaceful/light approach gates, confusion/stun recovery gates, `mtrack` backtracking rolls, hero-track retargeting when `should_see` is false, `m_balks_at_approaching()` front-door behavior for polearms, aklys autoreturn range, current ranged-attack availability, and cobra-style `AT_SPIT` venom through `spitmu()`/`m_throw()` front doors (`C refs: monmove.c:m_balks_at_approaching()`, `mhitu.c:ranged_attk_available()/mattacku()`, `mthrowu.c:spitmu()/m_throw()`), partial `mfndpos()` terrain eligibility for flying/swimming/pool/lava/wall-passable squares, boulder-occupied square exclusion unless `ALLOW_ROCK` applies, diagonal door blocking, openable closed-door candidates, trapped-door `postmov()`/`mb_trapped()` damage, minimal `m_search_items()`/`mpickstuff()` collector pickup with visible pickup plines that leave tty `--More--` pending until the next monster pline boundary, boot `m_dowear()` delay, creation-time boot wear, constrained post-move `mattacku()`/negative-AC `AC_VALUE()` for moved weapon users, a swallowed pre-finish turn front door matching `allmain.c:moveloop_core()`'s hero-can-move loop, and minimal `weapon_check` handoff for ranged failure into HTH wielding. Movement AI remains skeletal. `seed0116` and `seed5002` now pass completely with full RNG parity; `seed8000` remains complete on screens, `seed0383` reaches `S 196/219 R 16915/16915`, and `seed0360` reaches `S 679/833 R 111164/120639` with sentinels stable. |
| Turn counter | `allmain.c` | `js/allmain.js` | ✅ | `g.moves++` conditional on `g.context.move`. |
| Game over (`gameover`) | `end.c` | `js/cmd.js` (partial) | 🔴 | Wizard/explore death prompts, bones prompts, tombstone text, and default inventory disclosure prompt are partially implemented for current evidence. `really_done()` ordering now asks the possessions disclosure prompt before the tombstone unless `OPTIONS=disclose:-i` suppresses it (`C ref: src/end.c:really_done()/disclose()`). Loop termination, full disclosure pages, final scoring, and broad end-game cleanup remain incomplete. |

---

## 🎒 Items & Objects

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Object type table / `o_init` shuffle | `objects.c`, `o_init.c` | `js/object_data.js` + `js/o_init.js` | 🟡 | Static upstream object class/probability/charge/direction/material/color metadata is available for `mkobj` selection, erosion eligibility, and map display. `init_objects()` now consumes the general C description-shuffle RNG shape for non-replay sessions and the seed0002/seed8000 replay paths before their remaining dungeon/u-init replay tails, retaining shuffled color/material plus ring/amulet/potion/scroll/spellbook/wand/venom and shuffled armor-subrange description state. This reduced seed-specific startup replay debt, fixed the seed0002 startup wand map color by giving the generated `WAN_SLEEP` object C's shuffled `crystal`/bright-cyan appearance, and lets seed8000 Tourist item names/colors come from live `o_init()` state. Generated local IDs corrected current stale wand constants (`WAN_MAGIC_MISSILE=429`, `WAN_FIRE=430`, `WAN_COLD=431`, `WAN_DEATH=433`, `WAN_LIGHTNING=434`), so unknown wished wands use their live shuffled descriptions such as `pine wand` and `curved wand`; unknown potions/scrolls/rings/spellbooks/armor/special books now use live appearance text such as `ruby potion`, `scroll labeled ZELGO MER`, `wooden ring`, `cloth spellbook`, `tattered cape`, and `paperback book`. Gem-class gray stones now follow C init gates for luckstone/loadstone/rock instead of the ordinary gem quantity roll. Toughness swaps, artifact selection/origin tracking, more replay-path startup replacement, and complete item identity/discovery parity are still incomplete. |
| Weapon object constants | `include/objects.h`, `include/skills.h` | `js/cmd.js`, `js/dog.js`, `js/mklev.js`, `js/monmove.js`, `js/u_init.js` | 🟡 | Current weapon-object ids distinguish object ids from skill ids: `DART` is object 24 while `P_DART` is skill 23, and `CROSSBOW_BOLT` is object 23. Trap, startup, pet, and visible monster-pickup naming paths now use the object id, restoring `seed0383`'s `6 poisoned darts` pickup line while keeping Tourist and Rogue evidence exact. |
| Random text data files | `rumors.c`, `engrave.c`, `makedefs.c`, `dat/rumors.*`, `dat/engrave.txt` | `js/random_text.js` + `js/random_text_data.js` | 🟡 | Random engraving/rumor selection builds padded/xcrypted virtual chunks from checked-in generated JS data and applies C-like `get_rnd_line()` and `wipeout_text()` RNG. Production JS is browser-safe and does not read upstream data files with `fs` at runtime. It currently serves graffiti generation; broader data-file consumers (epitaphs, bogus monsters, oracle text) still need integration. |
| Object placement on map | `mkobj.c:mksobj()` | `js/mklev.js:mksobj_at()` / `mkobj_at()` + legacy hardcoded arrays in `allmain.js` | 🟡 | Generated `_at` object creation now retains non-gold objects on `level.objects` with class glyph and upstream display-color state, uses C's newest-first `fobj` order, and `mkcorpstat()` now places created corpses/statues at the supplied coordinates instead of only consuming their init RNG. `mktrap_victim()` now retains the trap item, cursed random possessions, optional candle, and corpse at the trap square; trap-victim ammo now uses generated `ARROW`/`DART` IDs instead of stale hardcoded IDs. `mkcorpstat()` restarts ordinary rot timing when a caller override crosses a special-corpse timing boundary. Finishing a floor corpse eating occupation now deletes through the current `delobj_core()` front door, including `obj_resists(obj,0,0)`, after live rotten-corpse first-bite accounting. Wand polymorph pile hits now replace or delete floor objects through the same `mkobj()`/`poly_obj()`/`delobj_core()` front doors, preserving pile order and the destruction `obj_resists(0,0)` probe. Unburied `mineralize()` gold/gems are retained, while buried object chains, timer storage/effects, and PIT/landmine `breaktest()` destruction remain pending. Seed0002 still has legacy hardcoded startup objects, and object stacking/descriptions/material swaps/container contents remain incomplete. Evidence: `seed0116` dog blocker moved through the first run-turn mineral object scan; retained corpse/statue/trap-victim/mineral state and C-like floor-list order reduce hidden object-state debt for later dog/display/pickup work. Seed0002's carried Healer money now participates in later `dog_goal()` inventory scans; `seed0398-wizard-wandpoly-pile` is exact (`S 87/87 R 3026/3026 C 0`). |
| Trap-victim object piles | `mklev.c:mktrap_victim()` | `js/mklev.js:mktrap_victim()` | 🟡 | Trap-victim corpse species now preserves C's `PM_ELF`/`PM_DWARF`/`PM_ORC`/`PM_GNOME`/`PM_HUMAN` and role-corpse RNG shape, then stores local monster identities rather than stale placeholder numeric ids. This keeps corpse display/naming tied to the actual victim while retaining the trap item and possessions. Evidence: `seed1500-rogue-explore-move` is exact through the orc corpse color and pet reluctance message. |
| Gold | `mkobj.c`, `u_init.c` | `js/mklev.js:mkgold()` + `js/u_init.js` partial | 🟡 | Floor gold created during level generation now uses `GOLD_PIECE` object metadata for display color and merges quantities on existing squares. Healer and Tourist startup gold now create real carried `GOLD_PIECE` inventory objects with `$` invlet after role inventory, matching `u_init_inventory_attrs()`'s `ini_inv(Money)` path and feeding later pet inventory scans. Other player-gold roles and full money inventory/payment semantics remain incomplete. |
| Autopickup | `pickup.c`, `insight.c:attributes_enlightenment()` | `js/options.js`, `js/cmd.js` (partial) | 🟡 | `autopickup`, `pickup_types`, and `pickup_thrown` are parsed for current config evidence, and `^X` reports `Autopickup is on for '<types>' plus thrown` or `off` using the C insight wording. Actual pickup filters and exceptions remain incomplete. |
| Inventory management | `invent.c` + `pickup.c` | `js/cmd.js` + `js/u_init.js` partial | 🟡 | Inventory is still not a full `invent.c` port, but carried objects now receive persistent `invlet` letters lazily while preserving `$` for coin stacks and preassigned pickup letters, startup/wished/floor stackable objects use conservative `addinv()`-style merge semantics, wizard wishes preserve explicit blessed/cursed/uncursed and enchantment text for current known-object evidence, current named wand/tool wishes create inventory objects with shuffled unknown appearances, `wand of polymorph` uses its namedesc denominator and `(recharged:spe)` charge suffixes set `recharged`/`spe`, `wand of magic missile` uses the C namedesc pool that also sees the spellbook, exact chest wishes consume the C `rnd_otyp_by_namedesc()` `rn2(36)` pool before selecting `CHEST`, stethoscope/magic-marker/mirror/camera wishes use namedesc probability pools, magic-marker apply enters the write-on target prompt and preserves that prompt across invalid inventory-letter `--More--`, startup inventory clears cursed state before role adjustments and marks initial items known, known descriptor-bearing startup object types populate a limited discovery set so later floor objects of that type use real names without leaking per-object charges, basic pickup removes one floor object and prints the assigned inventory letter, gold pickup uses the coin inventory path, heavy chain-mail pickup queues the lifting message before adding the object and setting burden, basic drop places the object on `fobj`, `!verbose` drop suppresses the drop pline while preserving the prompt frame, `P`/`W`/`z`/`r`/`e` prompt filters use object class and worn state rather than current array position, worn/wielded startup object state feeds generated inventory menus, unknown magic-marker charges are hidden, and inventory display groups live carried objects while hiding BUC, enchantment, and charges for unknown appearance objects. Priest/Cleric object naming now suppresses `uncursed` under `implicit_uncursed`, potion-water discovery uses `potion of water (clear)`, and current irregular food plurals cover cloves of garlic and wolfsbane sprigs (`C ref: objnam.c:doname_base()`). Selecting an inventory-menu item now opens a stateful item-action menu; invalid action keys redraw it, `t` enters the shared direction prompt, invalid throw directions use the cmdassist `--More--` screen, throw/getobj prompts downplay singleton wielded weapons while still accepting `*`, apply/getobj suggestions compactify only when more than five letters, invalid throw letters block and redraw after More, invalid eat letters now block behind `You don't have that object.--More--` and redraw the eat prompt after dismissal like `invent.c:getobj()`, Space at read/throw prompts cancels with `Never mind.`, and `e` with no food reports `You don't have anything to eat.`. Consuming one item from a carried stack now takes the `splitobj()`/`next_ident()` ID path; stethoscope self-status uses the hero name plus `piousness()`, and apples use the Macintosh special line. Inventory menus now paginate current long lists and include a gems/stones group for current Healer evidence. Level-teleport arrival floor lists now resume behind the materialize More when the hero lands on visible non-gold objects, with current boulder and unknown-gem names following object naming state. Evidence: `seed0016` has full RNG parity through sleep/wake; `seed0105`, `seed0106`, `seed0108`, `seed0116`, `seed0360`, `seed0383`, `seed0398`, and `seed5002` are full passes; `seed0002` now sees and picks up the VE FORBRYDERNE scroll as `m`, carries startup gold for pet scans, picks up floor gold, handles chain-mail burden, recognizes a second wand of sleep by type, and reaches `S 234/595 R 10750/27158`. |
| Quiver/ready and basic ranged throw | `dothrow.c:throw_obj()`, `dothrow.c:dofire()`, `wield.c:doswapweapon()`, `invent.c:getobj()`, `weapon.c` | `js/cmd.js` | 🟡 | `Q` now opens the current ready prompt, handles alternate-weapon confirmation, marks the chosen stack `(at the ready)`, and clears stale alternate state for the current Ranger evidence. Throwing arrows without wielding a matching bow follows the by-hand path and prints the bow warning before the ordinary thrown-object flow instead of consuming multishot launcher RNG. `f` now enters the basic fire front door: quivered arrows with an alternate bow use the fireassist swap path, print the new wielded bow behind `--More--`, spend the swap turn, then resume at `In what direction?` with the arrow stack still pending. The discovery pager pads Ranger preknown weapon rows to tty row 23 before `--More--`. Full launcher matching beyond current bow/arrow evidence, multishot, projectile damage, and thrown-object landing/breakage remain incomplete. Evidence: `seed0101-ranger-quiver-throw-travel-engrave` is exact (`S 27/27 R 2371/2371 C 0`); `seed0102-ranger-name-cancel` is exact (`S 25/25 R 4485/4485 C 0`). |

---

## 👾 Monsters

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Monster placement (mklev fill) | `makemon.c`, `include/monsters.h`, `include/monflag.h` | `js/mklev.js:makemon()` + `js/monster_data.js` + `js/allmain_turns.js` | 🟡 | Random monster selection follows `rndmonst_adj()` reservoir sampling over generated `monsters.h` data and consumes `next_ident`, adjusted instance level/HP including golem fixed-HP no-RNG cases and the Wizard of Yendor base-level `adj_lev()` exception, generated `mattk[]` attack metadata, `peace_minded()` predicate/call order, broader selected-monster weapon/inventory gates, retained monster-init `minvent` objects, creation-time boot wear, `m_initthrow()` stack quantities, `mflags1` defensive-item predicates, Nazgul/lich inventory branches, leprechaun gold/minvent state, greedy gold dice/object creation, `rnd_misc_item()` slices, retained level monster records, `MM_ASLEEP`, Soko mimic boulder appearance state with post-`makemon()` explicit override, spider/snake hide-under object creation during mklev, sea-monster hide-under-water state during mklev, filled beehive special rooms with queen/killer-bee placement, KOP/wraith/zombie/salamander/angel weapon ordering, current sandestin/doppelganger/chameleon/vampire initial `newcham()` front doors before inventory, wizard `^G` named-monster creation through `makemon(ptr,u.ux,u.uy)`, and `goodpos()` occupied-square rejection for placement even without `GP_AVOID_MONPOS`. Turn-tail `maybe_generate_rnd_mon()` now calls live `makemon(NULL,0,0)` with C's demigod/below-stronghold/ordinary frequency split, and that path uses `makemon_rnd_goodpos()` before species selection like C. Exact selected-monster initialization/equipment, some special-level placement/selection details, full role-monster/guardian doppelganger shapes, chameleon animal branch, and real movement/weapon AI are still incomplete; `seed0116` has full RNG parity, `seed0360` now reaches Wizard3 movement at `S 355/833 R 86319/120639` with current first RNG blocker `FR 86078`, `seed0383` is exact again (`S 219/219 R 16915/16915 C 0`), `seed5002` now reaches `S 88/410 R 5934/12167`, and `seed8000` still blocks in live monster movement at FR 2985. |
| Monster wait strategy | `makemon.c:makemon()`, `monmove.c:dochug()`, `include/monflag.h` | `js/mklev.js`, `js/monmove.js`, `js/monster_data.js` | 🟡 | Generated monster data carries `mflags3`. `makemon()` initializes `mstrategy` from `M3_WAITFORU`, `M3_CLOSE`, and covetous bits unless `MM_NOWAIT`; `movemon()` clears `STRAT_WAITFORU` only when the monster can see the hero or is damaged, then suppresses sleep, `distfleeck()`, and movement while `STRAT_WAITMASK` remains set. Evidence: `seed0360` moved from `S 355/833 R 90410/120639 FR 86014` to `S 355/833 R 90438/120639 FR 86103` with sentinels stable. |
| Monster trap effects | `trap.c:maketrap()`, `trap.c:mintrap()`, `trap.c:trapeffect_slp_gas_trap()`, `trap.c:trapeffect_web()`, `mhitm.c:sleep_monst()` | `js/mklev.js:maketrap()` + `js/monmove.js:mintrap_basic()` | 🟡 | `maketrap()` reinitializes an existing same-square trap unless it is undestroyable, so later scripted traps overwrite stale entries instead of creating duplicates. Monster sleep-gas traps now check sleep resistance, breathlessness, and helplessness before `rnd(25)` and set timed `mcanmove=0`/`mfrozen` sleep. Monster web traps now leave ordinary monsters `mtrapped`, allow C-like web tearers to delete the web, and route the next `m_move()` through the pre-path `mintrap()` escape probe. Evidence: `seed0360` moved from `S 355/833 R 90438/120639 FR 86103` to `S 355/833 R 90439/120639 FR 90181` with sentinels stable; `seed0399` moved past the Bigroom web-trapped elf-noble `rn2(40)` boundary to `S 157/532 R 10658/11409 FR 10632` with strict sentinels exact. |
| Monster projectile traps | `trap.c:trapeffect_dart_trap()`, `trap.c:t_missile()`, `trap.c:thitm()` | `js/monmove.js:mintrap_basic()` | 🟡 | Monster arrow/dart traps now create a one-quantity projectile through `mksobj()`, run the dart poison gate, roll `thitm()` against current monster AC evidence, print visible hit/miss lines, and leave missed missiles on the floor. Trap plines produced behind an active tty `--More--` defer until the prompt is dismissed while RNG is consumed immediately. Evidence: `seed1500-rogue-explore-move` is exact through kitten dart-trap miss and pickup. |
| Rolling boulder trap launch | `trap.c:maketrap()`, `trap.c:find_random_launch_coord()`, `trap.c:mkroll_launch()` | `js/mklev.js:maketrap()` | 🟡 | Non-Sokoban rolling-boulder traps search a random launch line with `rn1(5,4)` and `rn2(8)`, require clear paths through both launch directions, reject pool/lava launch squares, create a boulder at the launch coordinate on success, and store both launch endpoints; Sokoban keeps the no-random trap-square launch. Evidence: `seed0360` moved from `S 355/833 R 90439/120639 FR 90181` to `S 355/833 R 98474/120639 FR 90693` with sentinels stable. |
| Monster movement | `monmove.c`, `were.c`, `track.c` | `js/monmove.js` + `js/track.js` (partial) | 🟡 | Ordinary monsters now have movement budget ownership, a pass snapshot matching `iter_mons_safe()`, post-`m_move()` `distfleeck()` recalc, swallowed-bystander `MMOVE_MOVED`, `set_apparxy()`-style hero targeting including displacement apparent-square RNG and the second `m_move()` targeting call, zeroed `makemon()` apparent target coordinates until `set_apparxy()` updates them, swallowed hero position sync while `u.uswallow`, a narrow adjacent-candidate `m_move()` skeleton, retained `mtrack` backtracking rolls, hero-track retargeting through `gettrack()` when the C `should_see` gate is false, the pre-allocation `mcalcdistress()`/human-were `were_change()` and vampire-shifter `decide_to_shapeshift()` gates, non-vampire shapeshifter `newcham(NULL)` selection/HP-fraction front doors, `mspec_used` timeout gates, retained fog gas regions aged after the final movement pass with zero-TTL visibility and fog-cloud TTL extension for `m_everyturn_effect()`, C-shaped front doors for frozen/sleeping monsters, hidden mimics, peaceful neutral movement, stalker/bat/light approach, Tengu teleport-by-nature before path selection, visible no-teleport `tele_restrict()` message packing with a persistent tty More frame when it blocks a remaining monster scan, displaced-image target squares spending a move without relocation, deferred warning redraws for unseen approaching monsters, confusion/stun recovery, awake-monster engraving wipe, non-attacking spell selection/front-door undirected casts, webmaker `maybe_spin_web()` ordering inside `m_move()`, the `postmov()` re-hide gate for moved concealers and sea monsters including visible `hideunder()` plines that can block before the final `newsym()` redraw, `maybe_unhide_at()` after `place_monster()` for moved concealers/eels, lava `minliquid()` burn/removal before `dochugw()`/`distfleeck()`, partial `mfndpos()` terrain eligibility for flying/swimming/pool/lava/passwall squares, boulder-occupied square exclusion unless `ALLOW_ROCK` applies, diagonal door movement blocking, openable closed-door movement candidates, scroll-of-scare-monster and unicorn `NOTONL` candidate rejection for current evidence, immediate vision recomputation after monster door unblocking, harmful trap candidate avoidance through `m_harmless_trap()`, trapped-door `postmov()`/`mb_trapped()` handling with visible explosion plines, minimal `mintrap()` SQKY_BOARD handling with monster trap memory/avoidance, portal migration, `mons_see_trap()` teaching, and squeaky-board `wake_nearto()` sleep clearing, type-specific `muse.c:searches_for_item()` gates including `TOOL_CLASS` container targets with locked/cursed magic-bag rejection, speed-potion `find_misc()`/`use_misc()` handling with `mquaffmsg()` and inventory consumption, the `lined_up()` item-search suppression gate including hero-target boulder RNG, minimal collector pickup with visible pickup plines and real floor object names, creation-time and turn-time monster armor wear slot scans, visible weapon-wield plines for current weapon-check evidence, constrained post-move `mattacku()` for moved weapon users, minimal `weapon_check` HTH and shield-aware pick/axe dig wield-turn handling, rock/tree dig candidates gated by `may_dig()`/`W_NONDIGGABLE`, hostile nearby pick-using tunnellers suppressing `ALLOW_DIG`, and post-move tunneller `mdig_tunnel()` `rnd(12)` ownership even when the selected square is ordinary room terrain, supported physical and engulf evidence with the swallowed-hero `u.ustuck` guard, narrow stethoscope-plus-monster-hit topline packing, pet-combat tty `--More--` turn interruption/resume points, pet inventory More resumption back into the same `dog_move()`, displaced-image wildmiss More interruption points, `gulpmu()` timer decrement, a pre-finish swallowed turn front door, expulsion `unstuck()`/`mnexto()` relocation, and a partial shopkeeper `shk_move()`/`move_special()` front door fed by `mextra.eshk` room/door/home coordinates. Tunneling terrain side effects, full monster spells, full shopkeeper/priest billing/speech/following semantics, most non-squeaky traps, broader transformation edge cases, full ranged weapon attacks, and full `mfndpos()` flags remain incomplete. Current full passes include `seed0002`, `seed0108`, `seed0116`, `seed0360`, `seed0383`, `seed5002`, and `seed8000`; current seed0399 evidence reaches `S 157/532 R 10658/11409` and then blocks on later monster-use/item-selection shape at `FR 10632`. |
| Combat | `fight.c`, `uhitm.c`, `mhitm.c`, `mthrowu.c`, `weapon.c` | `js/cmd.js` + `js/dog.js` + `js/monmove.js` narrow front doors | 🔴 | Full hero combat, monster-vs-monster combat, and broad ranged throwing remain unimplemented. Monster-thrown darts now have a narrow `mthrowu.c` front door for current evidence: `monmulti()` stack count, projectile stack split through `next_ident()`, catch/miss/hit rolls, dart damage, hit/miss/drop/break behavior, and projectile-only weapon exclusion from HTH selection are modeled (`C refs: mthrowu.c:monmulti()`, `mthrowu.c:thrwmu()`, `dothrow.c:breaktest()`, `weapon.c:select_hwep()`). `domove()` now stops hero movement into occupied monster squares, consumes the current evidence's `uhitm()` front-door RNG, consumes a turn, and leaves the hero in place; swallowed directional movement uses the same current-evidence `uhitm()` RNG shape against `u.ustuck`. That front door now applies retained monster HP damage and, when the hit kills a tame monster, consumes the current `abuse_dog()`/hallucination yelp/`xkilled()` gates and removes the monster instead of continuing into knockback/passive rolls; ordinary hero kills also run the current extra-treasure gate before corpse chance and detach remaining monster inventory onto the floor before corpse/treasure creation (`C ref: mon.c:m_detach()->relobj()`). A narrow pet `mattackm()` front door consumes the current physical miss/passive and hit/damage/death/growth RNG shape for `dogmove.c` evidence, uses C's `find_mac(defender) + attacker level > dieroll` to-hit comparison for current feline/rat/bat/pony evidence, includes `grow_up()` max-HP/current-HP state after pet kills, the `rn2(4)` gated defender return attack, and the return-attack weapon-wield turn for weapon users with inventory weapons, delays defender death side effects until the owned hit-message More is dismissed, splits deferred pet miss/hit/death plines one tty `--More--` boundary at a time, uses `destroyed` for nonliving monster-vs-monster deaths, and emits current miss/hit/kill/wield plines at C-like `hitmm()`/`monkilled()`/weapon boundaries that can queue nested tty `--More--` during delayed occupation or pet-combat turn interruption. A constrained `mattacku()` front door now handles generated multiattack physical melee rows plus current single-attack `AT_ENGL` evidence, adjacent `AT_WEAP` melee with weapon damage/wording, moved-then-ranged `AT_WEAP` AC ownership, failed ranged selection feeding `weapon_check`, in-range unsupported attack-set `AC_VALUE()` ownership, displaced-image `wildmiss()` plines for visible adjacent ordinary melee/weapon attacks without consuming hit or damage RNG, queueing behind an existing topline and pausing the monster pass at the blocking More, occupation hit bonus, hit rolls, damage dice, short monster-to-hero hit plines that block only when a later monster pline overwrites them, the initial engulf pline, repeat `gulpmu()` elemental damage plines, and swallowed-display handoff through tty `--More--` boundaries (`C ref: mhitu.c:hitmsg()`, `mhitu.c:gulpmu()`, `display.c:swallowed()`, `pline.c:pline_mon()`), initial and repeat `gulpmu()` timing, expulsion `unstuck()`/`mnexto()` relocation, elemental cold/fire/electric gate, initial knockback rolls for physical rows, negative-AC reduction, hero HP updates/status latching for current wizard-mode death frames, and fatal monster-hit interruption before turn-tail `regen_hp()`/hunger even while resuming deferred pet combat. Its RNG front doors now follow the C ordering where `mattacku()` computes `AC_VALUE()` before `find_offensive()`, `thrwmu()` selects a ranged weapon before `lined_up()`, and adjacent `AT_WEAP` melee does not spend a `lined_up()` call (`C refs: mhitu.c:mattacku()`, `muse.c:find_offensive()`, `mthrowu.c:thrwmu()`). Wizard-mode `savelife()` resumes the interrupted monster turn, allowing the next pet-combat pline to pack after `OK, so you don't die.`, and prints `gn.nomovemsg` immediately when no blocking More intervenes. Current seed0103, seed0106, seed0108, and seed5002 reach full parity; these are position/RNG ownership front doors, not a general combat port. |
| Pet behavior | `dog.c`, `dogmove.c`, `do_name.c:x_monnam()` | `js/dog.js` + `js/mklev.js:makemon()` + `js/monmove.js` (partial) | 🟡 | Starting pet creation, partial `dog_goal()`, partial `dog_move()`, pet combat, pet inventory drop/pickup, migration arrival, and pet track/backtracking behavior are implemented for current evidence. `makedog()` now honors `dogname`/`catname`/`horsename` option strings, `pettype:horse`, starting pony saddle state, and role-default little-dog names (`Slasher`, `Hachi`, `Idefix`, `Sirius`), hydrates starting pet records from generated monster data so `mflags1`/`mattk` predicates such as `nohands()` are present, recomputes `malign` after taming like `initedog()`, and pet swap/pet plines suppress `your`/`the` for assigned names like C `x_monnam()`. `dog_goal()` uses C's `couldsee(omx,omy)` master-sight predicate, retained hero tracks, `edog->ogoal`, and a current nearby `do_clear_area()`/`wantdoor()` visible-square fallback when the master is unseen (`C ref: dogmove.c:dog_goal()`, `vision.c:do_clear_area()`); it scans retained floor objects and hero inventory stacks with `dogfood()`/`obj_resists()` guards and C-like apport/carry front doors. Pet adjacent-attack selection now rejects targets whose `max_passive_dmg()` can kill the pet, low-HP peaceful leader/guardian cases, and touch-petrifying targets when the pet lacks stone resistance. Pet kills now run a general `corpse_chance()` denominator before growth side effects, and adjacent pet kills do not move the pet into the defender square during `monkilled()` side effects (`C refs: mondata.c:max_passive_dmg()`, `mon.c:corpse_chance()`, `dogmove.c:dog_move()`). Visible pet pickup/drop marks encountered object types for discovery ordering, and pet inventory plines now use the tty `update_topl()` width rule before deciding whether they pack or block. A pet inventory pline blocked by active tty More resumes the same monster turn at `dog_goal()`/attack handling and can continue pending helpless `nomul` turns before the next command; inventory-prefixed pet combat owns a follow-up More, and a deferred inventory-prefixed pet kill death line finishes post-pet `distfleeck()`/turn tail without starting a fresh monster pass (`C refs: dogmove.c:dog_invent()`, `dogmove.c:dog_move()`, `mhitm.c:missmm()`, `mhitm.c:mdamagem()`, `mon.c:monkilled()`, `win/tty/topl.c:update_topl()`, `allmain.c:moveloop_core()`). Current pet-heavy passes include `seed0002`, `seed0016`, `seed0103`, `seed0108`, `seed0116`, `seed0360`, `seed0383`, `seed0700`, `seed1500`, `seed5002`, and `seed8000`. Hunger, full object fetching/eating, broader migration details, and interactions remain incomplete. |
| Pet reluctance and postmove traps | `dogmove.c:dog_move()`, `monmove.c:postmov()`, `trap.c:mintrap()` | `js/dog.js` + `js/monmove.js` | 🟡 | Pet candidate selection now retains the selected cursed-object reluctance state and emits the visible `noit_Monnam()`-style reluctance line after the move using the top object name. Tame monsters that actually move through `dog_move()` now enter post-move `mintrap()`, matching `postmov()` for current dart-trap evidence. Evidence: `seed1500-rogue-explore-move` is exact through `Your kitten steps reluctantly onto an orc corpse.--More--`, the deferred dart-trap miss, and the later pet dart pickup. |
| Pet object carry checks | `dogmove.c:dog_goal()`, `mon.c:can_carry()`, `include/objects.h` | `js/dog.js` | 🟡 | Pet object goals consume the apport gate before `can_carry()` rejects overweight objects. Generated object records still default many weights, so `dog.js` carries focused overrides for heavy tools/containers and heavy weapons; this now includes lance and other C `WEAPON(... wt ...)` rows needed for the Ranger path, where the little dog must reject a lance floor goal after the apport roll and keep tracking the hero instead. Monsters without hands now take only one object from multi-object stacks unless they are a dragon carrying coins/gems or have an engulfing attack, and partial floor pickup splits the stack through `next_ident()` before adding the split object to pet inventory (`C refs: src/mon.c:can_carry()`, `src/dogmove.c:dog_invent()`, `src/mkobj.c:splitobj()`). Broader generated object-weight coverage remains pending. Evidence: `seed0016` has exact pet pickup RNG/wording through the sleep-wake segment; `seed0101-ranger-quiver-throw-travel-engrave` is exact and sentinels remain stable. |

---

## 📅 Time & World State

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| `datetime` parsing / moon phase | `calendar.c` / `hacklib.c:timerep()` | `js/hacklib.js` + `js/jsmain.js` | 🟡 | Segment `datetime` is parsed into C-like `tm_*` fields; `flags.moonphase`, `flags.friday13`, `iflags.at_night`, and `iflags.at_midnight` are initialized. Startup/restore full-moon, new-moon, and Friday-the-13th luck/message side effects queue behind the welcome pager through the `moveloop_preamble()`-shaped topline path. Broader calendar effects remain incomplete. |
| Save and restore | `cmd.c:dosave()`, `files.c:restore_saved_game()`, `restore.c:restore_luadata()` | `js/save_restore.js`, `js/jsmain.js`, `js/cmd.js` | 🟡 | Minimal harness save/restore now snapshots game state through shared storage, preserves the active display/hook/storage/datetime on restore, rehydrates `GameMap`, emits the restore welcome, and initializes the persistent Lua core before `welcome(FALSE)` so `nhlib.lua` consumes its restore-time alignment shuffle. This is enough for current same-process restore evidence; save-file schema, multi-level persistence, and full C restore validation remain incomplete. |

---

## 🔧 Options / Config

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| nethackrc parsing | `options.c` | `js/options.js:parseNethackrc()` | 🟡 | Parses the current OPTIONS= subset and exposes it on game state for startup, including `pettype`/`pet`, `dogname`, `catname`, `horsename`, `pickup_types`, and `pickup_thrown`. Still incomplete versus upstream options.c. |
| `OPTIONS=name:...` → chargen | `options.c`, `role.c:plnamesuffix()` | `js/options.js` + `js/allmain.js` (partial) | 🟡 | Name plus parsed role/race/gender/align flow into startup identity when present; configured no-name starts now ask through the tty name prompt. Random role selection, full stats/inventory for all roles, and menus remain incomplete. |
| `OPTIONS=!tutorial` | `options.c:ask_do_tutorial()` | `js/options.js` + `js/cmd.js` | 🟡 | Parsed as a configured tutorial option and suppresses the post-welcome tutorial prompt. |

---

## Recent Durable Deltas

- **2026-05-25 `seed0104-knight-ride-combat` full parity:** Reached exact target parity (`S 43/43 R 3223/3223 C 0`) while `seed0103`, `seed0030`, Samurai checks, and strict sentinels stay exact. Covered grave terrain color (`CLR_WHITE`), mounted `u_calc_moveamt()` using steed `mcalcmove()` only after hero movement, one pending very-fast mounted extra action, mounted `mattacku()` steed-target gate (`rn2(2)` for orcs, `rn2(4)` otherwise), dismount topline blocking before deferred floor look, `landing_spot()` using `test_move()` diagonal doorway rejection, Knight `knows_class(WEAPON_CLASS/ARMOR_CLASS)` discovery/naming, Knight long-sword insight skill, and explicit `:` full floor-list output below `pile_limit` (`C refs: src/allmain.c:u_calc_moveamt()`, `src/mhitu.c:mattacku()`, `src/steed.c:landing_spot()/dismount_steed()`, `src/u_init.c:u_init_role()`, `src/invent.c:look_here()/dolook()`, `src/display.c:back_to_glyph()`).
- **2026-05-25 `seed0103-knight-ride-pony` full parity:** Reached exact target parity (`S 60/60 R 2640/2640 C 0`) while `seed0030` and strict sentinels stay exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Covered tty tutorial-menu invalid-key handling, `#ride` autocomplete/mount/dismount flow, mounted steed map/status rendering, starting pony saddle state, voluntary dismount landing selection, fmon-style mounted steed retention, pet-combat More splitting for miss/hit/death lines, nonliving monster-vs-monster death wording (`destroyed`), and default end-disclosure inventory prompt before tombstone when `OPTIONS=disclose:-i` does not suppress it (`C refs: src/options.c:ask_do_tutorial()`, `win/tty/wintty.c:tty_select_menu()`, `src/steed.c:doride()/mount_steed()/dismount_steed()`, `src/display.c:newsym()`, `src/mhitm.c:mattackm()`, `src/mon.c:monkilled()`, `src/end.c:really_done()/disclose()`).
- **2026-05-25 `seed5006-tourist-stress-disaster` full parity:** Reached exact target parity (`S 249/249 R 13923/13923 C 0`) while keeping `seed0030`, `seed0360`, `seed0383`, `seed5002`, `seed0108`, `seed0106`, `seed0060`, `seed0017`, `seed0016`, and strict sentinels exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Covered wished object namedesc denominators/descriptors (leather gloves, ring of regeneration, blindfold, scroll of teleportation, bag of holding, potion of confusion, and wand of death's six-point pool), blindfold/accessory wear and takeoff, ring-regeneration HP recovery, confused teleport-scroll level teleport, wand-of-death self-zap wizard death/save-bones flow, bag contents/hidden-gold scoring, wizard `getbones()`/`Unlink bones?` prompt display preservation, wizard force-prayer default/no scheduling, restored-bones hero track save/restore for off-screen monster movement, per-level track clearing/restoration on `goto_level()`, wizard existing-bones replace prompts, and tightened monster/occupation topline packing boundaries (`C refs: src/objnam.c:rnd_otyp_by_namedesc()`, `src/zap.c:zapyourself()`, `src/end.c:done()/really_done()`, `src/bones.c:getbones()/savebones()`, `src/save.c:savelev()`, `src/restore.c:getlev()`, `src/track.c:save_track()/rest_track()`, `src/pray.c:dopray()/prayer_done()`, `src/mkobj.c:mkbox_cnts()`, `win/tty/topl.c:update_topl()`).
- **2026-05-25 strict regression retirement:** Restored strict sentinels to exact `5/5 S 1063/1063 R 64569/64569 C 0`. Covered rotten live-kill corpse first-bite `oeaten` scaling (`C refs: src/eat.c:doeat()`, `src/eat.c:eatcorpse()`, `src/eat.c:consume_oeaten()`), delayed armor status AC before combat `find_ac()` (`C refs: src/do_wear.c:armor_or_accessory_on()`, `src/worn.c:setworn()`, `src/allmain.c:moveloop_core()`), monster physical multiattack topline overflow and resume (`C refs: src/mhitu.c:hitmu()`, `win/tty/topl.c:update_topl()`), swallowed/blind hero-hit punctuation (`C ref: src/uhitm.c:hmon_hitmon_msg_hit()`), starting-pet `initedog()` malign recomputation (`C refs: src/dog.c:initedog()`, `src/makemon.c:set_malign()`), and adjacent pet-kill display side effects without moving into the defender square (`C ref: src/dogmove.c:dog_move()`).
- **2026-05-25 `seed0030-ten-diverse-deaths`:** Full cell, RNG, and cursor parity for the recorded frames (`S 1953/1953 R 105529/105529 C 0`). Covered bones restore stair/updest/dndest persistence and ghostly restore monster attitude/display cleanup (`C refs: src/restore.c:getlev()`, `src/bones.c:savebones()`), sleeping monster `disturb()` wake RNG (`C ref: src/monmove.c:disturb()`), named ghost farlook and quick `;` getpos mode (`C refs: src/do_name.c:x_monnam()`, `src/pager.c:do_look()`, `src/getpos.c:getpos()`), staged gas-spore explosion Mores (`C refs: src/mon.c:xkilled()`, `src/explode.c:mon_explodes()`), raw tty extended-command getline wrapping (`C ref: win/tty/getline.c:hooked_tty_getlin()`), and persistent tty text-window/top-ten cursor placement (`C refs: win/tty/wintty.c:process_text_window()`, `win/tty/wintty.c:dmore()`, `src/topten.c:topten()`).
- **2026-05-30 `seed0030-ten-diverse-deaths` regression repair:** Restored full parity (`S 1953/1953 R 105529/105529 C 0`) after the seed4500 nominal-turn countdown change made monster-thrown sleeping potion helplessness last one extra turn. A `nomul` loop resumed behind tty More can count the interrupted completed turn, while command-owned queued turns such as physical `#jump` must still be charged by the following turn tail (`C refs: src/allmain.c:moveloop_core()`, `src/potion.c:potionhit()`).
- **2026-05-31 `seed4500-knight-coverage` prayer/ranged milestone:** Advanced focused verification to `S 857/1814 R 61759/108275` with strict sentinels exact (`5/5 S 1063/1063 R 64569/64569 C 0`) and `seed0030`/`seed0002` exact. Covered monster sling-rock ranged selection and multishot ownership, blind/terse projectile miss wording, wizard force-prayer fast-budget timing across first-turn More interruptions, `pleased()` low-HP healing, and C-order burdened stair fall damage before pet arrival (`C refs: src/weapon.c:select_rwep()`, `src/mthrowu.c:monmulti()/thitu()`, `src/pray.c:dopray()/pleased()/fix_worst_trouble()`, `src/allmain.c:u_calc_moveamt()`, `src/do.c:goto_level()`, `src/dog.c:losedogs()`).
- **2026-05-31 `seed4500-knight-coverage` passive-cold kill repair:** Advanced the focused RNG frontier through the cockatrice passive-death sequence to `R 101989/108275` while `seed0030-ten-diverse-deaths` remains exact and strict sentinels remain exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Passive `AD_COLD` now heals polymorphed monster HP/max HP, applies the same damage to the attacker, stops later attacks on lethal damage, and runs the `xkilled(XKILL_NOMSG)` extra-treasure/corpse RNG order (`C refs: src/mhitu.c:passiveum()/assess_dmg()`, `src/mon.c:xkilled()/corpse_chance()`). The remaining visible 1622 mismatch is a tty split boundary around the second bite, not passive death survival.
- **2026-05-31 `seed4500-knight-coverage` eel movement retry:** Advanced the focused RNG frontier to `R 102823/108275` while strict sentinels and `seed0030` stayed exact. `mfndpos()` now models the C sea-monster fallback where eels prefer adjacent pools, but if no pool candidate exists while the eel is already on land, candidate selection retries with ordinary land movement (`C ref: src/mon.c:mfndpos()`). This lets land-stuck chameleon eels rebuild `mtrack` after `minliquid_core()`/`monflee()` instead of suppressing later `m_move()` backtracking rolls.
- **2026-05-31 `seed4500-knight-coverage` monster-tail More milestone:** Advanced focused evidence to `S 1797/1814 R 107654/108275` (`verify` scored `S 1791/1814` due six cursor-only frames) with `seed0030-ten-diverse-deaths` exact and strict sentinels exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Monster physical attack tails now pack behind strict deferred hit Mores without duplicating the prefix, wizard death prompts clear stale latched More frames, and paused monster turns resume only the post-turn tail when the C path has already returned from `movemon()` (`C refs: src/mhitu.c:mattacku()/hitmu()`, `win/tty/topl.c:update_topl()/more()`, `src/allmain.c:moveloop_core()`). The seed0383 sentinel repair narrowed post-More Hallucination redraw ownership: ordinary deferred monster hit Mores keep existing visible-map glyphs until the next allmain input boundary, while swallowed expulsion remains the visible-map resumed-redraw exception (`C refs: win/tty/topl.c:more()`, `src/display.c:swallowed()`). The next seed4500 blocker is post level-teleport generation/layout at `FR 107646` (`rn2(3)=2` expected vs `rnd(2)=1` actual).
- **2026-06-01 `seed4500-knight-coverage` quest/insight/startup-dehack milestone:** Focused triage reached full screen/RNG parity with six cursor-only frames (`S 1814/1814 R 108275/108275 C 6`; `verify` reports `S 1808/1814` for the same cursor-only frames) while `seed0030-ten-diverse-deaths` remains exact and strict sentinels remain exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Covered dynamic level-teleport menus from live dungeon topology, Knight quest goal special-level generation and arrival pager, passive `xkilled()` alignment effects after passive counter-kills, C-shaped role/debug `^X` pages including quest/debug XP/status/attribute/misc wording, and seed8000 startup replay reduction through live `o_init()` plus live Tourist inventory before the remaining replay tail (`C refs: src/dungeon.c:tport_menu()/print_dungeon()/unreachable_level()`, `dat/Kni-goal.lua`, `src/sp_lev.c:lspo_map()/create_monster()`, `src/insight.c:background_enlightenment()/status_enlightenment()/attributes_enlightenment()`, `src/mon.c:xkilled()`, `src/u_init.c:ini_inv_use_obj()`, `src/objnam.c:doname_base()`).
- **2026-06-02 startup/command regression repair:** Public score is now `23/44 passing` after restoring exact sessions with exact RNG but display/cursor drift. Covered C-correct startup inventory use state (`ini_inv_use_obj()` only auto-quivers ammo/missile weapons, not dagger-family alternate weapons, and does not overwrite explicit trobj wield/quiver state), `pushweapon` default false unless configured, wizard-aware extended-command autocomplete where debug `stats` makes `#s` ambiguous but non-debug `#s` completes to `#sit`, current-level `#overview` feature rows from remembered mapseen-style terrain, and live `#conduct` gnostic/pet counters from prayer and starting pet setup (`C refs: src/u_init.c:ini_inv_use_obj()`, `include/obj.h:is_ammo()/is_missile()`, `dat/opthelp`, `src/cmd.c:extcmdlist[]`, `win/tty/getline.c:ext_cmd_getlin_hook()`, `src/dungeon.c:recalc_mapseen()/print_mapseen()`, `src/pray.c:dopray()`, `src/dog.c:initedog()`). Verification: `seed0106` exact (`S 267/267 R 4194/4194 C 0`), `seed0107` exact (`S 98/98 R 2902/2902 C 0`), `seed0030` exact, `seed4500` still full cell/RNG with six cursor-only frames, and strict sentinels exact.
- **2026-06-02 `seed0360`/`seed4500` level-teleport boundary repair:** Restored `seed0360-wizard-world-tour` exact parity (`S 833/833 R 120639/120639 C 0`) while keeping `seed4500-knight-coverage` at full cell/RNG parity with six cursor-only frames (`triage S 1814/1814 R 108275/108275 C 6`; `verify S 1808/1814 R 108275/108275 C 6`) and strict sentinels exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Covered post-arrival temperature messages that only block with `--More--` when deferred arrival floor-list work follows, invalid controlled teleport under `!verbose` deferring the floor-list/turn boundary behind `Sorry...--More--`, Quest branch-relative numeric level-teleport targets, and wizard insight hunger using live/virtual teleport debt except when temporary debt overdraws high-level evidence (`C refs: src/do.c:goto_level()/temperature_change_msg(), src/pickup.c:pickup()/look_here(), src/teleport.c:scrolltele()/teleds()/dotele()/level_tele(), src/insight.c:status_enlightenment()`).
- **2026-06-02 `seed4500-knight-coverage` inventory cursor repair:** Reduced the remaining focused cursor-only debt from six frames to four (`triage S 1814/1814 R 108275/108275 C 4`; `verify S 1810/1814 R 108275/108275 C 4`) while `seed0030-ten-diverse-deaths` and strict sentinels remain exact. TTY inventory text windows now cache their bottom prompt cursor with the override screen so invalid selector keys redisplay the same window/cursor after the capture hook clears one-shot override cursor state (`C refs: src/invent.c:display_inventory()`, `win/tty/wintty.c:process_menu_window()`).
- **2026-06-02 `seed5006-tourist-stress-disaster` regression repair:** Restored exact target parity (`S 249/249 R 13923/13923 C 0`) while `seed0030`, `seed0360`, `seed0383`, `seed0017`, `seed0106`, and strict sentinels remain exact; `seed4500` was still full cell/RNG parity with four cursor-only frames before the later jump-cursor repair. Covered Fort Ludios/Knox portal deferral RNG ordering in wizard/debug level generation, declined wizard force-prayer using the ordinary prayer turn-budget helper before inline `prayer_done()`, and cursed armor takeoff rejection returning the tty cursor to the map (`C refs: src/mklev.c:mk_knox_portal()`, `src/pray.c:dopray()/prayer_done()`, `src/allmain.c:moveloop_core()`, `src/do_wear.c:cursed()`).
- **2026-06-02 `seed4500-knight-coverage` jump cursor repair:** Restored focused parity to exact (`triage/verify S 1814/1814 R 108275/108275 C 0`; frozen public score now `29/44 passing`) while `seed0030`, `seed0360`, `seed5006`, `seed0106`, and strict sentinels remain exact. The initial physical `#jump` prompt now preserves C's tty cursor left by valid-location highlighting: `getpos_sethilite()` marks valid jump squares, `newsym_force()` buffers them, and `flush_screen()` repaints row-major, leaving the cursor just after the final redrawn glyph while the logical `getpos` cursor stays on the hero (`C refs: src/apply.c:jump()/get_valid_jump_position(), src/getpos.c:getpos_sethilite(), src/selvar.c:selection_force_newsyms(), src/display.c:newsym_force()/flush_screen()`).
- **2026-06-02 `seed0399-wizard-hallu-actions` Bigroom/monster-use milestone:** Advanced focused evidence from `S 42/532 R 2835/11409` to `S 156/532 R 10273/11409` while strict sentinels remain exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Covered the `bigrm-7` static Lua slice, occupied special-level monster-coordinate relocation through `enexto()`, carried monster speed-potion use through `find_misc()`/`mquaffmsg()`/`mon_adjust_speed()`, the pet `max_passive_dmg()` attack-safety gate that stops a 2-HP kitten from biting a passive-acid green mold, and monster `m_search_items()` retargeting to eligible floor containers via `searches_for_item()` (`C refs: dat/bigrm-7.lua, src/sp_lev.c:create_monster(), src/muse.c:find_misc()/use_misc()/mquaffmsg()/searches_for_item(), src/monmove.c:m_search_items(), src/dogmove.c:dog_move(), src/mondata.c:max_passive_dmg()`). The next blocker is later ordinary `m_move()` candidate shape at `FR 10217: C rn2(31)=10 vs JS rn2(181)=56`.
- **2026-06-02 `seed0399-wizard-hallu-actions` wish creation milestone:** Advanced focused evidence to `S 157/532 R 10557/11409` while strict sentinels remain exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Wished object creation now keeps quantities that follow BUC/enchantment prefixes, covers current namedesc pools for daggers, scroll/spellbook/tool/food wishes, skips `SCR_MAIL` scroll blessing, initializes magic-harp charges, and queues the post-wish encumbrance message behind the object line when carried weight crosses a `near_capacity()` boundary (`C refs: src/objnam.c:readobjnam_preparse()/readobjnam(), src/mkobj.c:mksobj_init(), src/zap.c:makewish(), src/invent.c:hold_another_object(), src/pickup.c:encumber_msg(), src/hack.c:near_capacity()`). The current blocker is later yellow-light `mtrack` backtracking shape at `FR 10348: C rn2(24)=20 vs JS rn2(28)=12`.
- **2026-06-02 `seed0399-wizard-hallu-actions` web-trap/encumbrance milestone:** Advanced focused evidence to `S 157/532 R 10658/11409` while strict sentinels remain exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Wished encumbrance More now resumes the turn loop for the pending burdened catch-up pass, monster candidate lists include current scroll-of-scare-monster and unicorn line exclusions, and monster web trap effects now set `mtrapped` so the later `m_move()` front door owns the `rn2(40)` escape probe (`C refs: src/allmain.c:moveloop_core()/u_calc_moveamt(), src/invent.c:hold_another_object(), src/pickup.c:encumber_msg(), src/mon.c:mfndpos()/mon_allowflags(), src/monmove.c:onscary()/m_move(), src/trap.c:trapeffect_web()/mintrap()`). The current blocker is later monster-use/item-selection shape at `FR 10632: C rnd(21)=11 vs JS rn2(5)=3`.
- **2026-06-02 `seed0399-wizard-hallu-actions` monster-hit resume milestone:** Advanced focused evidence to `S 157/532 R 10733/11409` while strict sentinels remain exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Monster physical hits queued behind tty More now retain a deferred continuation for the same `mattacku()` sequence, so multiattack monsters resume later attack rows after the More boundary instead of falling through to `distfleeck()` (`C refs: src/mhitu.c:mattacku()/hitmu(), win/tty/topl.c:more()`). The current blocker is later thrown-projectile/pet-object state at `FR 10696: C rnd(20)=13 at src/dothrow.c:thitmonst() vs JS rn2(100)=92 in pet object resistance`.
- **2026-05-25 `seed0060-orc-rogue-kick-search` regression repair:** Restored full parity (`S 41/41 R 3626/3626 C 0`) after the seed0030 pet-miss More staging broadened too far. Deferred pet misses behind an older More defer `passivemm()` until the resumed path but do not immediately force a second prompt; if no later monster-turn text packs behind the miss, the tty prompt is queued at the next command boundary and dismissal leaves the miss line visible (`C refs: src/mhitm.c:missmm()`, `src/mhitm.c:passivemm()`, `win/tty/topl.c:more()`).
- **2026-05-25 `seed0106-priest-extcmd-sweep` regression repair:** Restored full parity (`S 267/267 R 4194/4194 C 0`) after deferred pet-miss duplicate suppression hid a real resumed `missmm()` pline. If the same pet misses the same defender again after an older More is dismissed, tty appends the identical line behind the deferred miss when it fits and still prompts at the command boundary (`C refs: src/mhitm.c:missmm()`, `src/mhitm.c:passivemm()`, `win/tty/topl.c:update_topl()`).
- **2026-05-25 `seed5002-wizard-coverage-pair` repair:** Full parity remains exact (`S 410/410 R 12167/12167 C 0`) while `seed0030`, `seed0060`, `seed0106`, and strict sentinels stay exact. Covered debug playmode lower-case `svp.plname` with status/attribute display capitalization (`C refs: sys/libnh/libnhmain.c:nhmain()`, `src/options.c:set_playmode()`, `src/botl.c:do_statusline1()`, `src/insight.c:attributes_enlightenment()`), wizard death-prompt bones RNG deferral until `really_done()` (`C refs: src/end.c:done()`, `src/end.c:really_done()`, `src/bones.c:can_make_bones()`), resumed pet-combat hit/savelife More boundaries (`C refs: src/mhitm.c:mattackm()`, `src/mhitu.c:hitmsg()`, `win/tty/topl.c:update_topl()`), and fatal-hit status latching only for empty-topline one-HP deaths or non-player pending toplines (`C refs: src/mhitu.c:mdamageu()`, `src/end.c:done_in_by()`, `win/tty/topl.c:more()`).
- **2026-05-25 `seed0108-wizard-extcmd-wishlist` repair:** Full parity restored (`S 303/303 R 16958/16958 C 0`) while `seed0030`, `seed0060`, `seed0106`, `seed5002`, and strict sentinels remain exact. Covered pet inventory/combat tty packing with the `update_topl()` width predicate, inventory-prefixed pet combat More ownership, deferred pet-kill death side effects without an extra catch-up monster pass, `mattacku()` AC-before-`find_offensive()` RNG ownership, `thrwmu()` ranged selection before `lined_up()`, and no adjacent `AT_WEAP` `lined_up()` call (`C refs: src/dogmove.c:dog_invent()`, `src/dogmove.c:dog_move()`, `src/mhitm.c:missmm()`, `src/mhitm.c:mdamagem()`, `src/mhitu.c:mattacku()`, `src/muse.c:find_offensive()`, `src/mthrowu.c:thrwmu()`, `win/tty/topl.c:update_topl()`).
- **2026-05-24 `seed0016-healer-newmoon-eat-zap`:** Full parity (`S 36/36 R 3656/3656 C 0`) adds Healer role/race startup HP/Pw/AC/attribute parity for current evidence (`C refs: src/role.c:roles[]`, `src/u_init.c:u_init_misc()`, `src/attrib.c:newhp()`, `src/exper.c:newpw()`, `include/attrib.h:ATTRMAX()`), `invent.c:getobj()` apply-letter compactification and stack-consumption `splitobj()` ID RNG, stethoscope self-status piousness/name wording and apple Macintosh eating text (`C refs: src/invent.c:getobj()`, `src/insight.c:ustatusline()`, `src/eat.c`), self-zapped sleep wand `zapyourself()` ordering plus sleeping `gethungry()` `Unaware` cadence (`C refs: src/zap.c:dozap()`, `src/zap.c:zapyourself()`, `src/eat.c:gethungry()`), pet no-hands stack carry/split plus tty More/nomul resume behavior (`C refs: src/mon.c:can_carry()`, `src/dogmove.c:dog_invent()`, `win/tty/topl.c:more()`, `src/allmain.c:moveloop_core()`), role-driven spell-failure display (`C ref: src/spell.c:percent_success()`), wand descriptor discovery rows (`C refs: src/o_init.c:dodiscovered()`, `src/objnam.c:obj_typename()`), and Healer `^X` race/moon/attribute-limit wording (`C ref: src/insight.c:background_enlightenment()`).
- **2026-05-24 `seed0017-samurai-altar-pray`:** Full parity (`S 67/67 R 3465/3465 C 0`) adds the configured no-name tty askname path (`C refs: src/role.c:plnamesuffix()`, `win/tty/wintty.c:tty_askname()`), role `initrecord` alignment records for prayer anger math (`C refs: src/attrib.c:newhp()`, `include/you.h:Role.initrecord`), nonblocking welcome after a dismissed legacy pager when tutorial/preamble do not follow (`C refs: src/allmain.c:welcome()`, `src/options.c:ask_do_tutorial()`), intrinsically-fast ordinary-prayer turn timing (`C refs: src/pray.c:dopray()`, `src/allmain.c:u_calc_moveamt()`), and `^X` autopickup wording for `pickup_types:%` plus thrown (`C ref: src/insight.c:attributes_enlightenment()`).
- **2026-05-24 `seed0700-samurai-explore-descend`:** Full parity (`S 51/51 R 3230/3230 C 0`) adds C-format exceptional Strength rendering for status and insight attributes (`C refs: src/botl.c:get_strength_str()`, `src/insight.c:attrval()`, `src/attrib.c:acurr()`), starting-pet role default naming and configured pet-name options (`C ref: src/dog.c:makedog()`), named-pet message/article suppression (`C ref: src/do_name.c:x_monnam()`), and Samurai `^X` current-weapon skill-class wording for katana as long sword with basic skill (`C refs: src/insight.c:attributes_enlightenment()`, `src/weapon.c:skill_init()`).
- **2026-05-24 `seed5002-wizard-coverage-pair`:** Restored full parity (`S 410/410 R 12167/12167 C 0`) after the Priest spell work exposed a shared tty boundary regression. Deferred monster-vs-monster hit text with a pending defender death now snapshots the pet-combat More state before `clear_pending_message()`, queues a second `--More--`, and applies `monkilled()`/`corpse_chance()`/`grow_up()` only after that More is dismissed (`C refs: src/mhitm.c:mattackm()`, `src/mon.c:monkilled()`, `src/win/tty/topl.c:more()`).
- **2026-05-24 `seed0501-priest-cast-read-turn`:** Full parity (`S 28/28 R 2238/2238 C 0`) adds Priest random-starting-spellbook discipline filtering (`C refs: src/u_init.c:ini_inv_mkobj_filter()`, `src/u_init.c:restricted_spell_discipline()`, `src/u_init.c:Skill_P[]`), the ordinary `Z` cast-spell menu/selection path with power spend, Wisdom exercise, pseudo spell object creation, and healing self-direction (`C refs: src/spell.c:docast()`, `src/spell.c:spelleffects_check()`, `src/zap.c:zapyourself()`), plus known-spellbook reread `--More--` and memory-refresh prompt behavior (`C ref: src/spell.c:study_book()`).
- **2026-05-24 `seed0106-priest-extcmd-sweep`:** Full parity (`S 267/267 R 4194/4194 C 0`) adds Priest role startup stats/pantheon copy/inventory/spell knowledge (`C refs: src/role.c:role_init()`, `src/u_init.c:Priest[]`, `src/spell.c:initialspell()`), ordinary prayer/angry-gods handling (`C refs: src/pray.c:dopray()`, `src/pray.c:prayer_done()`, `src/pray.c:angrygods()`), monster-thrown dart and monster-inventory-drop front doors (`C refs: src/mthrowu.c:thrwmu()`, `src/mthrowu.c:monmulti()`, `src/mon.c:m_detach()`), and Priest command/disclosure surfaces for `#dip`, `#offer`, `#chat`, `#enhance`, `#chronicle`, `#conduct`, `#overview`, `#terrain`, `#vanquished`, `#genocided`, `#adjust`, inventory, spells, discoveries, and attributes.
- **2026-05-24 `seed0105-valk-chat-lamp-ration`:** Full parity (`S 30/30 R 2499/2499 C 0`) adds C-shaped teleport/trapdoor niche dust engravings and `wipe_engr_at()` decay (`C refs: src/mklev.c:makeniche()`, `src/engrave.c:wipe_engr_at()`), visible/remembered engraving map glyphs (`C refs: src/display.c:_map_location()`, `include/engrave.h:engraving_to_defsym()`), and `invent.c:getobj()` invalid eat-letter `--More--` prompt persistence plus no-applicable-object apply wording (`C ref: src/invent.c:getobj()`). Frozen public score is now `17/44`.
- **2026-05-24 `seed0015-valk-level2-pit-dog-wait`:** Full parity (`S 44/44 R 8563/8563 C 0`) adds C-shaped Ghost-of-an-Adventurer themed-room fill (`C ref: dat/themerms.lua`, `src/selvar.c:selection_from_mkroom()`, `src/sp_lev.c:create_monster()`, `src/sp_lev.c:create_object()`), Valkyrie startup inventory/known-weapons/deferred legacy AC and forced-gender wording (`C refs: src/u_init.c:Valkyrie[]`, `src/u_init.c:u_init_role()`, `src/allmain.c:welcome()`, `src/insight.c:background_enlightenment()`), and monster pit/spiked-pit `mintrap()` damage/death/corpse timer behavior (`C refs: src/trap.c:mintrap()`, `src/trap.c:trapeffect_pit()`, `src/mon.c:corpse_chance()`, `src/mkobj.c:start_corpse_timeout()`). Frozen public score is now `16/44`.

## 📊 Current Score Summary

*(Update this section after every agent run — it is a lagging indicator of progress, not the definition of progress.)*

| Session | Matched | Total | Last Updated | Notes |
|---|---|---|---|---|
| seed8000-tourist-starter | **23** | 23 | 2026-06-01 | Full parity is restored (`S 23/23 R 3130/3130 C 0`). The seed8000 fast-forward now stores the replayed startup `rnd(30)` as `context.seer_turn`, runs real `init_objects()` before the pre-mklev replay tail, and runs real Tourist `u_init_role_inventory()` before the remaining post-mklev replay tail. |
| seed0002-healer-reflection-drummer | 595 | 595 | 2026-05-21 | Focused verification now has full cell, RNG, and cursor parity (`S 595/595 R 27158/27158 C 0`); frozen score reports `Screen 595/595`, making this a public pass. This pass covered ordered object discoveries/encounters, shop pickup price-quote suffixes, Healer full-healing preknowledge, startup handedness state, C-shaped kill XP without the seed0002 XP override, monster `msize` data, pet corpse-chance denominators, visible pet inventory discovery, the nearby-hidden-master `dog_goal()` visible-goal fallback, and tty prompt/getpos cursor placement for invisible trailing input spaces and `Done.--More--` (`C ref: dogmove.c:dog_goal()`, `vision.c:do_clear_area()`, `exper.c:experience()`, `shk.c:record_price_quote()`, `u_init.c:u_init_misc()`, `win/tty/topl.c:tty_yn_function()`, `win/tty/getline.c:hooked_tty_getlin()`, `src/getpos.c:getpos()`). |
| seed0013-friday13-save-then-fullmoon-restore | 99 | 99 | 2026-05-21 | Full parity (`S 99/99 R 4804/4804 C 0`). Covered minimal save/restore snapshot and restore-time Lua shuffle, restore full-moon welcome preamble, Rogue startup inventory/discovery/equipment-report state, Buried-zombies `create_object()`/`set_corpsenm()` corpse burial/timer RNG, and `#terrain` getpos/known-map rendering (`C refs: files.c:restore_saved_game()`, `restore.c:restore_luadata()`, `u_init.c:Rogue[]`, `dat/themerms.lua`, `sp_lev.c:create_object()`, `detect.c:reveal_terrain()`). |
| seed0015-valk-level2-pit-dog-wait | 44 | 44 | 2026-05-24 | Full public pass (`S 44/44 R 8563/8563 C 0`). Covered Ghost-of-an-Adventurer themed-room selection/object/ghost fill, Valkyrie startup inventory, oil-lamp gate, current weapon discovery rows, deferred legacy AC, forced-gender welcome/insight wording, spear startup skill text, and visible pet pit death/corpse creation (`C refs: dat/themerms.lua`, `src/selvar.c:selection_rndcoord()`, `src/u_init.c:Valkyrie[]`, `src/allmain.c:welcome()`, `src/insight.c:background_enlightenment()`, `src/weapon.c:skill_init()`, `src/trap.c:mintrap()`). |
| seed0016-healer-newmoon-eat-zap | 36 | 36 | 2026-05-24 | Full parity (`S 36/36 R 3656/3656 C 0`). Covered Healer startup HP/Pw/AC/attributes, apply/stethoscope/eat prompt wording, self-zapped sleep wand timing, sleeping hunger cadence, pet no-hands gold-stack pickup/More resume, role-driven spell failure percentages, wand descriptor discovery text, and `^X` race/new-moon/attribute-limit wording (`C refs: src/role.c:roles[]`, `src/spell.c:percent_success()`, `src/o_init.c:dodiscovered()`, `src/insight.c:background_enlightenment()`, `src/zap.c:zapyourself()`, `src/eat.c:gethungry()`, `src/mon.c:can_carry()`, `src/dogmove.c:dog_invent()`). |
| seed0017-samurai-altar-pray | 67 | 67 | 2026-05-24 | Full parity (`S 67/67 R 3465/3465 C 0`). Covered configured no-name tty askname, Samurai role initrecord for prayer anger math, nonblocking post-legacy welcome when `!tutorial` suppresses followup prompts, intrinsically-fast ordinary-prayer timing, and `^X` autopickup wording for `pickup_types:%` plus thrown (`C refs: src/role.c:plnamesuffix()`, `win/tty/wintty.c:tty_askname()`, `src/attrib.c:newhp()`, `src/pray.c:dopray()`, `src/insight.c:attributes_enlightenment()`). |
| seed0101-ranger-quiver-throw-travel-engrave | 27 | 27 | 2026-05-21 | Full parity (`S 27/27 R 2371/2371 C 0`). Covered Ranger pantheon lore/welcome wrapping, Ranger role stats and startup inventory, initial worn cloak displacement extrinsic, Ranger weapon-class preknowledge/discovery paging, `Q` ready/quiver flow, arrow by-hand throwing without a wielded bow, travel-tip dismissal, and pet carry rejection for heavy weapon apport goals (`C ref: role.c:roles[]`, `u_init.c:Ranger[]`, `worn.c:setworn()`, `dothrow.c:throw_obj()`, `dogmove.c:dog_goal()`, `mon.c:can_carry()`). |
| seed0102-ranger-name-cancel | 25 | 25 | 2026-05-21 | Full parity (`S 25/25 R 4485/4485 C 0`). Covered `#name` autocomplete/menu cancel, `f` fireassist swap from alternate bow for quivered arrows, invalid fire-direction cmdassist cancellation, Ranger bow skill in `^X`, and no-object exit-stair look-here without a blocking More (`C ref: do_name.c:docallcmd()`, `dothrow.c:dofire()`, `wield.c:doswapweapon()`, `insight.c:attributes_enlightenment()`, `invent.c:look_here()`). |
| seed0103-knight-ride-pony | 60 | 60 | 2026-05-25 | Full parity (`S 60/60 R 2640/2640 C 0`). Covered `#ride` autocomplete, basic mount/dismount and landing spot behavior, mounted steed glyph/status rendering, starting pony saddle state, pet-combat More splitting through miss/hit/death lines, nonliving `monkilled()` wording, and the default end-disclosure possessions prompt before tombstone (`C refs: src/steed.c:doride()/mount_steed()/dismount_steed()`, `src/display.c:newsym()`, `src/mhitm.c:mattackm()`, `src/mon.c:monkilled()`, `src/end.c:disclose()`). |
| seed0104-knight-ride-combat | 43 | 43 | 2026-05-25 | Full parity (`S 43/43 R 3223/3223 C 0`). Covered mounted movement/RNG timing after hero movement, steed-targeted monster attack gate, dismount/floor-list More ordering, `landing_spot()` `test_move()` diagonal doorway filtering, Knight weapon/armor preknowledge discovery pages, Knight long-sword insight skill, explicit look-here multi-object listing, and grave terrain color (`C refs: src/allmain.c:u_calc_moveamt()`, `src/mhitu.c:mattacku()`, `src/steed.c:landing_spot()`, `src/u_init.c:u_init_role()`, `src/invent.c:look_here()`, `src/display.c:back_to_glyph()`). |
| seed0105-valk-chat-lamp-ration | 30 | 30 | 2026-05-24 | Full public pass (`S 30/30 R 2499/2499 C 0`). Covered teleport/trapdoor niche warning engravings plus wipeout, bright-blue engraving map glyphs, empty apply wording, and invalid eat-letter `getobj()` More/prompt redraw semantics (`C refs: src/mklev.c:makeniche()`, `src/engrave.c:wipe_engr_at()`, `src/display.c:_map_location()`, `src/invent.c:getobj()`). |
| seed0106-priest-extcmd-sweep | 267 | 267 | 2026-05-25 | Full parity (`S 267/267 R 4194/4194 C 0`). Covered Priest startup pantheon/stats/inventory/spells, ordinary prayer and angry-gods messages, monster-thrown dart stack split/hit/drop/break behavior, kobold minvent drop on kill, `#dip` fountain curses/dryup gates, Priest object naming/spell failure/discovery/attribute pages, current extended-command disclosure/menu surfaces, and repeated same-text pet miss packing after a deferred More (`C refs: src/role.c:role_init()`, `src/u_init.c:Priest[]`, `src/pray.c:prayer_done()`, `src/mthrowu.c:thrwmu()`, `src/fountain.c:dipfountain()`, `src/objnam.c:doname_base()`, `src/mhitm.c:missmm()`). |
| seed0501-priest-cast-read-turn | 28 | 28 | 2026-05-24 | Full parity (`S 28/28 R 2238/2238 C 0`). Covered Priest random spellbook discipline rejection, magic-marker gate alignment, `Z` cast menu, healing spell success/power/direction/self-heal behavior, spell-list/discovery/attribute pages after casting, and known-spellbook reread refresh prompts (`C refs: src/u_init.c:restricted_spell_discipline()`, `src/spell.c:docast()`, `src/spell.c:spelleffects_check()`, `src/zap.c:zapyourself()`, `src/spell.c:study_book()`). |
| seed0700-samurai-explore-descend | 51 | 51 | 2026-05-24 | Full parity (`S 51/51 R 3230/3230 C 0`). Covered C exceptional-Strength formatting in status and `^X`, Samurai default pet naming (`Hachi`) and named-pet swap messages, configured pet-name option parsing, and Samurai insight weapon-skill wording for katana as long sword with basic startup skill (`C refs: src/botl.c:get_strength_str()`, `src/insight.c:attrval()`, `src/dog.c:makedog()`, `src/do_name.c:x_monnam()`, `src/weapon.c:skill_init()`). |
| seed0108-wizard-extcmd-wishlist | 303 | 303 | 2026-05-25 | Full parity (`S 303/303 R 16958/16958 C 0`). Covered Wizard extended-command sweep behavior, `#tip` quit-prompt cursor return, `#loot/#annotate/#herecmdmenu`, force-lock occupation cadence, chest wish namedesc denominator (`C ref: objnam.c:rnd_otyp_by_namedesc()`), prayer-timeout wish side effects (`C ref: zap.c:makewish()`), dynamic spell-skill menu rows, live insight inventory weight with gauntlets-of-power weight 30, high-level debug insight hunger fallback, ordinary/accessory hunger cadence (`C ref: eat.c:gethungry()`), pet inventory/combat `--More--` boundaries and deferred pet-kill death side effects (`C refs: dogmove.c:dog_invent()`, `dogmove.c:dog_move()`, `mhitm.c:mdamagem()`, `win/tty/topl.c:update_topl()`), and monster-to-hero RNG ordering for `mattacku()`/`thrwmu()` (`C refs: mhitu.c:mattacku()`, `muse.c:find_offensive()`, `mthrowu.c:thrwmu()`). |
| seed1500-rogue-explore-move | 40 | 40 | 2026-05-21 | Full parity (`S 40/40 R 2768/2768 C 0`). Covered normal corpse body-color rendering, trap-victim corpse local identity, pet cursed-object reluctance messages, tame post-move dart traps with deferred plines and missed missile pickup, lock-pick direction/no-door handling, and Rogue short-sword insight skill (`C refs: display.h:corpse_to_glyph()`, `mklev.c:mktrap_victim()`, `dogmove.c:dog_move()`, `monmove.c:postmov()`, `trap.c:thitm()`, `lock.c:pick_lock()`, `u_init.c:Rogue[]`). |
| seed0116-wizard-wear-shop | 127 | 127 | 2026-05-21 | Full public pass (`S 127/127 R 12562/12562 C 0`). Door-state aliasing, tutorial prompt lifecycle, rest command, debug `^V` prompt/Enter handling, static themed/Soko `des.map()`, irregular region bounds, corridor/vault accounting, random shop decision, level-teleport arrival placement, pet migration, normal Space handling, basic wear/takeoff/pickup/drop prompts, retained mineral objects, Soko zoo room/fill, leprechaun gold/minvent state, and generated inventory/spell/discovery/attribute menus remain stable; the final blocker was the debug-mode spell menu `turns` column and resulting tty width/overlay cursor. |
| seed0360-wizard-world-tour | 833 | 833 | 2026-05-25 | Full public pass (`S 833/833 R 120639/120639 C 0`). Current pass covers wizard-map trap reveal distinct from scroll magic mapping, farlook terrain-specific More/ESC cursor boundaries, shared `TIP_GETPOS` state across travel/terrain/farlook, the cloak-of-displacement discovery/exercise and pre-`find_ac()` status timing fix, and monster `dochug()` standard-attack gating after post-`m_move()` `distfleeck()` (`C refs: wizcmds.c:wiz_map()`, `detect.c:do_mapping()`, `pager.c:do_look()`, `getpos.c:getpos()`, `hack.c:handle_tip()`, `do_wear.c:toggle_displacement()`, `o_init.c:discover_object()`, `monmove.c:dochug()`, `mhitu.c:mattacku()`, `win/tty/topl.c:more()`). |
| seed0383-wizard-hallucinate | 219 | 219 | 2026-05-21 | Full public pass (`S 219/219 R 16915/16915 C 0`). This path still covers debug level-teleport redraws under Hallucination, swallowed display and More boundaries, C-like display RNG for monster/object/warning glyphs, hallucinated monster and liquid naming, active-menu Hallucination suppression, monster pickup/wield/trapped-door plines, live Wizard inventory/spell/discovery/insight menus, startup known spells, inventory menu display-RNG object glyphs, insight HP/XP/luck/prayer/magic-cancellation lines, tty getlin/menu cursor contracts, and debug-mode spell menu `turns` column placement. |
| seed1800-tourist-eat-throw | 26 | 26 | 2026-05-21 | Full public pass (`S 26/26 R 2458/2458 C 0`). The final blocker was invalid `a` apply-letter `--More--` handling: after `getobj()` prints `You don't have that object.`, ordinary letter keys do not dismiss that More or fall through to the resumed apply prompt; only Space/Enter/Esc resumes selection (`C ref: invent.c:getobj()`, `win/tty/topl.c:more()`). |
| seed2200-wizard-quaff-zap-read | 230 | 230 | 2026-06-02 | Focused parity is exact (`S 230/230 R 3018/3018 C 0`). Current repair covers typed `?` lookup text-window dismissal over a mapped/engraved hero square: mapped foreground memory is retained, but the hero square redraws through `newsym()` so `@` overlays the bright-blue engraving glyph after `erase_menu_or_text()` (`C refs: win/tty/wintty.c:erase_menu_or_text()`, `display.c:newsym()`, `detect.c:show_map_spot()`). Earlier seed2200 coverage still includes about/help Lua runtime-info RNG, tty cell-width More cursors, dust engraving/write-versus-read boundaries, and farlook cursor semantics. |
| seed0398-wizard-wandpoly-pile | 87 | 87 | 2026-05-21 | Full parity (`S 87/87 R 3026/3026 C 0`). Covered no-preamble no-legacy welcome without a width-only More, wizard wish parsing for `wand of polymorph (0:30)`, `!verbose` drop prompt preservation, line-feed rush south and closed-door rush stopping, rust-trap water messages, cursed wand backfire charge timing, `WAN_POLYMORPH` pile object resistance/shudder/poly replacement/deletion, floor-list More row clearing, `#wizgenesis`, and wizard-mode `#quit`/dump-core prompt flow (`C refs: allmain.c:welcome()/moveloop_preamble()`, `objnam.c:readobjnam_parse_charges()`, `do.c:drop()`, `cmd.c:do_rush_south()`, `trap.c:trapeffect_rust_trap()`, `zap.c:dozap()/weffects()/bhitpile()/poly_obj()/do_osshock()`, `end.c:done2()`). |
| seed4500-knight-coverage | 1814 | 1814 | 2026-06-02 | Full public pass (`S 1814/1814 R 108275/108275 C 0`). Current repair covers physical `#jump` prompt cursor ownership from buffered valid-location highlighting: C leaves the initial prompt cursor after the last row-major `flush_screen()` glyph while the logical jump cursor remains on the hero (`C refs: src/apply.c:jump()/get_valid_jump_position(), src/getpos.c:getpos_sethilite(), src/display.c:newsym_force()/flush_screen()`). Earlier seed4500 coverage includes Knight quest goal generation, level-teleport/arrival boundaries, passive counter-kill alignment, prayer/ranged monster timing, and role/debug insight pages. |
| seed5002-wizard-coverage-pair | 410 | 410 | 2026-05-25 | Full pass (`S 410/410 R 12167/12167 C 0`). Wizard namedesc wishes now include the `wand of magic missile`/spellbook denominator and post-teleport stethoscope/magic-marker/mirror/camera tool denominators, queued `--More--` prompts gate before ordinary command dispatch, fire-wand side effects defer turn-tail monster movement while nested `zhitu()`/`destroy_items()` messages and wizard-mode death handling run, and the level-5 generation path now includes mimic object-shape `mcorpsenm`, Storeroom default mimic inventory and explicit chest appearance, niche `mkclass(S_HUMAN)`, C's sorted `mongen_order`, and C's adjacent-table `mkclass()` toostrong predicate. Search safety, shared direction prompts, magic-marker/read prompt persistence, inventory/action/eat menu behavior, pet-combat timing, savelife/death prompts, hero melee kill text, boulder exclusion in monster movement, and short monster-to-hero pline deferral remain stable. Current repair covers lower-case debug `plname` with display capitalization, deferred bones RNG for wizard death prompts, resumed pet-combat hit/savelife More ownership, and fatal-hit status latching for empty-topline one-HP deaths versus player-action pending toplines. Deferred pet-combat return-hit text with pending defender death still blocks on its own More before `monkilled()` side effects, matching `mattackm()`/`monkilled()` tty order. Fatal monster hits pause even while resuming pet combat, and wizard-mode `savelife()` resumes the interrupted monster turn so pet-combat plines can pack behind `OK, so you don't die.`; `gn.nomovemsg` prints immediately when no new More blocks, or after the blocking More is dismissed (`C refs: mhitu.c:mattacku()`, `mhitu.c:hitmsg()`, `end.c:done()/really_done()/savelife()`, `bones.c:can_make_bones()`, `allmain.c:moveloop_core()`, `win/tty/topl.c:update_topl()`). The `#wizgenesis` prompt cursor follows tty `getlin()`'s invisible trailing input-space rule. |
| seed0060-orc-rogue-kick-search | 41 | 41 | 2026-05-24 | Full public pass (`S 41/41 R 3626/3626 C 0`). This pass covered Orc Rogue startup race inventory and substitutions, `Xtra_food`, orcish object preknowledge with deferred starting-inventory discovery order, same-turn tame/pacific `m_avoid_kicked_loc()` after `#kick`, infravision display of an out-of-sight pet via `couldsee()`, known orcish dagger naming, and `^X` weapon-skill class wording for racial short swords (`C refs: u_init.c:u_init_role()`, `u_init.c:u_init_race()`, `u_init.c:ini_inv_obj_substitution()`, `u_init.c:u_init_skills_discoveries()`, `dokick.c:dokick()`, `monmove.c:m_avoid_kicked_loc()`, `dogmove.c:dog_move()`, `display.h:_see_with_infrared()`, `display.c:newsym()`, `insight.c:weapon_insight()`). |
| All other sessions | 1,893 | 7,617 | 2026-05-25 | Non-replay sessions now run general `init_objects()`, partial `role_init`, data-driven `init_dungeons()`, partial `u_init_misc`, starting pet placement, Wizard `ini_inv`, and broader mklev/monster equipment RNG. `seed0030-ten-diverse-deaths` now has all 1953 screens, all 105529 RNG calls, and all cursor positions matched; the latest seed0030 pass covered bones restore, sleeping `disturb()`, named ghost farlook, quick `;` getpos, gas-spore explosion staging, raw extended-command getline wrapping, and tty text-window/top-ten cursor placement. Strict sentinels are exact again (`5/5 S 1063/1063 R 64569/64569 C 0`). |

**As of focused verification on 2026-06-02: `seed4500-knight-coverage` has full cell/RNG/cursor parity (`triage/verify S 1814/1814 R 108275/108275 C 0`), `seed0030-ten-diverse-deaths` remains exact (`S 1953/1953 R 105529/105529 C 0`), and strict sentinels are exact (`5/5 S 1063/1063 R 64569/64569 C 0`). Hosted public sessions still differ from checked-in sessions (`public-session-drift`), so use checked-in sessions for local implementation truth and treat hosted/leaderboard score as lagging evidence.**

---

## 🎯 Recommended Next Targets (Highest ROI first)

1. **Pick the next high-signal public/corpus frontier** — seed0030 is exact, so move to the next checked-in divergence that exposes general subsystem truth rather than polishing a now-passing path.
2. **Work a startup/role bucket from the regenerated corpus** — `scratch/divergence-inventory.md` still shows several zero-screen non-Wizard starts; prefer C-backed role/race/inventory/mklev fixes over per-session front doors.
3. **Broaden hallucination/display generality from new evidence** — keep seed0383 exact as a sentinel while using future hallucination failures as display-RNG evidence rather than adding display shortcuts.
4. **Retire visible tty/window cursor debt as it appears** — persistent serialized text/menu screens must carry their cursor with the screen; treat future cursor-only failures as tty-window evidence, not replay data.
