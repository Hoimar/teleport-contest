# Feature Map — NetHack JS Port

Cross-reference of every major subsystem between the C reference and the JS port.
Use this to choose your next implementation target. Update the **Status** and **Notes** columns after each session.

Context note: this file is an index. Prefer `rg` by session id, subsystem, JS module, C source, or FR number, then read only the relevant row and nearby section. Avoid full-reading it during tight implementation loops unless refreshing global priorities.

**Status legend:**
- 🔴 Not started / fully stubbed
- 🟡 Partially implemented / hardcoded for specific seeds
- 🟢 Correct for all tested seeds
- ✅ Verified passing (screen-level parity confirmed)

**Metric column:** "gates Y screens" means getting this right is expected to unlock Y screens across the public corpus.
Treat screen totals as lagging evidence of subsystem progress, not as the optimization target.

---

## 🏗️ Harness & Infrastructure

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| `runSegment()` API contract | `nethack-c/upstream/unixmain.c` | `js/jsmain.js` `runSegment()` | ✅ | Entry point, contract correct. Segment `datetime` now reaches game state. Do not break. |
| Screen capture hook (pre-nhgetch) | `sys/tty/tty_getch()` | `js/jsmain.js` `_installCaptureHook()` | ✅ | Fires before every `nhgetch()` call. Captures exactly one screen and one cursor per input boundary via `terminal.serialize()`. Do not push raw override strings or import scorer-side screen decoders from production `js/`. |
| PRNG log accumulation | `rng.c` | `js/rng.js` + `frozen/isaac64.js` | ✅ | Frozen ISAAC64 engine. Log format must be `rn2(N)=M` etc. |
| Screen comparison / canonicalization | `frozen/ps_test_runner.mjs` | (frozen — do not modify) | ✅ | DEC→Unicode, SGR normalise, version banner sentinel. |
| Per-session regression runner | `frozen/ps_test_runner.mjs` | (frozen) | ✅ | `node frozen/ps_test_runner.mjs [session...]` |
| Full regression suite | `frozen/score.sh` | (frozen) | ✅ | Run before committing broad changes and before handoff. Regressions must be classified; accidental regressions should be fixed, while expected regressions from hack removal or upstream-shaped refactors should be documented. |
| Live public regression suite | hosted `/sessions/` | `scripts/fetch-live-public-sessions.mjs` | ✅ | `npm run score:live-public` fetches the current public session files into `.cache/live-sessions` before running the frozen scorer. Use this when comparing against leaderboard public totals. |
| Debug tooling convention | — | Reusable diagnostics in `scripts/`; generated/checkpoint notes in `scratch/` | 🟡 | Temp debug scripts MUST be deleted after use. Removed stale tracked one-off probes `scratch/check_features.mjs`, `scratch/check_mklev.mjs`, and `scratch/inspect_screens.mjs`; remaining tracked scratch files are checkpoint/generated triage outputs. `scripts/triage-corpus.mjs` regenerates `scratch/divergence-inventory.md` from compact triage data for subsystem-level planning. `scripts/trace-dog-goal.mjs` runs a session prefix and prints the tame pet, C-shaped dog-goal search rectangle, floor objects, inventory, and expected-vs-actual RNG window for pet/object parity work. Never leave `console.log` or `import('fs').writeFileSync` in production paths (jsmain.js, allmain.js). |

---

## 🎲 PRNG / RNG

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Core PRNG engine (ISAAC64) | `rng.c` / `frozen/isaac64.js` | `frozen/isaac64.js` | ✅ | Frozen. Bit-exact with C. |
| `rn2(N)` | `rng.c:rn2()` | `js/rng.js:rn2()` | ✅ | |
| `rnd(N)` | `rng.c:rnd()` | `js/rng.js:rnd()` | ✅ | |
| `rn1(N,B)` | `rng.c:rn1()` | `js/rng.js:rn1()` | ✅ | |
| `rnl`, `rne`, `rnz`, `d` | `rng.c` | `js/rng.js` | 🟡 | Stubs or partial — verify log format matches C exactly |
| Three PRNG contexts (core / Lua / display) | `rng.c` context switching | `js/rng.js` | 🔴 | Hallucination/display context not implemented. Blocks `seed0383-wizard-hallucinate`. |

---

## 🗺️ Level Generation (mklev)

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Room generation (random room count/size/pos) | `mkmaze.c`, `mkroom.c` | `js/mklev.js` | 🟡 | Produces correct room count for seed8000/seed0002. Verify other seeds. |
| Corridor generation | `mkmaze.c:makecorridors()` | `js/mklev.js` | 🟡 | Connected but exact corridor paths may differ from C for non-tested seeds. |
| Door placement | `mkmaze.c:makedoor()` | `js/mklev.js` | 🟡 | `dosdoor()` now keeps JS `flags` and `doormask` synchronized to mirror C's alias, fixing closed-door rendering/movement state exposed by `seed0116`. |
| Stair generation (down stairs) | `mklev.c:generate_stairs()` | `js/mklev.js:generate_stairs()` | 🟡 | Down stairs generated. |
| Stair generation (up stairs, dlvl≥2) | `mklev.c:generate_stairs()` | `js/mklev.js:generate_stairs()` | 🟡 | Correctly skipped on dlvl 1. |
| Branch entrance placement (Mines etc.) | `mklev.c:place_lregion()` | `js/mklev.js:place_lregion()` | 🟡 | `end1_up: true` hardcoded for seed8000. Verify with other seeds. |
| Wallification (corner/t-junction glyphs) | `mklev.c:wallification()` | `js/mklev.js` | 🟡 | Present but may have edge cases. |
| Monster/object fill (mkfill, mineralize) | `mklev.c`, `mkobj.c`, `makemon.c`, `engrave.c`, `rumors.c` | `js/mklev.js` + `js/random_text.js` + `js/monster_data.js` + startup fast-forward scaffolds | 🟡 | `mkobj()` consumes upstream-derived class/object probability and material metadata. `rndmonst_adj()` now walks a generated `include/monsters.h` table with C's weighted reservoir RNG shape and numeric dungeon/special alignment (`A_NONE` for unaligned special levels); trap constants match `trap.h`; `occupied()` rejects trap squares; `CORPSE`/`STATUE`, duplicate-name wereform `verysmall()` disambiguation, random-artifact eligibility before erosion, trap-victim role corpse range, special-corpse timer restart after caller corpsenm overrides, no-corpse retry, grave epitaph data-file offset, egg init, grid bug gender, multigen projectile quantity, tin spinach/random-meat ordering, spider/snake hide-under `mkobj_at()`, kobold/giant/elven/centaur/troll/gnome/ogre/default `m_initweap()` slices, mummy inventory, `S_QUANTMECH` inventory, leprechaun gold/minvent state, retained monster-init `minvent` objects, creation-time boot wear via `m_dowear(mtmp, TRUE)`, `m_initthrow()` stack quantities, `M2_GREEDY` money gates with minvent-shaped dice, adjusted-`m_lev` inventory gates, `mflags1` defensive-item predicates, golem fixed HP, shared offensive-item gate, `rnd_misc_item()` slices, amulet cursed-gate init, placed-gold object display color, and `peace_minded()` predicates now consume closer C RNG. Random graffiti uses NetHack-style padded data-file selection and `wipeout_text()`. `level_finalize_topology()` calls real `mineralize()`, retains unburied mineralized gold/gems on `level.objects`, and skips buried gold/gems after kelp on most special levels. Evidence: `seed0116` now carries Soko zoo monsters/gold/objects into complete RNG parity; `seed0383` has moved beyond earlier pet object scans, `mfndpos()` terrain denominators, swallowed-position drift, gas-region lifetime, fog-cloud region maintenance, swallowed-command attack, hallucination status exercise, expulsion relocation, occupied-square hero attack RNG, pet abuse/kill RNG, Oracle loading, item-search drift, ranged/weapon ownership, later openable/trapped-door monster movement, bigroom display, amulet/wand appearance naming, warning glyphs, delayed monster-message boundaries, and the current ape-hit pline. Current `seed0383` RNG is complete; remaining blocker is ape-hit map drift, not object generation. `seed5002` now reaches `S 72/410 R 5836/12167`; `seed8000` screen parity remains `23/23`. |
| Shop generation | `mkroom.c:mkshop()`, `shknam.c`, `makemon.c:set_mimic_sym()` | `js/mklev.js:do_mkroom()` / `mkshop()` / `stock_room()` partial | 🟡 | The ordinary-level special-room decision now runs before fill. `mkshop()` can scan for an eligible one-door ordinary room, mark a shop type, convert big wand/book shops to general stores, and the special-room fill path now creates a shopkeeper, capital gold, optional charging scroll, tribute special-spot RNG, and a first C-shaped `stock_room()`/`mkshobj_at()` pass with mimic/object front doors. Shop mimics now use the shop-specific `rn2(10) >= depth` strange-object branch before falling back to shop item/object-class appearance. Full shopkeeper metadata, billing, names, all shop-specific item tables, door state, and exact stocking side effects remain incomplete. Evidence: `seed0116` moved past the level-2 shop chance gate to FR 4592 and remains a full pass; `seed5002` now reaches `S 72/410 R 5836/12167`, with the next first mismatch at inventory message text for a wand. |
| Special level loading (Lua specials) | `sp_lev.c` | `js/mklev.js` static themed-map/bigroom/Soko/Oracle slices | 🔴 | Full special level support is still absent. Static themed `des.map()` fragments now use exact upstream map literals for implemented shapes, enable `in_mk_themerooms` failure semantics, create irregular `filler_region()` rooms from flood-filled floor bounds, and use C-like irregular door search. The mapped themed-room helper now includes the `Circular, medium` shape, retries colliding `lspo_map()` origins against terrain/room metadata before contents, and keeps the C order of origin rolls, `filler_region()` percent gate, `litstate_rnd(-1)`, and the separate `themeroom_fill()` reservoir. Dynamic `des.room({ type="themed", contents=themeroom_fill })` entries now also run the fill reservoir, with current support for `Buried zombies` and the `Storeroom` percent/default-inventory mimic front door plus explicit post-`makemon()` `appear_as="obj:chest"` override; corpse timer shape and many fill contents remain shared debt. `bigrm-12` now dispatches before ordinary level generation, consumes the random variant, Lua shuffle/init, applies the C-centered map offset (`x=3,y=1` for the 75x19 slice), runs scripted percent gates, lights the whole selected area (`C ref: dat/bigrm-12.lua des.region(..., "lit")`), applies `des.wallify()`'s preliminary `wallify_map()` pass, performs stair/object/trap/monster placement with retained `des.trap()` state, runs final `wallification()` before the post-load horizontal flip (`C ref: sp_lev.c:lspo_final_map_cleanup()`), consumes the post-load `flip_level_rnd()` horizontal gate for `noflipy`, and skips special-level buried minerals after kelp. Soko1 slices now create filled irregular zoo room metadata, attach doors in x-major order, flip room/door metadata with the map, run `fill_special_room()` after flip/fixup, apply dummy-endgame depth fixup, repair display wall spines after flip, and premap Sokoban remembered terrain/boulders/traps. Oracle now scripts the centered room, centaur statues, Delphi subroom, fountains, Oracle monster, random rooms, corridors, and wallification. Evidence: `seed0116` is complete (`S 127/127`, `R 12562/12562`); `seed0383` now reaches `S 175/219`, `R 16915/16915`; `seed5002` now reaches `S 222/410 R 11673/12167`, and `seed0013` sentinel RNG prefix remains `573/4804` without screen regression. |

---

## 🧑 Player Initialization (newgame / chargen)

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Name prompt / character selection UI | `pline.c`, `sounds.c` | `js/allmain.js:player_selection()` | 🟡 | **Hardcoded `_override_screen` for seed0002 only.** Must be generalized. Seed0002 no longer injects display-only floor objects after `mklev()`; visible map object state now comes from retained real level objects. |
| Role/race/gender/align selection from nethackrc | `role.c:plnamesiz()` etc. | `js/options.js` + `js/allmain.js` + `js/roles.js` (partial) | 🟡 | Parsed role/race/gender/align now drive startup identity, level-1 rank, alignment record, welcome text, and status title where config supplies them. This still does not implement chargen RNG, stats, or inventory. |
| Random chargen (pick_role etc.) | `role.c:pick4u()` | `js/allmain.js` (hardcoded `rn2()` calls) | 🔴 | Hard-coded for seed0002. Not a real implementation. |
| Hero placement (`u_on_upstairs`) | `stairs.c:u_on_upstairs()` | `js/mklev.js:u_on_upstairs()` | 🟡 | Works for seed8000/seed0002. **seed0002 step 12 has 1-cell offset bug.** |
| Player stats (HP, Pw, AC, attributes) | `attrib.c`, `role.c` | `js/u_init.js` + `js/allmain.js` (partial) | 🟡 | Configured non-replay Wizard/Rogue/Healer/Tourist now use partial role-driven HP/Pw/AC/gold and `init_attr()`/`vary_init_attr()` RNG shape. Attribute storage follows C order (`Str, Int, Wis, Dex, Con, Cha`), human race maxes are used during redist, and positive `vary_init_attr()` adjustments can raise the per-attribute maximum like `adjattrib()`. Wizard startup AC now distinguishes the legacy quest-intro screen, where cloak wear is deferred past the first status render, from `OPTIONS=!legacy`, where the first visible map/status screen already reflects worn cloak AC. Seed replay paths remain scoped. |
| Starting inventory (`ini_inv`) | `invent.c:ini_inv()` | `js/u_init.js` + `js/mklev.js` object creation (partial) | 🟡 | Wizard `ini_inv(Wizard)` now consumes the general `trobj` path for fixed/random wands, rings, potions, scrolls, spellbooks, and generated-id magic marker charges, using `mkobj()`/`mksobj()` rather than replay. Random starting objects pass through a partial `ini_inv_mkobj_filter()` port with no-create tracking for polymorph combinations, duplicate ring/spellbook suppression, useless-item rejection, and spellbook level gating (`C ref: u_init.c:ini_inv_mkobj_filter()`). The optional Wizard blindfold branch is retained behind its `!rn2(5)` gate. Initial-inventory erosion is suppressed via the upstream `moves <= 1 && !in_mklev` rule. Wizard startup inventory now marks the quarterstaff wielded and cloak worn as object state, while legacy startup still defers visible AC until after the first status render. Other roles and full inventory use/wear/discovery side effects remain incomplete. Evidence: `seed5002` inventory display now shows the initial weapon/cloak state at screen 276 without regressing `seed0116`'s later takeoff state. |
| `o_init` (object type shuffle) | `o_init.c` | `js/o_init.js` | 🟡 | General RNG shape for gem colors, description shuffle ranges, and `WAN_NOTHING` direction is implemented for non-replay sessions. Runtime `oc_color`, `oc_material`, and current ring/amulet/potion/scroll/spellbook/wand/venom plus shuffled helm/glove/cloak/boot descriptions are retained for map display, material predicates, and unknown carried appearance names where user-facing naming exists (`C ref: objects.h` descriptor rows, `o_init.c:shuffle_all()`). Novel and Book of the Dead fixed descriptors are retained outside the ordinary spellbook shuffle range. Toughness swaps, discovery persistence, and complete user-facing identity state are still incomplete. |
| Welcome / lore screens | `sounds.c`, `pline.c`, `role.c:Hello()` | `js/allmain.js:newgame()` + `js/roles.js:roleGreeting()` | 🟡 | Welcome greeting is role-driven rather than seed-driven. Configured role/align splash text now uses role god/rank data for the generic quest intro overlay and honors `OPTIONS=!legacy` by skipping that pager; no-legacy startup welcome prompts render tty `--More--` on the next message row only when the welcome text plus prompt would overflow the 80-column topline. The late `nhlib.lua` shuffle is now scoped to the legacy pager path, while full-moon/new-moon/Friday-the-13th `moveloop_preamble()` messages queue behind the welcome prompt as normal topl messages. Full chargen/menu flow and exact windowing remain incomplete. Evidence: `seed5002` now reaches the first post-startup map/cursor mismatch at `S 4/410`, `R 3986/12167` instead of the startup inventory/status/message lifecycle. |
| Tutorial prompt | `pline.c`, `options.c:ask_do_tutorial()` | `js/cmd.js` prompt overlay + seed0002 override | 🟡 | General post-welcome tutorial prompt now appears when `tutorial` was not set in config, including Space/Enter invalid-choice redraw. Seed0002 still uses older startup override screens. |

---

## 👁️ Vision & Display

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Shadow-casting vision (`vision_recalc`) | `vision.c` | `js/vision.js` | 🟡 | Implemented. Spot-checked for seed8000/seed0002. Waterwall, lava-wall, and cloud terrain now block sight even though they are not movement-obstructed (`C ref: vision.c:does_block()`). |
| Terrain glyph rendering | `display.c:newsym()` | `js/display.js:newsym()` + `terrain_glyph()` | 🟡 | Mostly correct. DEC line-drawing output works. TTY `CLR_GRAY` now normalizes to `NO_COLOR`, ordinary stairs render in default color while branch stairs remain yellow, undiscovered `SDOOR` cells render as oriented walls, T-wall glyphs use `seenv` for the visible wall angle (`C ref: display.c:wall_angle()`), liquid terrain renders with the corpus-compatible DEC backtick byte (`C ref: display.c:back_to_glyph()`), and pools/lava cover floor objects/traps in map rendering (`C ref: display.h:covers_objects()`). |
| Object rendering on map | `display.c:newsym()` | `js/display.js:newsym()` | 🟡 | Renders live objects from `game.level.objects[]` with runtime `objects[otyp].oc_color`, including `o_init()` shuffled appearance colors for non-replay sessions. Under Hallucination, visible objects now use `display.h:random_obj_to_glyph()` shape on the display RNG, with `NUM_OBJECTS` excluding the generated sentinel slot, corpse glyphs taking a random monster color, and statues taking the random-monster/gender path. Remaining visible color mismatches with exact RNG should be treated as object identity/state drift, retained glyph timing, or display-RNG ownership before blaming class-default rendering. |
| Monster rendering on map | `display.c:newsym()` | `js/display.js:newsym()` | 🟡 | Renders from `game.level.monsters[]` and now overlays warning glyphs for unseen hostile monsters within `Warning` range (`C ref: display.h:_mon_warning()`, `display.c:display_warning()`). Warning refresh is driven from the main input-boundary path instead of every screen serialization, and the Warning branch is skipped when the Hallucination branch already ran, matching `allmain.c:moveloop_core()`. Swallowed `newsym()` returns before external monster/object/trap redraws like C, and non-visible `newsym()` no longer materializes hallucinated monster/object glyphs before deciding to show memory/warnings. Visible real monsters use `what_mon(..., rn2_on_display_rng)` for hallucinated appearance glyph/color. Single-step movement/attacks now distinguish C's `context.mv` from turn-consuming `context.move`, so the once-per-input Hallucination redraw can run after ordinary swallowed attacks. Mimic hallucination substitutions and many visibility edge cases remain incomplete. Current `seed0383` residue is the post-expulsion visible-map hallucination redraw stream on screen 172. |
| Remembered glyph (out-of-sight) | `display.c` | `js/display.js` | 🟡 | Implemented via `remembered_glyph`. |
| Status line (bot) | `status.c` | `js/display.js:bot()` | 🟡 | Verified passing for seed8000 (23/23). Bottom line now includes the active hallucination condition abbreviation (`Hallu`) for current swallowed Wizard evidence; broader condition ordering/encumbrance/status coverage remains partial. |
| Message line (pline) | `pline.c` | `js/display.js:pline()` + `js/cmd.js:rhack()` | 🟡 | Message lifetime now matches prompt-time capture better: persist through `nhgetch`, clear on next real command start, and preserve queued messages behind override `--More--` screens. More prompts are rendered into the terminal grid with row-0 cursor placement, queued `--More--` prompts are gated before normal command dispatch, and current monster-turn and fire-destruction plines can perform nested tty `--More--` reads before death/pickup or inventory-destruction side effects continue. Current fire destruction also defers turn-tail monster movement while `zap.c:zhitu()` is blocked and reaches the wizard-mode death confirmation prompt after `You die...`. Wizard `#levelchange` now queues `pluslvl()` welcome and innate-ability messages with tty-style prompt packing (`C ref: exper.c:pluslvl()`, `attrib.c:adjabil()`). Broader message production is still partial. |
| Sounds system | `sounds.c` | `js/sounds.js` | 🟡 | Basic `dosounds` with "slow drip" and the zoo room `rn2(200)` gate implemented. Broader room/noise messages are still partial. |
| Full-screen menus (inventory, spells, discoveries, attributes) | `pline.c`, `invent.c`, `spell.c`, `pager.c`, `wizcmds.c:wiz_intrinsic()` | `js/cmd.js` menu builders + `_override_screen` lifecycle | 🟡 | Inventory `i` now builds a grouped menu from live inventory, clears the C-like right-side window rectangle, preserves underlying map/status cells, uses live shuffled ring/amulet/potion/scroll/spellbook/wand descriptors for unknown current evidence, and keeps seed8000's Tourist starter as visible fallback debt rather than a raw screen replay. Wizard `+`, `\`, and `^X` now build current evidence spell/discovery/attribute windows and page through generated attributes. `#wizintrinsic` now opens the paged `NHW_MENU`, honors cmdassist text, toggles hallucination on `h`, and commits on Enter with the right message-more boundary. Residual debt: broader discovery persistence, full spell retention/failure calculation, non-Wizard/non-Tourist attribute text, and the remaining swallowed-display color drift on the hallucinatory commit screen. Evidence: `seed0116` moved from screen 122 to `S 127/127`; `seed0383` now reaches the intrinsic-menu commit screen instead of the prompt stub. |
| DEC line-drawing output | `curses.c` | `js/display.js` | ✅ | Outputs Unicode directly; runner translates DEC→Unicode for comparison. |
| `cls()` / `docrt()` | `display.c` | `js/display.js` | 🟡 | Functional. |
| Hallucination display context | `display.c`, `do_name.c:rndmonnam()` | `js/rng.js` + `js/display.js` + `js/allmain.js` + `js/random_text.js` | 🟡 | Display RNG is now split from core RNG; tty black uses the same bright-black SGR shape as upstream (`wc2_darkgray`), and visible monster/object glyphs consume display RNG through `what_mon()`/`random_obj_to_glyph()`-shaped paths when hallucinating. The generated monster table now strips inactive `CHARON` rows, so active `NUMMONS` is 383 like this C build. `#wizintrinsic` hallucinatory transitions route edge changes through a shared swallowed-redraw helper (`make_hallucinated()->swallowed(0)`), `moveloop_core()` refreshes hallucination at C-like `!context.mv` input boundaries with `see_monsters(); see_objects(); see_traps();`, the Warning branch is not repeated after that Hallucination branch, `topl.c` More dismissal gets the resumed swallowed redraw, hallucinated monster names now use `rndmonnam()`/padded `bogusmon` data on display RNG, and swallowed expulsion latches the old stomach/status frame across the blocking More while `unstuck()`/relocation RNG remains immediate. Remaining drift is the visible-map hallucination stream after expulsion at `seed0383` screen 172. |

---

## 🕹️ Command Dispatch (moveloop)

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Main game loop (`moveloop_core`) | `allmain.c:moveloop_core()` | `js/allmain.js:moveloop_core()` | 🟡 | Basic loop. Fast-forward stubs for per-step RNG. |
| Command dispatch (`rhack`) | `cmd.c:rhack()` | `js/cmd.js:rhack()` | 🟡 | Move commands, uppercase vi run commands, rest/wait (`.`), safe-search prevention for `s` near visible hostile monsters, close-command direction prompting with tty cmdassist invalid-direction help, empty-square pickup messaging, `#pay` no-shopkeeper message, basic drop, put-on/wear prompts for rings, amulets, and delayed armor evidence, zap-wand prompt/direction flow with current digging/fire ray front doors, wizard wishes for current object evidence including explicit BUC/enchantment state and namedesc cross-class lookup, debug `^G` create-monster prompt/name dispatch, `#wizintrinsic` active-menu dispatch, debug `^V` numeric/menu level teleport with current-row marking, `#levelchange` prompt/level-gain RNG plus welcome/innate message packing, and zero-turn inventory/spell/discovery/attribute menu lifecycles are present. Next `#wizintrinsic` work belongs in zero-time active-menu dispatch, not in a prompt-letter branch: Enter after completing the extended command opens the menu, `h` toggles a row, and Enter commits side effects. Run commands and delayed armor occupations now consume repeated movement/turn loops before the next input boundary, apply deferred armor AC for the final occupation turn, and can pause/resume on tty `--More--` during delayed occupation, queued fire-zap side effects, and current monster-turn message boundaries. Stop/turn/lookaround rules and full interruption semantics remain approximate. Most commands remain unimplemented. |
| Cardinal movement (`domove`) | `do.c:domove()` | `js/cmd.js:domove()` | 🟡 | Basic movement plus occupied-monster and swallowed-hero front doors: movement into a monster square consumes a turn, leaves the hero in place, and now uses the same narrow current-evidence `uhitm()` RNG shape as swallowed directional movement into `u.ustuck`. Hero melee now applies damage to retained monster HP, prints kill text before `xkilled()` side effects when damage is lethal, and handles a minimal tame-kill `abuse_dog()`/`xkilled()` front door. Full `uhitm()` hit/damage/passive/death effects, traps, and item pickup remain incomplete. |
| Zero-turn commands (inventory, ESC, etc.) | `cmd.c` | `js/cmd.js` | 🟡 | `g.context.move=0` on zero-turn commands confirmed in lessons. |
| Per-step monster movement / regen RNG | `monmove.c`, `regen.c`, `were.c`, `track.c` | `js/monmove.js` + `js/track.js` + partial turn consumers | 🟡 | Per-step replay is no longer called from `moveloop_core()`, and the dead `fastforward_step()` exports have been removed from the seed replay modules. `makemon()` now preserves C `fmon` head-insertion order, `movemon()` snapshots the pass like `iter_mons_safe()`, `mcalcmove()` handles slow/fast speed states, `mcalcdistress()` owns human-form werecreature `were_change()`, `mspec_used` timeout, and monster frozen-state timeout before movement allocation, damaged-hero `regen_hp()` owns the post-generation `rn2(100)` turn-tail roll, `exerchk()` handles hunger/Con exercise plus hallucination/confusion Wisdom abuse on 5-turn boundaries, fog clouds retain vapor regions, keep zero-TTL regions visible until the next region tick, apply the fog-cloud `inside_gas_cloud()` TTL extension, and age after the final monster movement pass for the turn, uppercase run commands reuse the normal turn advancement loop, `settrack()` records the hero track ring at the turn-boundary and `initrack()` clears it on new games and level teleports (`C ref: track.c:settrack()/gettrack()`, `cmd.c` level-change `initrack()`), sleeping/frozen monsters and object/furniture-appearing hiders spend movement without entering `distfleeck()`, and ordinary non-pet monsters now take a minimal `dochug()`/`m_move()` path with post-`m_move()` `distfleeck()`, swallowed-bystander `MMOVE_MOVED`, `set_apparxy()`-style hero targeting, swallowed hero coordinates pinned to `u.ustuck`, peaceful/light approach gates, confusion/stun recovery gates, `mtrack` backtracking rolls, hero-track retargeting when `should_see` is false, `m_balks_at_approaching()` front-door behavior for polearms, aklys autoreturn range, and current ranged-attack availability (`C ref: monmove.c:m_balks_at_approaching()`, `mhitu.c:ranged_attk_available()`), partial `mfndpos()` terrain eligibility for flying/swimming/pool/lava/wall-passable squares, diagonal door blocking, openable closed-door candidates, trapped-door `postmov()`/`mb_trapped()` damage, minimal `m_search_items()`/`mpickstuff()` collector pickup with visible pickup plines that leave tty `--More--` pending until the next monster pline boundary, boot `m_dowear()` delay, creation-time boot wear, constrained post-move `mattacku()`/negative-AC `AC_VALUE()` for moved weapon users, a swallowed pre-finish turn front door matching `allmain.c:moveloop_core()`'s hero-can-move loop, and minimal `weapon_check` handoff for ranged failure into HTH wielding. Movement AI remains skeletal. `seed0116` now passes completely with full RNG parity; `seed8000` remains complete on screens, and `seed0383` reaches `S 163/219 R 16915/16915`; the current blocker is the `#wizintrinsic` menu, not swallowed cold-damage timing. |
| Turn counter | `allmain.c` | `js/allmain.js` | ✅ | `g.moves++` conditional on `g.context.move`. |
| Game over (`gameover`) | `end.c` | `js/cmd.js` (stub) | 🔴 | Not implemented. Loop never terminates normally. |

---

## 🎒 Items & Objects

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Object type table / `o_init` shuffle | `objects.c`, `o_init.c` | `js/object_data.js` + `js/o_init.js` | 🟡 | Static upstream object class/probability/charge/direction/material/color metadata is available for `mkobj` selection, erosion eligibility, and map display. `init_objects()` now consumes the general C description-shuffle RNG shape for non-replay sessions and retains shuffled color/material plus ring/amulet/potion/scroll/spellbook/wand/venom and shuffled armor-subrange description state, reducing seed-specific startup replay reliance and fixing current shuffled object colors/names. Generated local IDs corrected current stale wand constants (`WAN_MAGIC_MISSILE=429`, `WAN_FIRE=430`, `WAN_COLD=431`, `WAN_DEATH=433`, `WAN_LIGHTNING=434`), so unknown wished wands use their live shuffled descriptions such as `pine wand` and `curved wand`; unknown potions/scrolls/rings/spellbooks/armor/special books now use live appearance text such as `ruby potion`, `scroll labeled ZELGO MER`, `wooden ring`, `cloth spellbook`, `tattered cape`, and `paperback book`. Gem-class gray stones now follow C init gates for luckstone/loadstone/rock instead of the ordinary gem quantity roll. Toughness swaps, artifact selection/origin tracking, and complete item identity/discovery parity are still incomplete. |
| Random text data files | `rumors.c`, `engrave.c`, `makedefs.c`, `dat/rumors.*`, `dat/engrave.txt` | `js/random_text.js` + `js/random_text_data.js` | 🟡 | Random engraving/rumor selection builds padded/xcrypted virtual chunks from checked-in generated JS data and applies C-like `get_rnd_line()` and `wipeout_text()` RNG. Production JS is browser-safe and does not read upstream data files with `fs` at runtime. It currently serves graffiti generation; broader data-file consumers (epitaphs, bogus monsters, oracle text) still need integration. |
| Object placement on map | `mkobj.c:mksobj()` | `js/mklev.js:mksobj_at()` / `mkobj_at()` + legacy hardcoded arrays in `allmain.js` | 🟡 | Generated `_at` object creation now retains non-gold objects on `level.objects` with class glyph and upstream display-color state, uses C's newest-first `fobj` order, and `mkcorpstat()` now places created corpses/statues at the supplied coordinates instead of only consuming their init RNG. `mktrap_victim()` now retains the trap item, cursed random possessions, optional candle, and corpse at the trap square; trap-victim ammo now uses generated `ARROW`/`DART` IDs instead of stale hardcoded IDs. `mkcorpstat()` restarts ordinary rot timing when a caller override crosses a special-corpse timing boundary. Unburied `mineralize()` gold/gems are retained, while buried object chains, timer storage/effects, and PIT/landmine `breaktest()` destruction remain pending. Seed0002 still has legacy hardcoded startup objects, and object stacking/descriptions/material swaps/container contents remain incomplete. Evidence: `seed0116` dog blocker moved through the first run-turn mineral object scan; retained corpse/statue/trap-victim/mineral state and C-like floor-list order reduce hidden object-state debt for later dog/display/pickup work. |
| Gold | `mkobj.c` | `js/mklev.js:mkgold()` + hardcoded `g._goldCount` | 🔴 | Floor gold created during level generation now uses `GOLD_PIECE` object metadata for display color and merges quantities on existing squares. Player gold remains per-seed hardcoded. |
| Autopickup | `pickup.c` | — | 🔴 | |
| Inventory management | `invent.c` | `js/cmd.js` + `js/u_init.js` partial | 🟡 | Inventory is still not a full `invent.c` port, but carried objects now receive persistent `invlet` letters lazily, startup/wished stackable objects use conservative `addinv()`-style merge semantics, wizard wishes preserve explicit blessed/cursed/uncursed and enchantment text for current known-object evidence, current named wand/tool wishes create inventory objects with shuffled unknown appearances, `wand of magic missile` uses the C namedesc pool that also sees the spellbook, stethoscope/magic-marker/mirror/camera wishes use namedesc probability pools, magic-marker apply enters the write-on target prompt and preserves that prompt across invalid inventory-letter `--More--`, startup inventory clears cursed state before role adjustments and marks initial items known, basic drop places the object on `fobj`, `P`/`W`/`z`/`r`/`e` prompt filters use object class and worn state rather than current array position, worn/wielded startup object state feeds generated inventory menus, unknown magic-marker charges are hidden, and inventory display groups live carried objects while hiding BUC, enchantment, and charges for unknown appearance objects. Selecting an inventory-menu item now opens a stateful item-action menu; invalid action keys redraw it, `t` enters the shared direction prompt, invalid throw directions use the cmdassist `--More--` screen, Space at the read prompt cancels with `Never mind.`, and `e` with no food reports `You don't have anything to eat.`. Evidence: `seed0116` remains a full pass; `seed0383` Wizard duplicate scrolls still scan as one carried stack in `dog_goal()`; `seed5002` advances through inventory/action/read-prefix/eat evidence to `S 368/410 R 12039/12167`, with the next visible blocker in hero-vs-bat kill text. |

---

## 👾 Monsters

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| Monster placement (mklev fill) | `makemon.c`, `include/monsters.h`, `include/monflag.h` | `js/mklev.js:makemon()` + `js/monster_data.js` + `js/allmain_turns.js` | 🟡 | Random monster selection follows `rndmonst_adj()` reservoir sampling over generated `monsters.h` data and consumes `next_ident`, adjusted instance level/HP including golem fixed-HP no-RNG cases, generated `mattk[]` attack metadata, `peace_minded()` predicate/call order, broader selected-monster weapon/inventory gates, retained monster-init `minvent` objects, creation-time boot wear, `m_initthrow()` stack quantities, `mflags1` defensive-item predicates, leprechaun gold/minvent state, greedy gold dice/object creation, `rnd_misc_item()` slices, retained level monster records, `MM_ASLEEP`, Soko mimic boulder appearance state, spider/snake hide-under object creation during mklev, wizard `^G` named-monster creation through `makemon(ptr,u.ux,u.uy)`, and `goodpos()` occupied-square rejection for placement even without `GP_AVOID_MONPOS`. Turn-tail `maybe_generate_rnd_mon()` now calls live `makemon(NULL,0,0)`, and that path uses `makemon_rnd_goodpos()` before species selection like C. Exact selected-monster initialization/equipment, some special-level placement/selection details, and real movement/weapon AI are still incomplete; `seed0116` has full RNG parity, `seed0383` has full RNG parity with residual ape-hit map-state drift, `seed5002` now reaches `S 88/410 R 5934/12167`, and `seed8000` still blocks in live monster movement at FR 2985. |
| Monster movement | `monmove.c`, `were.c`, `track.c` | `js/monmove.js` + `js/track.js` (partial) | 🟡 | Ordinary monsters now have movement budget ownership, a pass snapshot matching `iter_mons_safe()`, post-`m_move()` `distfleeck()` recalc, swallowed-bystander `MMOVE_MOVED`, `set_apparxy()`-style hero targeting, swallowed hero position sync while `u.uswallow`, a narrow adjacent-candidate `m_move()` skeleton, retained `mtrack` backtracking rolls, hero-track retargeting through `gettrack()` when the C `should_see` gate is false, the pre-allocation `mcalcdistress()`/human-were `were_change()` and `mspec_used` timeout gates, retained fog gas regions aged after the final movement pass with zero-TTL visibility and fog-cloud TTL extension for `m_everyturn_effect()`, C-shaped front doors for frozen/sleeping monsters, hidden mimics, peaceful neutral movement, stalker/bat/light approach, confusion/stun recovery, partial `mfndpos()` terrain eligibility for flying/swimming/pool/lava/passwall squares, diagonal door movement blocking, openable closed-door movement candidates, trapped-door `postmov()`/`mb_trapped()` handling, type-specific `muse.c:searches_for_item()` gates, the `lined_up()` item-search suppression gate, minimal collector pickup with visible pickup plines plus boot wear delay, creation-time boot wear, constrained post-move `mattacku()` for moved weapon users, minimal `weapon_check`/HTH wield turn handling, supported physical and engulf evidence with the swallowed-hero `u.ustuck` guard, narrow stethoscope-plus-monster-hit topline packing, pet-combat tty `--More--` turn interruption/resume points, `gulpmu()` timer decrement, a pre-finish swallowed turn front door, and expulsion `unstuck()`/`mnexto()` relocation. Tunneling, special movement, most traps, transformations, full ranged weapon attacks, dog `gettrack()` fallback, and full `mfndpos()` flags remain incomplete. Current seed0116 evidence now has complete RNG parity; current seed0383 evidence has complete RNG parity and is blocked on `#wizintrinsic` menu rendering rather than movement RNG. |
| Combat | `fight.c`, `uhitm.c`, `mhitm.c`, `mthrowu.c`, `weapon.c` | `js/cmd.js` + `js/dog.js` + `js/monmove.js` narrow front doors | 🔴 | Full hero combat, monster-vs-monster combat, and ranged throwing remain unimplemented. `domove()` now stops hero movement into occupied monster squares, consumes the current evidence's `uhitm()` front-door RNG, consumes a turn, and leaves the hero in place; swallowed directional movement uses the same current-evidence `uhitm()` RNG shape against `u.ustuck`. That front door now applies retained monster HP damage and, when the hit kills a tame monster, consumes the current `abuse_dog()`/hallucination yelp/`xkilled()` gates and removes the monster instead of continuing into knockback/passive rolls. A narrow pet `mattackm()` front door consumes the current physical miss/passive and hit/damage/death/growth RNG shape for `dogmove.c` evidence, uses C's `find_mac(defender) + attacker level > dieroll` to-hit comparison for current feline/rat/bat evidence, includes `grow_up()` max-HP/current-HP state after pet kills and the `rn2(4)` gated defender return attack, delays defender-kills-attacker death side effects until the blocking hit-message More is dismissed, and emits current miss/hit/kill plines at C-like `hitmm()`/`monkilled()` boundaries that can queue nested tty `--More--` during delayed occupation or pet-combat turn interruption. A constrained `mattacku()` front door now handles generated multiattack physical melee rows plus current single-attack `AT_ENGL` evidence, moved-then-ranged `AT_WEAP` AC ownership, failed ranged selection feeding `weapon_check`, `AC_VALUE()`, occupation hit bonus, hit rolls, damage dice, current physical hit plines, the initial engulf pline, repeat `gulpmu()` elemental damage plines, and swallowed-display handoff through tty `--More--` boundaries (`C ref: mhitu.c:hitmsg()`, `mhitu.c:gulpmu()`, `display.c:swallowed()`, `pline.c:pline_mon()`), initial and repeat `gulpmu()` timing, expulsion `unstuck()`/`mnexto()` relocation, elemental cold/fire/electric gate, initial knockback rolls for physical rows, negative-AC reduction, hero HP updates, and fatal non-pet hit interruption before turn-tail `regen_hp()`/hunger with only the pending tail resumed after wizard-mode `savelife()`. Current seed0383 reaches full RNG parity after later movement fixes; these are position/RNG ownership front doors, not a general combat port. |
| Pet behavior | `dog.c`, `dogmove.c` | `js/dog.js` + `js/mklev.js:makemon()` + `js/monmove.js` (partial) | 🟡 | Starting pet creation is partially ported for configured and current replay-backed startup paths: role/preferred-pet selection, `MM_EDOG|NO_MINVENT`, near-hero `collect_coords()` ring shuffling, pet tame/peaceful setup, pre-inventory `edog->apport` initialization, `edog->whistletime`, and kitten/little-dog/pony metadata. Seed0002 no longer injects a stale hardcoded dog record; it now creates the initial little dog through `makedog()` before post-pet startup replay. Tame monster turns now enter a partial `dog_move()` path with C-like `M2_WANDER` short-circuit behavior, C-like neighbor scan order, `whappr` suppression of worse-candidate backtracking, `chcnt` reset after strictly better candidates, boulder-occupied square rejection, Sokoban `m_avoid_soko_push_loc()` push-block avoidance, old/new square redraws, candidate-square pet melee after `mfndpos()`-style square reachability, adjusted-level/HP pet attack balking fed by `grow_up()` HP max/current state, `mattackm()` defender return attacks except during the narrow savelife resume, and a narrow `pet_ranged_attk()`/`score_targ()` scan that consumes line-target fuzz before final movement. Pet-combat `--More--` can now pause `movemon()` and resume after the blocking pet in the same pass so later monsters act before any second fast-pet pass; a packed monster message More is suppressed at the final movement pass when no pet-combat boundary latched behind it. `dog_goal()` now scans retained floor objects and hero inventory stacks with `dogfood()`/`obj_resists()` guards for real numeric `otyp` objects; its apport branch has the C lighting/front-door predicate, `m_cansee()` via `clear_path()`, swallowed-master sight suppression, partial diet-aware food classification, rock-class `UNDEF` handling after the resistance roll, non-hungry fallback for non-`DOGFOOD`/non-`APPORT` goals, pool/lava/boulder reachability gates, and a conservative carry-capacity front door that rejects heavy tools for small domestic pets. Unique-object resistance checks use current generated IDs for the real Amulet of Yendor and Book of the Dead instead of stale ring/spellbook IDs. Level-teleport pet arrival preserves the migrating tame monster's movement/data across `mklev()` and no longer recreates a killed pet from `game.pet_type`. Current `seed0116` has full RNG parity. Current `seed0383` has moved beyond pet arrival, pet goal, pet melee, swallowed-master, expulsion, hero-pet kill side effects, and later non-pet movement into full RNG parity. Hunger, naming, saddles, full object fetching/eating, migration details, and interactions remain incomplete. |

---

## 📅 Time & World State

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| `datetime` parsing / moon phase | `calendar.c` / `hacklib.c:timerep()` | `js/hacklib.js` + `js/jsmain.js` | 🟡 | Segment `datetime` is parsed into C-like `tm_*` fields; `flags.moonphase`, `flags.friday13`, `iflags.at_night`, and `iflags.at_midnight` are initialized. Startup luck/message side effects and save/restore date handling are not wired yet. |

---

## 🔧 Options / Config

| Feature | C Reference | JS Implementation | Status | Notes |
|---|---|---|---|---|
| nethackrc parsing | `options.c` | `js/options.js:parseNethackrc()` | 🟡 | Parses the current OPTIONS= subset and exposes it on game state for startup. Still incomplete versus upstream options.c. |
| `OPTIONS=name:...` → chargen | `options.c` | `js/options.js` + `js/allmain.js` (partial) | 🟡 | Name plus parsed role/race/gender now flow into startup identity when present. Align, random role selection, stats, inventory, and menus remain incomplete. |
| `OPTIONS=!tutorial` | `options.c` | `js/options.js` | 🟡 | Parsed as flag. Not enforced in player_selection. |

---

## 📊 Current Score Summary

*(Update this section after every agent run — it is a lagging indicator of progress, not the definition of progress.)*

| Session | Matched | Total | Last Updated | Notes |
|---|---|---|---|---|
| seed8000-tourist-starter | **23** | 23 | 2026-05-12 | Screen parity preserved. First RNG mismatch remains in live monster movement after generalized ordinary-monster movement work; current lagging RNG is `3060/3130`. |
| seed0002-healer-reflection-drummer | 11 | 595 | 2026-05-16 | First visible screen mismatch unchanged at screen 11. Current sentinel RNG evidence is `2652/27158` after moving starting pet creation out of stale post-mklev replay, replacing the replayed startup turn-tail RNG with live `mcalcmove()`/`maybe_generate_rnd_mon()`/`dosounds()`/`gethungry()`/engraving-wipe calls, applying C's `NODIAG(PM_GRID_BUG)` movement candidate rule, and removing seed-specific display-only floor-object injections. The pet path now matches through early `dog_goal()` object-resistance scans and the first grid-bug mtrack denominator; first RNG mismatch remains `FR 2375` (`rn2(5)` expected vs `rn2(100)` actual), a later movement/pet-goal ordering boundary. Lagging RNG count is lower than the prior replay-heavy checkpoint, but hack debt decreased because replay consumers now mutate live state and fake floor objects are gone. |
| seed0013-friday13-save-then-fullmoon-restore | 0 | 99 | 2026-05-16 | Sentinel evidence is `573/4804` RNG after shared Oracle/random-monster generation work. Save/restore and datetime effects remain broad blockers; the short-prefix regression is classified as shared turn-tail behavior surfacing earlier, not a screen regression. |
| seed0116-wizard-wear-shop | 127 | 127 | 2026-05-13 | Door-state aliasing, tutorial prompt lifecycle, rest command, debug `^V` prompt/Enter handling, static themed/Soko `des.map()`, irregular region bounds, corridor/vault accounting, random shop decision, level-teleport arrival placement, pet migration, normal Space handling, basic wear/takeoff/pickup/drop prompts, retained mineral objects, Soko zoo room/fill, leprechaun gold/minvent state, `rnd_misc_item()`, spider/snake hide-under objects, Soko mimic appearance state, sleeping/hider movement front doors, pet wanderer short-circuiting, partial `dog_goal()` object/inventory scans, Sokoban boulder avoidance, `whappr`, uppercase `L` run handling, ordinary non-pet `m_move()`/`mtrack` ownership, hero track reset on level teleport, persistent inventory letters, wished ring/digging-wand retention, `P` ring put-on prompts, `z` wand selection/direction flow, room sound gates, lycanthrope distress, upstream/runtime shuffled object display colors, tty gray normalization, Soko premap/trap/mimic display, level-depth fixup, and generated inventory/spell/discovery/attribute menus moved this evidence path to full pass (`S 127/127`, `R 12562/12562`). Residual debt is no longer an active blocker for this session: discovery description strings and some menu text are still limited evidence tables rather than a complete `o_init`/`invent.c`/`pager.c` port. |
| seed0383-wizard-hallucinate | 175 | 219 | 2026-05-16 | Current lagging RNG is `16915/16915` with no RNG mismatch. Startup pager clearing, oriented secret-door rendering, C-like `#levelchange` message packing, debug level-teleport current markers, `bigrm-12` lit selection/final wallification, liquid glyph rendering, `covers_objects()`, waterwall/lavawall vision blocking, `wall_angle()` T-wall rendering, live shuffled amulet/wand appearance names, unknown-object BUC/enchantment/charge hiding, warning glyphs, delayed-occupation nested `--More--` resume, pet miss/hit/kill plines, visible monster pickup plines, current physical `mattacku()` hit plines, initial and repeat `gulpmu()` cold plines, the pre-finish swallowed turn, `display.c:swallowed()` 3x3 stomach glyphs, active `NUMMONS=383`, swallowed `newsym()` early return, More-dismiss swallowed redraw, `do_name.c:rndmonnam()`/`bogusmon` hallucinated monster names, C-like `context.mv` ownership for swallowed attack redraws, expulsion More latching, C-ordered Hallucination `see_monsters()`/`see_objects()`/`see_traps()`, hallucinated object glyphs with the C `NUM_OBJECTS` bound, visible-only display-RNG materialization, and skipping duplicate Warning refresh after Hallucination advanced the visible path without changing core RNG ownership. Refreshed triage: `FS 172:char+attr:map:Space FR -`; screen 171 message/cursor/status now match through `You get expelled!--More--`; the remaining blocker is the post-expulsion visible-map hallucination redraw stream. |
| seed5002-wizard-coverage-pair | 407 | 410 | 2026-05-16 | Current lagging RNG is `12127/12167`. Wizard namedesc wishes now include the `wand of magic missile`/spellbook denominator and post-teleport stethoscope/magic-marker/mirror/camera tool denominators, queued `--More--` prompts gate before ordinary command dispatch, fire-wand side effects defer turn-tail monster movement while nested `zhitu()`/`destroy_items()` messages and wizard-mode death handling run, the level-5 generation path now includes mimic object-shape `mcorpsenm`, Storeroom default mimic inventory and explicit chest appearance, niche `mkclass(S_HUMAN)`, C's sorted `mongen_order`, and C's adjacent-table `mkclass()` toostrong predicate, and the apply/stethoscope path now reaches mimic reveal/status evidence. Search safety now blocks ordinary `s` near a found hostile while the `m` prefix forces a zero-message search turn; `c`/`o`/throw use the shared direction prompt and cmdassist invalid-direction help; magic-marker apply and `r` read prompts preserve prompt state across invalid inventory-letter `--More--`; inventory/eat menus now show known Wizard startup objects, worn/wielded state, item-action menus, unknown marker charge hiding, generated object names, and no-food `e` messaging for the current evidence. Pet-combat now uses C-shaped monster-vs-monster to-hit for current AC evidence, suppresses nonblocking packed monster/death More at C-like boundaries, delays defender-kills-attacker death side effects until after the hit-message More, and resumes from the live monster list when the paused pet dies. Death prompts latch HP 0/cursor; pet-combat savelife can pack the next combat pline after the OK line, non-paused savelife packs `You survived...`, and fatal non-pet `mattacku()` now pauses before turn-tail RNG then resumes only the tail after HP restoration. Hero melee now prints `You kill the giant bat!` for the current lethal hit. First visible mismatch is now screen 407, where C has a small mimic hit but JS is one movement/attack decision late; first RNG mismatch is `FR 12117` (`rn2(5)` expected vs `rn2(8)` actual), the next late monster movement candidate-order gap. |
| All other sessions | 2 | 10,343 | 2026-05-13 | Non-replay sessions now run general `init_objects()`, partial `role_init`, data-driven `init_dungeons()`, partial `u_init_misc`, starting pet placement, Wizard `ini_inv`, and broader mklev/monster equipment RNG. Many RNG prefixes improved, but first-screen parity still awaits broader role/race/inventory side effects, shop/special-room handling, save/restore, and hallucination display context. |

**As of focused verification on 2026-05-16: sentinel suite `S 338/1063 R 35995/64569`; full public suite not rerun in this iteration.**

---

## 🎯 Recommended Next Targets (Highest ROI first)

1. **Continue `seed0383` post-expulsion hallucination redraw ownership** — `seed0383` has `S 175/219`, `R 16915/16915`; swallowed hit colors, expulsion message, cursor, status, and core RNG now match through screen 171. The next blocker is screen 172's visible-map hallucinated glyph stream after the expulsion More is dismissed. The duplicate Warning refresh and object sentinel-bound drift are gone; continue with earlier `docrt()`/`newsym()` display-RNG state and retained glyph timing, not seed-specific glyph sequences.
2. **Broaden menu/discovery data paths after live descriptors** — seed0116 now passes, but discovery persistence and the Tourist starter inventory fallback still rely on limited evidence tables. Keep moving these toward `o_init`/`invent.c`/`pager.c`/`wizcmds.c` truth.
3. **Broaden sleeping/hider front doors only from C evidence** — sleeping `disturb()` wake-up RNG and full mimic `set_mimic_sym()` are still partial. Port those predicates when a session reaches them instead of adding broad approximate wake-up rolls.
4. **Continue hallucination display context after the menu blocker** — `#levelchange` HP/Pw and prompt packing are now modeled, but hallucination display context remains absent for later exact-RNG screen drift.
